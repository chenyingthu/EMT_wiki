---
title: "刚性系统处理 (Stiff System Handling)"
type: method
tags: [stiff-system, numerical-stability, l-stable, multirate, emt-simulation]
created: "2026-05-04"
---

# 刚性系统处理 (Stiff System Handling)

## 定义与边界

刚性系统处理是针对同一 EMT 模型中快衰减模态、慢动态和离散事件共存时的数值策略。刚性不是单纯“系统很复杂”，而是指稳定性要求的时间步长远小于精度要求的时间步长，或某些高频数值模态在常用积分器下难以衰减。

本页关注处理策略：如何识别刚性、选择 L 稳定积分、处理事件后历史项和协调多时间尺度。具体积分公式见 [[numerical-integration]]；多速率分区见 [[multirate-method]]。

## EMT 中的作用

电力电子开关、故障切除、限幅控制、饱和磁化曲线和长短时间常数并存都会使 EMT 离散系统表现出刚性。若处理不足，仿真可能出现：

- 显式方法必须使用远小于研究目标频带所需的步长。
- 梯形法在不连续事件后保留持续数值振荡。
- 非线性迭代收敛困难或历史源与新拓扑不一致。
- 实时仿真为了满足 deadline 被迫使用过度简化的阻尼策略。

## 核心机制

对线性化系统 $\dot{x}=Ax$，若 $A$ 的特征值实部跨度很大，快模态会限制显式步长。常用刚性指标是特征值实部或时间常数的跨度，但在实际 EMT 中还应结合开关事件和控制限幅判断。

处理刚性的核心工具包括：

- 使用后向欧拉、DIRK、BDF 等隐式或 L 稳定方法阻尼高频数值模态。
- 在事件后临时切换到阻尼较强的积分步骤，再恢复常规积分。
- 对快慢子系统采用 [[multirate-method]] 或移频/动态相量模型。
- 对非线性元件使用一致的线性化、牛顿迭代和历史源重构。

以梯形法为例，其稳定函数在刚性极限趋近 $-1$，因此突变后误差可能步间换号而不衰减。后向欧拉和合适的 DIRK 方法在刚性极限趋近 $0$，可作为事件后阻尼机制。

## 分类与变体

| 策略 | 机制 | 适用边界 |
|------|------|----------|
| 后向欧拉阻尼 | 用一阶 L 稳定步骤衰减高频误差 | 可能局部降阶和过度阻尼 |
| 临界阻尼调整 | 事件后插入阻尼步骤，再恢复梯形法 | 需要可靠事件检测和历史项重构 |
| 2S-DIRK/3S-SDIRK | 用多阶段隐式法兼顾阶数和刚性阻尼 | 阶段数增加；非线性仍需迭代 |
| BDF/Gear | 用多步隐式公式处理平滑刚性动态 | 频繁事件会破坏多步历史 |
| 多速率处理 | 快动态小步长、慢动态大步长 | 接口稳定性和采样规则成为关键 |
| 固定导纳等效 | 降低开关导致的矩阵结构突变 | 需检查虚拟损耗和初始误差 |

## 适用边界与失败模式

- 刚性处理不是把所有高频分量都滤掉；若研究目标包括宽频暂态，阻尼策略必须与目标频带匹配。
- 事件检测不完整时，CDA 类策略可能漏掉线路行波、控制限幅或非线性工作点突变。
- BDF 和 Gear 方法在频繁开关场景中需要重启动；直接保留旧历史可能产生伪误差。
- 简化牛顿法和固定雅可比只适合工作点变化较小的非线性区间。
- 多速率分区若把强耦合刚性模态切在接口上，可能比单速率更不稳定。

### 刚性来源识别

| 来源 | 时间常数范围 | 处理策略 |
|------|--------------|----------|
| 电力电子开关 | ns~μs | 小步长或移频模型 |
| 输电线路行波 | μs~ms | 分布参数模型 |
| 电磁暂态 | ms~10ms | 标准EMT步长 |
| 机电振荡 | 0.1s~10s | 多速率或动态相量 |
| 长期动态 | 10s~min | 准稳态或降阶模型 |

### 积分器选择准则

| 场景 | 推荐方法 | 理由 |
|------|----------|------|
| 平滑刚性动态 | BDF/Gear | 高阶、高效 |
| 频繁开关事件 | 2S-DIRK+CDA | L稳定、事件处理 |
| 实时仿真 | 后向欧拉 | 简单、稳定 |
| 高精度需求 | 梯形法+阻尼 | 二阶、能量守恒 |
| 超大规模 | 多速率分区 | 计算效率 |

## 代表性来源

| 来源 | 支撑内容 | 证据边界 |
|------|----------|----------|
| [[numerical-integration-by-the-2-stage-diagonally]] | 2S-DIRK 用积分稳定函数抑制电感电流或电容电压突变后的持续振荡 | 不替代开关事件定位和非线性拓扑处理 |
| [[stability-evaluation-of-interpolation-extrapolation-and-numerical-oscillation-da]] | 插值和 CDA 可在严格无源条件下纳入切换系统稳定性分析 | 充分条件不覆盖所有主动控制或非无源系统 |
| [[three-stage-implicit-integration-for-large-time-step-size-electromagnetic-transi]] | 移频 EMT 中 L 稳定高阶隐式积分可缓解大步长下的梯形法振荡风险 | 结论限于移频建模和作者比较的积分器 |
| [[stability-assessment-of-multi-rate-electromagnetic-transient-simulations]] | 多速率 EMT 的整体稳定性可能由周期接口耦合决定，而非单个积分器决定 | 理论范围主要是 LTI 和整数步长比 |

## 与相关页面的关系

- [[numerical-integration]] 提供 A 稳定、L 稳定、DIRK、BDF 等基础。
- [[interpolation-method]] 用于事件时刻定位，但不单独保证刚性稳定。
- [[multirate-method]] 是多时间尺度刚性的一种分区处理。
- [[fixed-admittance]] 缓解频繁开关导致的矩阵重构，但不自动保证精度。
- [[trapezoidal-rule]] 是常见基线；其非 L 稳定性解释了许多事件后振荡。
- [[backward-euler]] 是常见阻尼步骤；其边界是低阶和数值耗散。

## 形式化表达

### 刚性指标

对于线性系统 $\dot{x} = Ax$，刚性比定义为：

$$S = \frac{\max|\text{Re}(\lambda_i)|}{\min|\text{Re}(\lambda_i)|}$$

其中 $\lambda_i$ 为矩阵 $A$ 的特征值。通常 $S > 10$ 认为是刚性系统，$S > 1000$ 为强刚性。

局部刚性可用条件数估计：
$$\kappa = \|A\| \cdot \|A^{-1}\|$$

### 稳定性函数

单步法的稳定性函数 $R(z)$ 决定数值解的衰减特性：

**梯形法**：
$$R(z) = \frac{1 + z/2}{1 - z/2}$$

当 $z \to -\infty$，$R(z) \to -1$，导致高频振荡不衰减。

**后向欧拉**：
$$R(z) = \frac{1}{1 - z}$$

当 $z \to -\infty$，$R(z) \to 0$，L稳定，高频分量快速衰减。

**2S-DIRK**：
$$R(z) = \frac{1 + (1-2\gamma)z}{(1-\gamma z)^2}, \quad \gamma = 1 - \frac{\sqrt{2}}{2}$$

具有二阶精度和L稳定性。

### 临界阻尼调整(CDA)

事件检测后插入阻尼步骤：

$$x_{n+1/2} = x_n + h f(t_{n+1/2}, x_{n+1/2}) \quad \text{(后向欧拉)}$$

$$x_{n+1} = x_n + \frac{h}{2}[f(t_n, x_n) + f(t_{n+1}, x_{n+1})] \quad \text{(梯形法)}$$

### 多速率分区

快慢子系统分离：
$$\begin{cases} \dot{x}_f = f_f(x_f, x_s, t) & \text{(快子系统，步长 } h_f \text{)} \\ \dot{x}_s = f_s(x_f, x_s, t) & \text{(慢子系统，步长 } h_s = mh_f \text{)} \end{cases}$$

接口稳定性条件：
$$|R_f(z_f)| \cdot |R_s(z_s)|^m < 1$$
