---
title: "稀疏矩阵技术 (Sparse Matrix Techniques)"
type: method
tags: [sparse-matrix, numerical-methods, computational-efficiency, power-system]
created: "2026-05-02"
---

# 稀疏矩阵技术 (Sparse Matrix Techniques)

## 定义与边界

稀疏矩阵技术是利用电力网络矩阵中大量零元素的结构特征来降低存储、组装、分解和求解成本的一组数值方法。它包括稀疏存储格式、重排序、符号分解、数值分解、迭代求解、预处理和稀疏数据结构维护。

本页讨论矩阵层方法，不把“稀疏”直接等同于“快速”或“实时”。实际收益取决于非零元数量、填充元、条件数、开关事件、缓存局部性和并行调度。网络方程背景见 [[nodal-admittance-matrix]]，伴随模型来源见 [[companion-model]]。

## EMT 中的作用

固定步长 EMT 常在每个步长求解：

$$
\mathbf{Y}_k \mathbf{v}_k = \mathbf{i}_{k}
$$

其中 $\mathbf{Y}_k$ 由网络拓扑、伴随导纳、开关状态和等效源共同决定。多数电网拓扑只连接局部节点，因此 $\mathbf{Y}_k$ 的非零元素数量 $nnz$ 通常远小于 $n^2$。稀疏矩阵技术的主要作用是：

- 避免存储完整 $n \times n$ 矩阵。
- 在结构稳定时复用符号分析、重排序和分解结构。
- 降低每步前代/回代或迭代求解成本。
- 为 [[gpu-parallel-acceleration]]、[[high-performance-computing]] 和 [[heterogeneous-computing]] 提供可并行数据结构。

## 存储格式

常见格式包括：

| 格式 | 适用环节 | 主要风险 |
|------|----------|----------|
| CSR | 按行访问、SpMV、迭代法 | 插入新非零元成本高 |
| CSC | 稀疏直接法、列消去 | 行访问不便 |
| COO | 矩阵组装、并行累加中间态 | 求解前通常需排序和压缩 |
| Block CSR | 三相或多端口块结构 | 块大小不均会浪费存储 |

对 EMT 而言，矩阵组装阶段和求解阶段可能需要不同格式。COO 适合并行收集元件贡献，CSR/CSC 适合后续求解；格式转换本身也应计入性能评估。

## 符号分解与数值分解

稀疏直接法通常分为：

1. 重排序：寻找置换矩阵 $\mathbf{P}$，使 $\mathbf{PYP}^T$ 的填充元较少。
2. 符号分解：确定 $L$ 和 $U$ 的非零结构。
3. 数值分解：计算具体数值。
4. 前代/回代：求解 $\mathbf{Ly}=\mathbf{b}$、$\mathbf{Ux}=\mathbf{y}$。

若开关状态不改变矩阵结构，符号分解可在多个步长复用；若拓扑频繁改变，则必须重新检查结构、排序或分解，收益会下降。

## 排序与填充控制

填充元是稀疏直接法的核心成本来源。常见排序包括最小度、近似最小度、嵌套剖分和电力系统传统 Tinney 类排序。应把这些算法写成启发式或结构依赖方法，而不是“最优排序”的保证。

填充控制可用填充因子描述：

$$
\rho_{fill}=\frac{nnz(L)+nnz(U)}{nnz(Y)}
$$

该指标需要绑定具体网络、排序算法和主元策略；不同拓扑、三相建模和电力电子开关会产生不同填充模式。

## 直接法与迭代法

### 直接法

稀疏 LU/Cholesky 适合需要稳定、可重复求解的中小到大规模网络，特别是矩阵结构稳定、右端项频繁变化的场景。失败模式包括主元过小、填充元失控、内存不足和开关导致的结构频繁改变。

### 迭代法

CG、BiCGSTAB、GMRES 等迭代法以 SpMV、向量内积和预处理为核心。每次迭代常见成本为 $O(nnz)$，但总成本还取决于迭代次数和预处理质量。对于非对称、不定或病态矩阵，收敛性不能默认保证。

### 预处理

Jacobi、ILU、块 Jacobi、AMG 等预处理可降低迭代次数，但会引入额外构造成本和并行化难度。预处理是否复用，取决于矩阵数值和结构变化速度。

## EMT 实施要点

- 元件组装应避免并行写冲突；常用策略是局部缓冲、原子累加或先 COO 后压缩。
- 开关事件应区分“只改数值”和“改变结构”，因为二者对分解复用影响不同。
- 三相不平衡模型可保留块结构，避免把自然小块完全打散。
- 稀疏求解应同时报告重排序、分解、求解、格式转换和数据传输时间。

## 适用边界与失败模式

- 稀疏度高不必然意味着求解快；填充元和内存访问模式可能主导成本。
- GPU 上的稀疏计算可能受不规则访问和负载不均限制。
- 迭代法在强非线性、开关密集或病态网络上可能需要频繁重启或改用直接法。
- 只报告 $nnz$ 而不报告填充、条件数、步长、拓扑变化和收敛准则，证据不足。

## 代表性证据

- [[nodal-admittance-matrix]] 说明节点方程为何天然形成稀疏网络矩阵。
- [[companion-model]] 说明 EMT 元件离散化后如何贡献等效导纳和历史源。
- [[improving-numerical-efficiency-of-frequency-dependent-transmission-line-models-f]] 支撑“数值模型改写影响计算成本”的单篇证据边界。

## 与相关页面的关系

- [[computational-acceleration]]：把稀疏技术放入更大的加速策略中。
- [[gpu-parallel-acceleration]]：讨论 CSR/SpMV 等在 GPU 上的实现问题。
- [[high-performance-computing]]：讨论稀疏求解在多节点并行中的通信和负载均衡。
- [[heterogeneous-computing]]：讨论稀疏组装、求解和事件处理在不同硬件间的分配。
- [[network-partitioning]]：通过分区改变矩阵块结构和接口耦合。

## 开放问题

- 如何建立同时覆盖开关频率、填充元、收敛性和缓存行为的 EMT 稀疏求解基准。
- 如何在 GPU 或异构系统中维护可复用的符号结构。
- 如何把数值稳定性和计算性能共同纳入方法比较。
