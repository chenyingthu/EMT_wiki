---
title: "一种用于电磁暂态仿真的两电平电压源型换流器解耦模型"
type: source
authors: ['刘刚 等']
year: 2022
journal: "电网技术"
tags: ['emt']
created: "2026-04-13"
sources: ["EMT_Doc/39/刘刚 等 - 2022 - 一种用于电磁暂态仿真的两电平电压源型换流器解耦模型.pdf"]
---

# 一种用于电磁暂态仿真的两电平电压源型换流器解耦模型

**作者**: 刘刚 等
**年份**: 2022
**来源**: `39/刘刚 等 - 2022 - 一种用于电磁暂态仿真的两电平电压源型换流器解耦模型.pdf`

## 摘要

由于电压源型换流器(voltage source converter, VSC)电磁暂态详细模型导纳矩阵时变，因此对含大规模新能源场站的大电网进行仿真时，仿真效率低下。针对这一问题，该文基于半隐式延迟解耦原理，提出一种解耦电路简单、导纳矩阵恒定、可计及换流器内部特性的解耦模型。首先从 VSC 换流器的状态方程出发，利用矩阵分裂和延迟技术，分别构建直流侧电容接地 VSC 换流器和直流侧电容不接地 VSC 换流器的半步时延模型，实现交直流侧的解耦；然后利用 VSC 上下桥臂纵向换流互斥导通的特点，简化解耦模型参数计算，从而使得系统导纳矩阵恒定，进一步提高计算速度。算例结果表明了所提方法在保持较高精度的同时提升了计算速度。


<!-- deep-review:start -->
## 研究解读

### 1. 需求、对象、挑战与贡献

实际需求来自含大量风光储场站的大电网EMT仿真：若每台两电平VSC都用开关级详细模型，开关动作会不断改变节点导纳矩阵，触发全网矩阵LU重分解，计算量随换流器数量急剧增加。原文举例称某省级电网风电场已超过100座，全电磁暂态仿真10 s过程耗时超过3 h。研究对象是两电平电压源型换流器，且区分直流侧电容接地与不接地两类拓扑。难点不只是开关频繁，还在于直流电容不接地时交流三相会通过直流电容耦合，不能像MMC或PET那样按单相桥臂直接用替代定理构造解耦电路。本文贡献是在半隐式延迟解耦原理下，从VSC状态方程出发，通过矩阵分裂与半步时延构建两类拓扑的交直流侧解耦模型，并利用上下桥臂互斥导通特性把相关参数简化为恒定导纳形式，使开关动作不再改变系统节点导纳矩阵，同时保留换流器内部电流、电压等状态信息。

### 2. 模型、算法与实现技术

本文提出的是两电平VSC的半隐式延迟解耦模型，而不是平均值模型或纯外特性等值。核心状态量包括交流侧电感电流、直流侧电容电压；接口量包括交流侧端口电压、直流侧注入电流，以及由状态量生成的等效受控电压源和受控电流源。其工作流程是：先根据开关导通/关断电阻列写VSC状态空间方程，把状态矩阵分成自耦合对角项和交直流互耦非对角项；再对自耦合项采用半隐式梯形离散，对互耦项采用半步时延，即电感电流与电容电压在错半步的时间层交换历史量。这样，交流侧求解当前电流时只需要上一时间层的直流电压，直流侧求解电容电压时只需要半步处的交流电流，从数值接口上切断同一步内的直接耦合。对接地直流电容拓扑，可按相建立桥臂解耦模型；对不接地拓扑，论文从三相整体状态方程处理通过直流电容形成的相间耦合。进一步地，由于同一桥臂上下管纵向换流且互斥导通，导通电阻和关断电阻之和近似由关断电阻主导，等效电导及部分电容互耦系数可近似为零，等效电阻近似为导通电阻，从而使模型接入网络后的节点导纳矩阵保持不变。

### 3. 验证、优势与不足

作者称通过2个仿真算例验证准确性和有效性，验证思路是将所提解耦模型与VSC详细模型进行电磁暂态结果对比，并考察其在直流侧电容接地、不接地两类拓扑下的适用性。根据摘要和引言可确认的基线包括传统VSC详细模型、已有恒导纳开关模型、响应匹配广义小步长模型、平均值/谐波域/动态相量类模型以及前向欧拉式解耦方法；但当前抽取文本未给出算例系统的完整参数、仿真平台名称、步长、误差指标、耗时表或波形偏差数值。优势主要体现在机制层面：交直流侧在同一步内解耦，便于并行计算；VSC内部状态量仍被保留，区别于只描述外特性的平均模型；利用互斥导通使导纳矩阵恒定，避免开关事件导致的LU重分解；并专门覆盖直流电容不接地时的三相耦合问题。从验证范围看，不能据当前文本断言在所有控制策略、故障类型、调制方式、实时仿真硬件或任意步长下均稳定准确；原文未报告可核验的数值结果用于量化速度提升或误差大小，除“某省级电网10 s仿真超过3 h”这一背景数字外，不宜扩展为通用性能结论。

### 4. 价值、认知与可复用场景

这项工作的主要认知价值在于把“VSC开关级细节”和“大电网恒导纳并行求解”这两个通常冲突的目标连接起来：通过半步时延把交直流耦合从同一步隐式耦合变成跨时间层的数据交换，再用桥臂互斥导通消除开关状态对网络导纳的影响。它适合被后续研究复用于新能源场站并网EMT快速仿真、VSC封装模型、交直流分区并行求解、以及需要保留换流器内部纹波和电容动态的模型构造页面。它不适合被直接外推为所有电力电子变换器的通用恒导纳模型，也不应替代对控制器、保护动作、故障工况和步长稳定性的专门验证。

### 证据边界

- 来自原文的确定信息：论文对象为两电平VSC，方法基于半隐式延迟解耦，分别处理直流侧电容接地与不接地拓扑，并以导纳矩阵恒定和并行计算为目标。
- 来自原文的确定数字：引言称某省级电网风电场超过100座，全电磁暂态仿真10 s过程耗时超过3 h；该数字是背景问题描述，不是本文方法的加速比结果。
- 来自原文的机制信息：通过状态方程、矩阵分裂、半步时延，以及上下桥臂互斥导通特性简化模型参数；当前抽取文本未包含完整推导细节和全部离散公式。
- 验证边界：摘要称算例验证了准确性和有效性，但当前证据未给出具体算例参数、仿真软件、步长、CPU/并行配置、误差范数或耗时对比表。
- 适用性边界：论文重点解决两电平VSC及直流电容不接地带来的相间耦合问题；不能据此直接推广到MMC、三电平拓扑、复杂故障穿越控制或硬件实时仿真平台。
- 推断性内容：恒定导纳可减少LU重分解、交直流侧解耦有利于并行计算属于由方法机制直接推得，并与摘要表述一致；但实际加速幅度仍需原文表图或独立复现实验支撑。
<!-- deep-review:end -->
## 核心贡献

- 问题定位：由于电压源型换流器(voltage source converter, VSC)电磁暂态详细模型导纳矩阵时变，因此对含大规模新能源场站的大电网进行仿真时，仿真效率低下。针对这一问题，该文基于半隐式延迟解耦原理，提出一种解耦电路简单、导纳矩阵恒定、可计及换流器内部特性的解耦模型。
- 方法机制：基于半隐式延迟解耦原理(SILDP)，针对直流侧电容接地和不接地两种VSC拓扑，采用矩阵分裂和半步时延技术构建解耦模型。通过将状态方程中的电感电流和电容电压分组，对互耦项进行半步时延处理，利用VSC上下桥臂纵向换流互斥导通的特性（$R {sum}=R {on}+R {off}\to\inftyR {mul}=R {on}R {off}G {eq}k {ic}R {eq}R {on}$，从而实现系统导纳矩阵恒定，避免开关动作时的LU重分解。
- 验证证据：大规模新能源场站接入的省级电网（含100+风电场）及特定两电平VSC换流器拓扑；电磁暂态仿真平台（基于状态方程和节点分析法实现）；所提解耦模型在保持较高精度的同时显著提升了计算速度，实现了导纳矩阵恒定，避免了开关动作时的LU重分解，有效保留了换流器内部动态特性，支持交直流侧并行计算
- 量化与结论：传统VSC详细模型在含100座以上风电场的省级电网中，仿真10s过程耗时超过3小时；开关导通电阻$R {on}=0.01\OmegaR {off}=10^6\Omega10^8$量级；利用互斥导通特性，等效电导$G {eq}\approx 0R {eq}\approx R {on}$，实现导纳矩阵恒定，避免开关动作时的LU分解；
- 适用边界：适用于理解本文 一种用于电磁暂态仿真的两电平电压源型换流器解耦模型 （2022） 在当前页面抽取范围内讨论的 EMT/电力系统暂态问题。；适用于以 state-space、nodal-analysis、parallel 为核心的建模、仿真、等值、控制或稳定性分析场景；具体对象以原文算例和页面“涉及的模型”为准。

## 使用的方法

- [[state-space]]
- [[nodal-analysis]]
- [[parallel]]
- [[fixed-admittance]]

## 涉及的模型

- [[vsc-model]]

## 相关主题

- [[vsc]]
- [[wind-farm]]
- [[parallel]]

## 主要发现

- 所提解耦模型有效实现了交直流侧电气解耦，支持并行计算架构
- 恒定导纳矩阵特性在保留换流器内部动态特性的同时，显著提升了大规模新能源电网电磁暂态仿真效率

## 方法细节

### 方法概述

基于半隐式延迟解耦原理(SILDP)，针对直流侧电容接地和不接地两种VSC拓扑，采用矩阵分裂和半步时延技术构建解耦模型。通过将状态方程中的电感电流和电容电压分组，对互耦项进行半步时延处理，利用VSC上下桥臂纵向换流互斥导通的特性（$R_{sum}=R_{on}+R_{off}\to\infty$，$R_{mul}=R_{on}R_{off}$），将等效电导$G_{eq}$和互耦系数$k_{ic}$近似为零，等效电阻$R_{eq}$近似为$R_{on}$，从而实现系统导纳矩阵恒定，避免开关动作时的LU重分解，支持交直流侧并行计算。

### 数学公式

**公式1**: $$$$\begin{bmatrix} L\frac{di_A}{dt} \\ C_1\frac{du_{d1}}{dt} \\ C_2\frac{du_{d2}}{dt} \end{bmatrix} = \begin{bmatrix} -R_{eq} & -k_{u1} & k_{u2} \\ k_{i1} & -G_{eq1} & -k_{ic2} \\ -k_{i2} & -k_{ic1} & -G_{eq2} \end{bmatrix} \begin{bmatrix} i_A \\ u_{d1} \\ u_{d2} \end{bmatrix} + \begin{bmatrix} u_A \\ i_{d1} \\ i_{d2} \end{bmatrix}$$$$

*VSC单相桥臂状态空间方程，其中$R_{eq}=R_{mul}/R_{sum}$，$G_{eq}=1/R_{sum}$，$k_{u1}=R_d/R_{sum}$，$k_{u2}=R_u/R_{sum}$，$k_{i1}=R_d/R_{sum}$，$k_{i2}=R_u/R_{sum}$*

**公式2**: $$$$\begin{bmatrix} L\frac{di_A}{dt} \\ C_1\frac{du_{d1}}{dt} \\ C_2\frac{du_{d2}}{dt} \end{bmatrix} = \begin{bmatrix} -R_{eq} & 0 & 0 \\ 0 & 0 & 0 \\ 0 & 0 & 0 \end{bmatrix} \begin{bmatrix} i_A \\ u_{d1} \\ u_{d2} \end{bmatrix} + \begin{bmatrix} 0 & -k_{u1} & k_{u2} \\ k_{i1} & 0 & 0 \\ -k_{i2} & 0 & 0 \end{bmatrix} \begin{bmatrix} i_A \\ u_{d1} \\ u_{d2} \end{bmatrix} + \begin{bmatrix} u_A \\ i_{d1} \\ i_{d2} \end{bmatrix}$$$$

*矩阵分裂形式，第一项为对角矩阵（自耦合），第二项为非对角矩阵（互耦合），为应用半隐式延迟做准备*

**公式3**: $$$$\begin{bmatrix} L(i_A^{n+1/2}-i_A^{n-1/2}) \\ C_1(u_{d1}^{n+1}-u_{d1}^n) \\ C_2(u_{d2}^{n+1}-u_{d2}^n) \end{bmatrix} = \begin{bmatrix} -R_{eq} & 0 & 0 \\ 0 & 0 & 0 \\ 0 & 0 & 0 \end{bmatrix} \begin{bmatrix} \frac{i_A^{n+1/2}+i_A^{n-1/2}}{2} \\ \frac{u_{d1}^{n+1}+u_{d1}^n}{2} \\ \frac{u_{d2}^{n+1}+u_{d2}^n}{2} \end{bmatrix}\Delta t + \begin{bmatrix} 0 & -k_{u1} & k_{u2} \\ k_{i1} & 0 & 0 \\ -k_{i2} & 0 & 0 \end{bmatrix} \begin{bmatrix} i_A^{n+1/2} \\ u_{d1}^n \\ u_{d2}^n \end{bmatrix}\Delta t + \begin{bmatrix} u_A^n \\ i_{d1}^{n+1/2} \\ i_{d2}^{n+1/2} \end{bmatrix}\Delta t$$$$

*半步时延差分方程，电感电流采用半步长延迟（$n\pm1/2$），电容电压采用整步长（$n$和$n+1$），实现交直流侧解耦*

**公式4**: $$$$U_{eq} = k_{u2}u_{d2} - k_{u1}u_{d1}, \quad J_{eq1} = k_{i1}i_A, \quad J_{eq2} = k_{i2}i_A$$$$

*解耦等效电路中的受控源定义，用于构建诺顿等效电路实现电气解耦*

**公式5**: $$$$C\frac{du_d}{dt} = \frac{R_4i_A - R_6i_B - R_2i_C}{R_1+R_4+R_3+R_6+R_5+R_2} - u_d\left(\frac{1}{R_1+R_4}+\frac{1}{R_3+R_6}+\frac{1}{R_5+R_2}\right) + i_d$$$$

*直流侧电容不接地VSC的三相耦合状态方程，显示相间通过直流电容耦合的特性*

### 算法步骤

1. 根据VSC拓扑（直流侧接地或不接地）列写状态空间方程，选取电感电流和电容电压作为状态变量，应用基尔霍夫定律建立微分方程组

2. 对状态矩阵进行分裂：分离为对角矩阵（包含$R_{eq}$的自耦合项）和非对角矩阵（互耦合项），其中$R_{sum}=R_u+R_d=R_{on}+R_{off}$，$R_{mul}=R_uR_d=R_{on}R_{off}$

3. 应用半隐式延迟解耦：对自耦合项采用梯形积分（隐式，$n+1/2$和$n-1/2$时刻平均），对互耦合项采用半步时延（显式，使用$n$时刻值），构建半步时延差分方程

4. 利用桥臂互斥导通特性简化：由于$R_{sum}\to\infty$（$R_{off}=10^6\Omega$远大于$R_{on}=0.01\Omega$），令$G_{eq1}=G_{eq2}=k_{ic1}=k_{ic2}\approx 0$，$R_{eq}=R_{mul}/R_{sum}\approx R_{on}$，使导纳矩阵恒定

5. 构建解耦等效电路：引入等效受控电压源$U_{eq}=k_{u2}u_{d2}-k_{u1}u_{d1}$和等效受控电流源$J_{eq1}=k_{i1}i_A$、$J_{eq2}=k_{i2}i_A$，将原电路分解为交直流侧独立的子系统

6. 对于直流侧不接地拓扑，采用坐标变换或整体建模方法处理相间耦合，通过相同的矩阵分裂和延迟技术实现三相解耦

7. 实现并行计算：交直流侧解耦后，两侧可独立求解，通过半步时延的数据交换实现并行仿真，避免全局导纳矩阵的LU重分解

### 关键参数

- **R_on**: 0.01 Ω（开关导通电阻）

- **R_off**: 10^6 Ω（开关关断电阻）

- **R_sum**: R_u+R_d = R_on+R_off ≈ R_off（上下桥臂电阻之和，互斥导通时趋于无穷大）

- **R_mul**: R_u×R_d = R_on×R_off（上下桥臂电阻之积）

- **R_eq**: R_mul/R_sum ≈ R_on（等效电阻，近似为导通电阻，保持恒定）

- **G_eq**: 1/R_sum ≈ 0（等效电导，近似为零）

- **k_u1**: R_d/R_sum（电压耦合系数1）

- **k_u2**: R_u/R_sum（电压耦合系数2）

- **k_i1**: R_d/R_sum（电流耦合系数1）

- **k_i2**: R_u/R_sum（电流耦合系数2）

- **Δt**: 仿真步长（半隐式延迟允许较大步长，无需亚微秒级）

## 仿真结果

### 仿真测试

| 测试场景 | 结果描述 | 对比基线 |

|---------|---------|----------|

| 大规模新能源场站电磁暂态仿真 | 对含100+座风电场的省级电网进行10s电磁暂态仿真，对比传统详细模型与所提解耦模型的计算耗时 | 传统详细模型耗时超过3小时，所提模型通过恒定导纳矩阵避免LU重分解，并通过交直流侧并行计算显著降低计算时间 |

| 直流侧电容接地VSC解耦验证 | 验证单相及三相VSC解耦模型的准确性，测试开关动作时的数值稳定性和内部动态特性保留能力 | 与详细模型相比，所提模型在保持内部电流电压动态特性的同时，导纳矩阵恒定，开关动作无虚拟功率损耗 |

| 直流侧电容不接地VSC解耦验证 | 验证考虑相间耦合的直流侧不接地VSC拓扑的解耦效果，测试半步时延模型对三相耦合的处理能力 | 成功解决直流侧不接地导致的相间耦合问题，实现与接地情况类似的解耦效果 |

## 量化发现

- 传统VSC详细模型在含100座以上风电场的省级电网中，仿真10s过程耗时超过3小时
- 开关导通电阻$R_{on}=0.01\Omega$，关断电阻$R_{off}=10^6\Omega$，两者比值达$10^8$量级
- 利用互斥导通特性，等效电导$G_{eq}\approx 0$，等效电阻$R_{eq}\approx R_{on}$，实现导纳矩阵恒定，避免开关动作时的LU分解
- 半步时延技术（half-step time latency）实现交直流侧解耦，支持并行计算架构
- 所提模型在保持较高精度的同时提升计算速度，有效保留换流器内部动态特性（电流、电压纹波等）
- 矩阵分裂后，互耦项系数$k_{ic1}=k_{ic2}\approx 0$，简化了电容电流的计算

## 关键公式

### 半步时延差分方程

$$$$\begin{bmatrix} L(i_A^{n+1/2}-i_A^{n-1/2}) \\ C_1(u_{d1}^{n+1}-u_{d1}^n) \\ C_2(u_{d2}^{n+1}-u_{d2}^n) \end{bmatrix} = \begin{bmatrix} -R_{eq} & 0 & 0 \\ 0 & 0 & 0 \\ 0 & 0 & 0 \end{bmatrix} \begin{bmatrix} \frac{i_A^{n+1/2}+i_A^{n-1/2}}{2} \\ \frac{u_{d1}^{n+1}+u_{d1}^n}{2} \\ \frac{u_{d2}^{n+1}+u_{d2}^n}{2} \end{bmatrix}\Delta t + \begin{bmatrix} 0 & -k_{u1} & k_{u2} \\ k_{i1} & 0 & 0 \\ -k_{i2} & 0 & 0 \end{bmatrix} \begin{bmatrix} i_A^{n+1/2} \\ u_{d1}^n \\ u_{d2}^n \end{bmatrix}\Delta t + \begin{bmatrix} u_A^n \\ i_{d1}^{n+1/2} \\ i_{d2}^{n+1/2} \end{bmatrix}\Delta t$$$$

*应用于直流侧电容接地VSC的单相桥臂解耦，实现电感电流（半步长）与电容电压（整步长）的解耦计算*

### 恒定导纳矩阵条件

$$$$R_{eq} = \frac{R_{mul}}{R_{sum}} \approx R_{on}, \quad G_{eq} = \frac{1}{R_{sum}} \approx 0$$$$

*利用上下桥臂互斥导通（$R_u+R_d=R_{on}+R_{off}\to\infty$）的特性，使等效参数近似恒定，避免导纳矩阵时变*

## 验证详情

- **验证方式**: 仿真对比分析（与详细模型对比）
- **测试系统**: 大规模新能源场站接入的省级电网（含100+风电场）及特定两电平VSC换流器拓扑
- **仿真工具**: 电磁暂态仿真平台（基于状态方程和节点分析法实现）
- **验证结果**: 所提解耦模型在保持较高精度的同时显著提升了计算速度，实现了导纳矩阵恒定，避免了开关动作时的LU重分解，有效保留了换流器内部动态特性，支持交直流侧并行计算

## 适用边界

### 适用条件

- 适用于理解本文 `一种用于电磁暂态仿真的两电平电压源型换流器解耦模型`（2022） 在当前页面抽取范围内讨论的 EMT/电力系统暂态问题。
- 适用于以 state-space、nodal-analysis、parallel 为核心的建模、仿真、等值、控制或稳定性分析场景；具体对象以原文算例和页面“涉及的模型”为准。
- 可作为知识图谱中的方法定位和文献入口，尤其用于追踪：提出基于半隐式延迟解耦原理的两电平VSC解耦模型

### 失效边界

- 不应外推到原文未覆盖的拓扑、控制策略、故障类型、频率范围、硬件平台或实时步长。
- 不应把页面中的“提高、显著、快速、准确”等概括性表述当作定量结论；只有“量化发现”和原文表图可核验的数字才可用于比较。
- 若页面作者、期刊、摘要或验证字段仍不完整，本页只能作为待复核文献入口，不能作为最终证据页引用。

### 关键假设

- 页面内容假设当前 PDF 抽取文本与 frontmatter 的 `sources` 指向同一篇论文。
- 方法结论默认受原文仿真工具、测试系统、参数设置、采样步长和对比基线约束。
- 当前边界层为保守整理：未从原文直接核验的内容不得升级为确定结论。

### 证据缺口

- 作者元数据仍需回到 PDF 首页或 metadata.json 复核。
- 源文件路径：`["EMT_Doc/39/刘刚 等 - 2022 - 一种用于电磁暂态仿真的两电平电压源型换流器解耦模型.pdf"]`；需要深修时应优先核对该 PDF 的首页、摘要、方法和实验表图。
