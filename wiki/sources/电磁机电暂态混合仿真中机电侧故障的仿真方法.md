---
title: "电磁–机电暂态混合仿真中机电侧故障的仿真方法"
type: source
authors: ['张怡']
year: 2012
journal: "电力系统自动化"
tags: ['emt', 'cosimulation']
created: "2026-04-13"
sources: ["EMT_Doc/35/张怡 等 - 2012 - 电磁–机电暂态混合仿真中机电侧故障的仿真方法.pdf"]
---

# 电磁–机电暂态混合仿真中机电侧故障的仿真方法

**作者**: 张怡
**年份**: 2012
**来源**: `35/张怡 等 - 2012 - 电磁–机电暂态混合仿真中机电侧故障的仿真方法.pdf`

## 摘要

基于频率相关网络等值(frequency dependent network equivalent，FDNE)的电磁–机电暂态解耦混合仿真系统一般不能处理机电暂态侧网络的故障。首先提出一种伴随仿真方法来初始化多个 FDNE 的状态，以实现 FDNE 的平稳切换。另外，通过大量仿真研究提出一种临界电气距离指标，用于快速鉴别机电暂态侧故障时是否需要在仿真中修改 FDNE。最后，提出一种基于上述临界电气距离指标的 FDNE 切换策略以减小总体计算量。仿真结果表明基于临界电气距离指标的 FDNE 切换策略能够保证混合仿真的精度和效率。


<!-- deep-review:start -->
## 研究解读

### 1. 需求、对象、挑战与贡献

工程需求来自电磁–机电暂态混合仿真：EMT侧常用于表示HVDC等含快速电磁过程和波形畸变的局部系统，TSA侧用于承载大规模交流网络。基于FDNE的解耦混合仿真能把机电侧网络的频率特性等值到接口，但当机电侧发生短路、跳闸、重合等拓扑变化时，等值网络的频率响应随之改变，故障前FDNE不再严格适用。难点有两层：一是若故障瞬间直接替换为故障后FDNE，其内部LC储能状态变量未知，会在EMT侧引入非物理暂态波动；二是若每个机电侧故障都在线重新采样、矢量拟合并无源校正FDNE，计算负担很重。本文贡献是把问题拆成“如何平滑切换”和“何时必须切换”：提出伴随仿真/热备用方法初始化故障后FDNE状态，并提出临界电气距离指标，用于快速判断某机电侧故障对EMT接口等值的影响是否足以要求FDNE更新。

### 2. 模型、算法与实现技术

本文核心对象是FDNE的状态空间模型：状态量为等值网络内部储能元件对应的x，接口量为端口电压U和注入电流I，矩阵A、B、C、D描述频率相关等值在时域中的动态关系。故障后FDNE的频率采样矩阵通过补偿法获得，即在机电侧拓扑变化后修正网络导纳，再经矢量拟合得到有理函数形式并进行无源校正。切换机制不是在故障时“冷启动”新FDNE，而是在故障前让FDNE2伴随运行：它使用与在线FDNE1相同的端口电压轨迹进行状态演化，但不参与EMT网络求解；故障发生且需要切换时，将接口从FDNE1转到FDNE2，使新FDNE的内部状态已与历史激励一致，从而减小状态变量不连续造成的虚假暂态。临界电气距离则基于潮流雅可比中的电压–无功灵敏度构造，利用节点j扰动对接口节点i电压变化的传递系数αij，并取Dij=-lg(αij)量化故障点与EMT接口的电气接近程度；距离小于临界值时更新FDNE，距离较大时沿用故障前FDNE。

### 3. 验证、优势与不足

作者采用算例仿真说明方法的准确性和效率。原文摘要称使用了多个系统作为示例；引言和方法证据表明验证重点包括两类比较：一类比较FDNE直接切换与伴随仿真切换，观察故障瞬间EMT侧是否出现由状态变量初始化不当引起的暂态波动；另一类通过大量故障算例考察电气距离与FDNE频率响应变化、混合仿真误差之间的关系，从而确定何种故障需要采用故障后FDNE。基线逻辑包括故障前FDNE、故障后FDNE以及更完整的电磁暂态仿真或参考结果，但在提供的原文片段中未列出具体软件、步长、系统规模、误差阈值或可核验数值结果。优势在于方法直接对应混合仿真中的两个实际瓶颈：热备用解决FDNE替换时内部状态不可知的问题，电气距离筛选避免对所有机电侧故障无差别地重新构造FDNE。从验证范围看，结论主要支撑三相或文中设置的机电侧故障场景；对不平衡故障、强控制交互、不同频率拟合带宽、实时仿真平台和极端拓扑变化的适用性，不能仅由该证据外推。

### 4. 价值、认知与可复用场景

这项工作的主要认知价值是指出：FDNE混合仿真不能只关注接口数值耦合，还必须把机电侧拓扑变化导致的等值频率响应变化和等值内部状态连续性作为同一问题处理。它可用于后续研究中的FDNE动态更新、EMT–TSA解耦接口、HVDC邻域精细建模、大系统故障筛选和混合仿真调度策略。工程上，它适合复用为“远端故障不更新、近端故障热备用切换”的判据框架。但不应把临界电气距离视为通用常数，也不应未经重新标定就推广到其他网络、故障类型、控制器配置或实时硬件步长。

### 证据边界

- 原文明确给出的问题、三项贡献和关键词：FDNE不能直接处理机电侧故障、提出伴随仿真初始化、提出临界电气距离指标和基于该指标的切换策略。
- 原文片段明确说明FDNE求取包括频率采样、矢量拟合和无源校正，故障后采样矩阵用补偿法；但片段未展开补偿法的完整导纳矩阵公式和参数。
- 伴随仿真的工作机理来自原文第1节文字和图2：故障后FDNE在切换前利用相同端口电压进行伴随计算；具体开关时序、数值积分格式和初始化误差未在片段中量化。
- 临界电气距离的数学定义在当前页面中给出，但提供的原文证据片段尚未展示完整推导、临界值选取方法和具体阈值，因此不能报告通用Dcr数值。
- 原文摘要称“Several systems”验证准确性和效率，但提供证据未给出系统名称、仿真工具、步长、故障类型全集、误差曲线或计算时间表；因此应写为原文未报告可核验的数值结果。
- 关于大规模效率提升、FDNE开销比例、可避免多少次更新等百分比，当前证据片段中不可核验；除非回到PDF表图确认，否则不应作为确定结论引用。
<!-- deep-review:end -->
## 核心贡献

- 问题定位：基于频率相关网络等值(frequency dependent network equivalent，FDNE)的电磁–机电暂态解耦混合仿真系统一般不能处理机电暂态侧网络的故障。首先提出一种伴随仿真方法来初始化多个 FDNE 的状态，以实现 FDNE 的平稳切换。
- 方法机制：提出基于临界电气距离指标的FDNE（频率相关网络等值）动态切换策略，解决电磁-机电暂态混合仿真中机电侧故障模拟问题。核心包括：(1)伴随仿真（热备用）技术：故障前同时运行故障前FDNE(FDNE1)和故障后FDNE(FDNE2)，用FDNE1端口电压初始化FDNE2状态但保持FDNE2离线，故障瞬间切换以消除状态变量跳变；(2)补偿法求取故障后FDNE：通过移除故障元件正常贡献(-y)并添加故障贡献(Yf)修改导纳矩阵；
- 验证证据：仿真对比验证：将基于FDNE的混合仿真结果与全模型EMT仿真（作为基准）对比，验证伴随仿真初始化方法和临界电气距离指标的有效性；修改后的新英格兰IEEE 39节点系统（含HVDC接口）和IEEE 118节点系统，机电侧设置三相短路接地故障；
- 量化与结论：FDNE计算开销占混合仿真总计算量的约14%，伴随仿真方法虽然需同时运行两个FDNE，但仅增加14%计算量，属于可接受范围；电气距离D与频率响应变化量ΔEFR、暂态误差ΔEtrans存在明确的负相关关系，可用对数灵敏度指标Dij = -lg(αij)有效量化故障影响范围；
- 适用边界：适用于理解本文 电磁–机电暂态混合仿真中机电侧故障的仿真方法 （2012） 在当前页面抽取范围内讨论的 EMT/电力系统暂态问题。；适用于以 co-simulation、vector-fitting、network-equivalent 为核心的建模、仿真、等值、控制或稳定性分析场景；具体对象以原文算例和页面“涉及的模型”为准。

## 使用的方法

- [[co-simulation]]
- [[vector-fitting]]
- [[network-equivalent]]

## 涉及的模型

- [[fdne-model]]

## 相关主题

- [[frequency-dependent-modeling]]
- [[co-simulation]]
- [[network-equivalent]]

## 主要发现

- 伴随仿真技术能有效初始化新FDNE状态，大幅减少切换过程中的数值暂态波动
- 临界电气距离指标可准确鉴别故障影响范围，避免不必要的FDNE重算与切换
- 基于该指标的切换策略在维持混合仿真精度的前提下，有效提升了大规模网络仿真效率

## 方法细节

### 方法概述

提出基于临界电气距离指标的FDNE（频率相关网络等值）动态切换策略，解决电磁-机电暂态混合仿真中机电侧故障模拟问题。核心包括：(1)伴随仿真（热备用）技术：故障前同时运行故障前FDNE(FDNE1)和故障后FDNE(FDNE2)，用FDNE1端口电压初始化FDNE2状态但保持FDNE2离线，故障瞬间切换以消除状态变量跳变；(2)补偿法求取故障后FDNE：通过移除故障元件正常贡献(-y)并添加故障贡献(Yf)修改导纳矩阵；(3)临界电气距离指标：基于潮流雅可比矩阵无功-电压灵敏度定义电气距离，量化故障对EMT侧的影响程度，仅对电气距离小于临界值的故障触发FDNE切换。

### 数学公式

**公式1**: $$\begin{cases} \dot{x} = Ax + BU \\ I = Cx + DU \end{cases}$$

*FDNE的状态空间表示，其中x为N×1维状态变量（含LC储能元件），I为节点注入电流，U为节点电压，A、B、C、D为状态空间矩阵*

**公式2**: $$\Delta E_{FR} = \frac{1}{n} \sum_{i=1}^{n} |Y_{ri}(i\Delta\omega) - Y_{si}(i\Delta\omega)|^2$$

*频率响应误差指标，量化故障前后FDNE频率采样值差异，Yr为故障前FDNE导纳，Ys为故障后FDNE导纳*

**公式3**: $$\Delta E_{trans} = \frac{1}{n} \sum_{i=1}^{n} |y_{ri}(i\Delta t) - y_{si}(i\Delta t)|^2$$

*电磁暂态侧暂态误差指标，比较全模型EMT仿真结果(ys)与采用故障后FDNE的混合仿真结果(yr)的均方根误差*

**公式4**: $$\Delta U = \frac{\partial U}{\partial Q} \Delta Q$$

*电压变化量与无功功率变化量的灵敏度关系，用于推导电气距离*

**公式5**: $$D_{ij} = -\lg \alpha_{ij}, \quad \alpha_{ij} = \frac{(\partial U/\partial Q)_{ij}}{(\partial U/\partial Q)_{jj}}$$

*电气距离定义，基于快速分解法潮流雅可比矩阵的电压-无功灵敏度，αij表示节点i电压对节点j电压变化的灵敏度系数*

**公式6**: $$\Delta U_i = \alpha_{ij} \Delta U_j$$

*节点i与节点j之间的电压变化传递关系，假设仅节点j有无功功率变化*

### 算法步骤

1. 启动混合仿真，初始化故障前FDNE(FDNE1)并接入电磁-机电接口

2. 计算机电暂态侧所有母线到电磁暂态侧端口的电气距离矩阵D，基于当前运行点潮流雅可比矩阵

3. 启动伴随仿真（热备用）：并行计算故障后FDNE(FDNE2)，使用FDNE1端口电压u1初始化FDNE2状态变量x，但保持FDNE2离线（开关接B1点，仅跟踪仿真不参与网络计算）

4. 监测机电暂态侧网络状态，当检测到故障发生时，计算故障点对应的电气距离Dfault

5. 判断逻辑：若Dfault < Dcr（临界电气距离，通过离线仿真统计确定），执行步骤6；否则执行步骤7

6. 高影响故障处理：将接口开关从A1/B1切换至A2/B2，FDNE2接入网络参与混合仿真，同时FDNE1转为跟踪仿真（为后续跳闸/重合闸阶段做准备），使用伴随仿真技术保持状态变量连续性

7. 低影响故障处理：维持FDNE1在线，不切换FDNE，继续采用故障前FDNE进行仿真，避免重新计算FDNE的开销

8. 对于跳闸、重合闸等后续阶段，重复步骤3-7的伴随仿真与切换逻辑，确保各阶段FDNE状态平滑过渡

### 关键参数

- **FDNE_computation_ratio**: 14%

- **FDNE_computation_description**: 两端口FDNE的计算量约占混合仿真总计算量的14%

- **critical_electrical_distance_Dcr**: 通过离线仿真统计确定的临界值，当实际电气距离D<Dcr时需切换FDNE

- **state_variable_dimension**: N×1，N为FDNE端口数

- **compensation_method**: 通过添加-y（移除故障元件正常贡献）和Yf（添加故障贡献）修改诺顿等值网络

## 仿真结果

### 仿真测试

| 测试场景 | 结果描述 | 对比基线 |

|---------|---------|----------|

| 修改后的新英格兰IEEE 39节点系统机电侧故障 | 在IEEE 39节点系统中设置不同位置的短路故障，测试伴随仿真初始化方法。结果表明采用伴随仿真（热备用）技术可将FDNE切换过程中的虚假暂态（spurious transient）降至最低，相比零启动方式，状态变量跳变显著减小 | 相比简单切换（直接替换FDNE），伴随仿真方法消除了由于状态变量x未知导致的数值振荡 |

| 临界电气距离指标验证 | 通过扫描机电侧所有母线故障，建立电气距离D与频率响应误差ΔEFR、暂态误差ΔEtrans的统计关系。发现随着D增大，ΔEFR和ΔEtrans呈衰减趋势，存在明确的临界距离Dcr使得当D>Dcr时误差可忽略 | 采用基于Dcr的切换策略可避免约60-80%的不必要FDNE重算（取决于故障分布），仅对D<Dcr的故障（通常占少数）执行FDNE更新 |

| IEEE 118节点大规模系统效率测试 | 在118节点系统中验证基于临界电气距离的FDNE切换策略。对于远离接口的故障（D>Dcr），保持FDNE不变，计算时间节省显著；对于近端故障（D<Dcr），采用伴随仿真切换，精度与全模型EMT相当 | 在保证仿真精度（暂态误差ΔEtrans<允许阈值）的前提下，总体计算效率提升，避免了每次故障都重新进行矢量拟合（Vector Fitting）和无源校正的高开销过程 |

## 量化发现

- FDNE计算开销占混合仿真总计算量的约14%，伴随仿真方法虽然需同时运行两个FDNE，但仅增加14%计算量，属于可接受范围
- 电气距离D与频率响应变化量ΔEFR、暂态误差ΔEtrans存在明确的负相关关系，可用对数灵敏度指标Dij = -lg(αij)有效量化故障影响范围
- 对于两端口FDNE系统，采用伴随仿真技术可将FDNE切换时刻的暂态波动（spurious transient）抑制到与全模型EMT仿真相当的水平
- 基于临界电气距离Dcr的切换策略可将FDNE更新次数减少60%以上（取决于网络拓扑和故障位置分布），显著提升大规模网络仿真效率
- 当电气距离D > Dcr（临界值，典型值取决于系统参数）时，采用故障前FDNE引入的暂态误差ΔEtrans小于工程允许误差限，无需切换FDNE

## 关键公式

### 临界电气距离指标

$$D_{ij} = -\lg \left( \frac{(\partial U/\partial Q)_{ij}}{(\partial U/\partial Q)_{jj}} \right)$$

*用于快速判断机电侧故障对电磁侧影响程度，决定是否切换FDNE。基于潮流雅可比矩阵的电压-无功灵敏度计算，避免每次故障都进行完整的FDNE重算*

### FDNE状态空间模型

$$\begin{cases} \dot{x} = Ax + BU \\ I = Cx + DU \end{cases}$$

*描述含储能元件（LC）的FDNE动态行为，是伴随仿真初始化方法的基础。状态变量x的连续性通过热备用技术保证*

### 频率响应误差指标

$$\Delta E_{FR} = \frac{1}{n} \sum_{i=1}^{n} |Y_{ri}(i\Delta\omega) - Y_{si}(i\Delta\omega)|^2$$

*量化故障前后FDNE频率特性差异，用于离线确定临界电气距离Dcr与误差容忍度的关系*

## 验证详情

- **验证方式**: 仿真对比验证：将基于FDNE的混合仿真结果与全模型EMT仿真（作为基准）对比，验证伴随仿真初始化方法和临界电气距离指标的有效性
- **测试系统**: 修改后的新英格兰IEEE 39节点系统（含HVDC接口）和IEEE 118节点系统，机电侧设置三相短路接地故障
- **仿真工具**: 电磁暂态仿真环境（未明确指定具体软件，通常为PSCAD/EMTDC或类似平台），矢量拟合（Vector Fitting）算法用于FDNE有理函数拟合，快速分解法用于潮流计算和电气距离计算
- **验证结果**: 伴随仿真技术有效消除了FDNE切换时的状态变量跳变；临界电气距离指标能准确鉴别需更新FDNE的故障（D<Dcr），避免了对远端故障（D>Dcr）的不必要计算，在保证仿真精度（与全模型EMT误差在可接受范围内）的同时显著提升大规模系统仿真效率

## 适用边界

### 适用条件

- 适用于理解本文 `电磁–机电暂态混合仿真中机电侧故障的仿真方法`（2012） 在当前页面抽取范围内讨论的 EMT/电力系统暂态问题。
- 适用于以 co-simulation、vector-fitting、network-equivalent 为核心的建模、仿真、等值、控制或稳定性分析场景；具体对象以原文算例和页面“涉及的模型”为准。
- 可作为知识图谱中的方法定位和文献入口，尤其用于追踪：提出伴随仿真（热备用）方法初始化故障后FDNE状态，实现平稳切换并抑制虚假暂态

### 失效边界

- 不应外推到原文未覆盖的拓扑、控制策略、故障类型、频率范围、硬件平台或实时步长。
- 不应把页面中的“提高、显著、快速、准确”等概括性表述当作定量结论；只有“量化发现”和原文表图可核验的数字才可用于比较。
- 若页面作者、期刊、摘要或验证字段仍不完整，本页只能作为待复核文献入口，不能作为最终证据页引用。

### 关键假设

- 页面内容假设当前 PDF 抽取文本与 frontmatter 的 `sources` 指向同一篇论文。
- 方法结论默认受原文仿真工具、测试系统、参数设置、采样步长和对比基线约束。
- 当前边界层为保守整理：未从原文直接核验的内容不得升级为确定结论。

### 证据缺口

- 具体适用范围仍以原文算例、参数表和验证场景为准，当前页面不应外推到未验证系统。
- 源文件路径：`["EMT_Doc/35/张怡 等 - 2012 - 电磁–机电暂态混合仿真中机电侧故障的仿真方法.pdf"]`；需要深修时应优先核对该 PDF 的首页、摘要、方法和实验表图。
