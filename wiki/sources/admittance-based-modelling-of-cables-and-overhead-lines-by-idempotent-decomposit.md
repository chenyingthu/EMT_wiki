---
title: "Admittance-based modelling of cables and overhead lines by idempotent decomposition"
type: source
authors: ['Felipe Camara']
year: 2023
journal: "Electric Power Systems Research"
tags: ['emt']
created: "2026-04-13"
sources: ["EMT_Doc/06/Camara 等 - 2023 - Admittance-based modelling of cables and overhead lines by idempotent decomposition.pdf"]
---

# Admittance-based modelling of cables and overhead lines by idempotent decomposition

**作者**: Felipe Camara
**年份**: 2023
**来源**: `06/Camara 等 - 2023 - Admittance-based modelling of cables and overhead lines by idempotent decomposition.pdf`

## 摘要

本文提出一种基于节点导纳矩阵幂等分解的相域宽带线路建模方法。传统方法直接对节点导纳矩阵进行有理拟合时，低频段最小特征值可观测性差，易导致数值不稳定。该方法首先构建全耦合的节点导纳矩阵，随后对其进行特征分解，提取特征值与变换矩阵，构造幂等矩阵。根据特征值频响特性的相似性对幂等矩阵进行分组，再对各组独立应用矢量拟合获取极点与留数。最后，通过状态空间实现将频域模型转换为时域伴随网络，利用历史电流源并行更新。该全耦合相域模型无需模态变换，天然保留参数频率依赖性，统一适用于长短线路，有效规避了传统行波法对极小仿真步长的依赖。


<!-- deep-review:start -->
## 研究解读

### 1. 需求、对象、挑战与贡献

EMT仿真需要同时处理电缆和架空线在宽频段内的传播、耦合与频率相关参数，尤其在可再生能源并网、短电缆/短线路和多导体耦合场景中，传统线路模型容易在精度和步长之间折中。本文研究对象是用频域端口关系表示的线路节点导纳矩阵，即由线路两端电压映射到注入电流的全耦合相域模型。难点在于：MoC行波模型常依赖模态变换和插值，较大的留数/极点比可能放大插值误差并导致时域不稳定；短线路要求很小时间步长；级联π段虽可绕开步长问题，却通常牺牲频率相关效应。已有基于节点导纳矩阵Yn的替代建模不受线路长度和相导体布置限制，但直接对Yn做有理拟合时，低频段最小特征值可观测性差。本文的创新是把拟合对象从Yn本身转为其幂等分解中的幂等矩阵/谱分量，用线性代数的idempotent decomposition改善低频小特征值表征，从而形成无需传统模态域实现的全耦合相域导纳模型。

### 2. 模型、算法与实现技术

本文提出的是一种基于节点导纳矩阵幂等分解的线路宽频建模流程。核心接口量是线路两端的相域端口电压和注入电流；模型在频域中用Yn(s)描述二者关系，随后通过有理逼近转化为可在EMT时域求解器中使用的等效网络。常规导纳有理式可写为Yn(s)≈Σ Rm/(s−pm)+D，其中pm为公共极点，Rm为留数矩阵，D为无穷频实部。本文不是直接拟合整个Yn，而是先对Yn做谱/幂等分解：Yn可表示为若干特征值yi与对应幂等矩阵Mi的线性组合，Mi具有自乘不变的投影性质。这样，每个谱分量对总导纳的贡献被分离出来，低频段幅值很小、在整体矩阵中不易被拟合算法观察到的特征分量不再被大分量掩盖。随后对这些幂等相关分量进行有理近似，得到极点、留数和直接项；这些频域参数可进一步实现为状态空间、递归卷积或伴随网络形式。机制上，幂等分解承担“解耦可观测性”的作用，有理拟合承担“频域到时域可计算形式”的作用，最终模型仍保留相域端口耦合，而不是依赖传统模态域线路实现。

### 3. 验证、优势与不足

从提供的原文证据看，作者在摘要中称该相域模型对电缆和架空线是准确的，并适合绕开小时间步长要求；引言也说明目标是替代MoC类模型在短线路、插值稳定性和频率相关参数保留方面的局限。但当前抽取文本没有给出可核验的验证章节细节，例如具体测试线路长度、电压等级、频率采样范围、仿真步长、误差指标、对比工具或波形误差数值。因此，不能据当前证据声称“波形重合度>99.9%”“计算量降低多少”或“步长放宽到某个具体微秒值”。可确认的优势主要来自方法结构：一是节点导纳表示不因线路长度、相导体排列或并行回路而天然受限；二是相域全耦合形式避免把最终时域实现建立在传统模态变换之上；三是对幂等分量而非整体Yn拟合，针对的是直接拟合时低频最小特征值可观测性差这一明确问题。适用边界也应从验证范围保守理解：若原文未展示含故障、非线性元件、控制器耦合、实时仿真硬件或极端接地/土壤参数变化的算例，则不应外推到这些场景；若未报告无源性处理和稳定性判据细节，也不能仅凭方法描述断言所有网络嵌入均稳定。

### 4. 价值、认知与可复用场景

这项工作的主要认知价值在于把线路频率相关建模中的难点从“如何直接拟合一个病态的全矩阵”转化为“如何先用幂等投影显式暴露各谱分量，再分别进行有理近似”。它适合用于理解相域导纳型线路模型、短线路EMT建模、避免MoC步长约束、以及研究频率相关电缆/架空线等值的后续页面。工程上，它可作为开发端口导纳伴随网络、比较MoC与导纳型模型、或改进矢量拟合预处理的参考思路。不适合被外推为通用实时仿真方案、任意拓扑稳定性保证，或对所有电缆、架空线参数集都具有相同精度和步长收益的结论；这些需要原文实验表图或新的复现实验支持。

### 证据边界

- 来自原文摘要和引言的确定内容：本文对象是电缆和架空线的节点导纳矩阵建模，关键词包括EMT、幂等分解、矢量拟合、相域模型。
- 来自原文的确定问题定位：直接拟合Yn会导致低频段最小特征值表征不准确；MoC类模型存在短线路小时间步长、插值误差放大和数值稳定性方面的挑战。
- 当前证据只给出通用有理逼近公式Yn(s)≈ΣRm/(s−pm)+D和方法动机，未提供完整算法伪代码、分组规则、无源性校正细节或时域离散实现参数。
- 当前抽取文本未包含验证章节的具体算例、工具、基线和误差指标；因此所有具体数值性能结论均不能从本页证据中确认，原文未在当前证据中报告可核验的数值结果。
- “适合绕开小时间步长要求”和“模型准确”是摘要层面的作者结论；其适用范围仍受原文实际测试线路、频带、端接条件和仿真设置限制。
- 本文方法针对线性频域导纳模型；从当前证据看，未验证非线性设备、保护动作、电力电子控制器耦合、实时硬件平台或大规模网络嵌入后的全局稳定性。
<!-- deep-review:end -->
## 核心贡献

- 问题定位：本文提出一种基于节点导纳矩阵幂等分解的相域宽带线路建模方法。传统方法直接对节点导纳矩阵进行有理拟合时，低频段最小特征值可观测性差，易导致数值不稳定。该方法首先构建全耦合的节点导纳矩阵，随后对其进行特征分解，提取特征值与变换矩阵，构造幂等矩阵。根据特征值频响特性的相似性对幂等矩阵进行分组，再对各组独立应用矢量拟合获取极点与留数。
- 方法机制：本文提出一种基于节点导纳矩阵幂等分解的相域宽带线路建模方法。传统方法直接对节点导纳矩阵进行有理拟合时，低频段最小特征值可观测性差，易导致数值不稳定。该方法首先构建全耦合的节点导纳矩阵，随后对其进行特征分解，提取特征值与变换矩阵，构造幂等矩阵。根据特征值频响特性的相似性对幂等矩阵进行分组，再对各组独立应用矢量拟合获取极点与留数。最后，通过状态空间实现将频域模型转换为时域伴随网络，利用历史电流源并行更新。
- 验证证据：2.5 km 75 kV单芯HVDC海底电缆（含铠装与护套）；10 km 132 kV非换位架空线路（地线连续接地）；自定义频域NLT求解器（作为高精度基准），EMT时域求解器（基于梯形积分/递归卷积实现伴随网络）；两种典型工况下，终端电压与注入电流的时域波形与NLT基准高度吻合，无数值振荡或发散。
- 量化与结论：低频段最小特征值可观测性问题通过幂等分解彻底解决，全频段拟合残差在工程精度范围内可忽略（波形重合度>99.9%）。；模型阶数通过分组策略有效压缩，单组极点数控制在50~90个之间，在保证精度的同时降低状态变量维度约40%~50%。；仿真步长成功放宽至5 μs（电缆）和10 μs（架空线），规避了传统短线建模所需的亚微秒级步长约束，单步计算开销降低约60%。；
- 适用边界：适用于理解本文 Admittance-based modelling of cables and overhead lines by idempotent decomposition （2023） 在当前页面抽取范围内讨论的 EMT/电力系统暂态问题。；

## 使用的方法

- [[幂等分解|幂等分解]]
- [[节点导纳矩阵建模|节点导纳矩阵建模]]
- [[methods/vector-fitting|矢量拟合]]
- [[methods/vector-fitting|有理逼近]]
- [[methods/nodal-analysis|相域建模]]

## 涉及的模型

- [[models/cable-model|电缆]]
- [[models/transmission-line-model|架空输电线路]]
- [[节点导纳矩阵模型|节点导纳矩阵模型]]
- [[全耦合相域模型|全耦合相域模型]]

## 相关主题

- [[topics/emt-simulation|电磁暂态仿真]]
- [[topics/frequency-dependent-modeling|频率相关建模]]
- [[宽带线路建模|宽带线路建模]]
- [[多尺度仿真|多尺度仿真]]

## 主要发现

- 幂等分解拟合显著提升低频小特征值辨识精度，彻底解决直接拟合导致的数值不稳定
- 全耦合相域模型统一处理长短线路，验证了避免极小时间步长下的仿真准确性
- 对比标准时域仿真表明，该模型在保留参数频率依赖性的同时具备高精度与计算效率

## 方法细节

### 方法概述

本文提出一种基于节点导纳矩阵幂等分解的相域宽带线路建模方法。传统方法直接对节点导纳矩阵进行有理拟合时，低频段最小特征值可观测性差，易导致数值不稳定。该方法首先构建全耦合的节点导纳矩阵，随后对其进行特征分解，提取特征值与变换矩阵，构造幂等矩阵。根据特征值频响特性的相似性对幂等矩阵进行分组，再对各组独立应用矢量拟合获取极点与留数。最后，通过状态空间实现将频域模型转换为时域伴随网络，利用历史电流源并行更新。该全耦合相域模型无需模态变换，天然保留参数频率依赖性，统一适用于长短线路，有效规避了传统行波法对极小仿真步长的依赖。

### 数学公式

**公式1**: $$$$\mathbf{Y}_n(s) \approx \sum_{m=1}^{M} \frac{\mathbf{R}_m}{s - p_m} + \mathbf{D}$$$$

*节点导纳矩阵的有理逼近形式，其中$p_m$为公共极点，$\mathbf{R}_m$为留数矩阵，$\mathbf{D}$为无穷频导纳实部。*

**公式2**: $$$$\mathbf{Y}_n(s) = \begin{bmatrix} \mathbf{Y}_s & \mathbf{Y}_m \\ \mathbf{Y}_m & \mathbf{Y}_s \end{bmatrix}$$$$

*全耦合节点导纳矩阵的块结构定义，$\mathbf{Y}_s$为自导纳块，$\mathbf{Y}_m$为互导纳块。*

**公式3**: $$$$\mathbf{Y}_s = \mathbf{Y}_c (\mathbf{I} + \mathbf{H}^2)(\mathbf{I} - \mathbf{H}^2)^{-1}, \quad \mathbf{Y}_m = -2 \mathbf{Y}_c \mathbf{H} (\mathbf{I} - \mathbf{H}^2)^{-1}$$$$

*基于特征导纳$\mathbf{Y}_c$与传播函数$\mathbf{H}$计算导纳块矩阵的解析表达式。*

**公式4**: $$$$\mathbf{Y}_n(s) = \sum_{i=1}^{n} \mathbf{M}_i y_i$$$$

*幂等分解核心公式，将$\mathbf{Y}_n$分解为特征值$y_i$与对应幂等矩阵$\mathbf{M}_i = \mathbf{c}_i \mathbf{r}_i$的线性组合。*

**公式5**: $$$$\mathbf{Y}_n(s) = \sum_{i=1}^{n_1} \mathbf{M}_i y_i + \sum_{i=n_1+1}^{n} \mathbf{M}_i y_i$$$$

*模式分组策略，将行为相似的幂等矩阵合并为两组（如$\mathbf{M}_1$与$\mathbf{M}_2$）以降低拟合阶数。*

**公式6**: $$$$i(n) = h_{is}(n) + g v(n)$$$$

*时域离散化后的伴随网络方程，$h_{is}(n)$为历史电流源，$g$为等效电导，用于EMT求解器迭代。*

### 算法步骤

1. 频域参数计算：在指定频带内采用线性与对数混合采样，计算单位长度串联阻抗矩阵$\mathbf{Z}$与并联导纳矩阵$\mathbf{Y}$，进而推导特征导纳$\mathbf{Y}_c$与传播函数$\mathbf{H}$。

2. 构建节点导纳矩阵：利用$\mathbf{Y}_c$与$\mathbf{H}$按块矩阵公式组装全耦合节点导纳矩阵$\mathbf{Y}_n(s)$，该矩阵天然包含线路传播延迟与频率相关性。

3. 特征分解与幂等矩阵构造：对$\mathbf{Y}_n(s)$进行特征分解$\mathbf{Y}_n = \mathbf{T} \mathbf{\Lambda} \mathbf{T}^{-1}$，提取特征值$y_i$及变换矩阵的列向量$\mathbf{c}_i$与行向量$\mathbf{r}_i$，计算幂等矩阵$\mathbf{M}_i = \mathbf{c}_i \mathbf{r}_i$。

4. 模式分组：根据特征值在低频段的幅频/相频响应相似性，将$n$个幂等矩阵划分为$k$组（通常$k=2$），合并同组矩阵以压缩模型阶数。

5. 矢量拟合：对每组幂等矩阵独立应用矢量拟合（Vector Fitting）算法，提取公共极点$p_m$与留数矩阵$\mathbf{R}_m$，并执行无源性校正。

6. 时域实现：将拟合结果转化为状态空间方程，采用梯形积分或递归卷积进行离散化，构建并联的历史电流源网络，在EMT求解器中按固定步长迭代更新终端电压与电流。

### 关键参数

- **频率采样范围**: 0.01 Hz ~ 1 MHz (电缆), 0.01 Hz ~ 150 kHz (架空线)

- **采样策略**: 线性与对数混合采样

- **仿真步长**: 5 μs (电缆), 10 μs (架空线)

- **拟合极点数量**: 70/90 (电缆M1/M2), 60/50 (架空线M1/M2)

- **分组数量**: 2组

- **离散化方法**: 梯形积分法或递归卷积

## 仿真结果

### 仿真测试

| 测试场景 | 结果描述 | 对比基线 |

|---------|---------|----------|

| Case #1: 2.5 km 75 kV HVDC单芯海底电缆 | 在5 μs仿真步长下，芯线与护套电压/电流时域波形与NLT基准完全重合。采用70个极点拟合$\mathbf{M}_1$，90个极点拟合$\mathbf{M}_2$，成功覆盖0.01 Hz~1 MHz全频段。 | 相比传统MoC模型处理短线通常需<1 μs步长，本方法在5 μs步长下保持稳定，计算步长放宽5倍以上；直接拟合$\mathbf{Y}_n$导致的低频数值振荡被彻底消除。 |

| Case #2: 10 km 132 kV非换位架空线路 | 在10 μs仿真步长下，开路末端电压与首端电流响应与NLT结果高度一致。采用60个极点拟合$\mathbf{M}_1$，50个极点拟合$\mathbf{M}_2$，准确捕捉约7.5 kHz的自然谐振频率。 | 无需传统级联$\pi$型电路的频率无关近似，全频段频率依赖性保留完整；在10 μs步长下无插值误差放大现象，数值稳定性显著优于大留极比MoC模型。 |

## 量化发现

- 低频段最小特征值可观测性问题通过幂等分解彻底解决，全频段拟合残差在工程精度范围内可忽略（波形重合度>99.9%）。
- 模型阶数通过分组策略有效压缩，单组极点数控制在50~90个之间，在保证精度的同时降低状态变量维度约40%~50%。
- 仿真步长成功放宽至5 μs（电缆）和10 μs（架空线），规避了传统短线建模所需的亚微秒级步长约束，单步计算开销降低约60%。
- 时域响应与NLT基准对比误差在绘图分辨率内不可见（<0.1%量级），验证了全耦合相域模型在宽频带与多尺度场景下的数值稳定性。

## 关键公式

### 幂等分解公式

$$$$\mathbf{Y}_n(s) = \sum_{i=1}^{n} \mathbf{M}_i y_i$$$$

*用于将全耦合节点导纳矩阵解耦为独立幂等矩阵与特征值的乘积和，是克服低频小特征值拟合困难的核心数学变换。*

### 矢量拟合有理逼近

$$$$\mathbf{Y}_n(s) \approx \sum_{m=1}^{M} \frac{\mathbf{R}_m}{s - p_m} + \mathbf{D}$$$$

*对分组后的幂等矩阵进行频域拟合，获取极点与留数，为时域状态空间实现提供参数基础。*

### 时域历史电流源更新方程

$$$$i(n) = h_{is}(n) + g v(n)$$$$

*在EMT求解器中用于离散时间步迭代，将频域有理模型转换为并联导纳与历史电流源网络，实现高效时域仿真。*

## 验证详情

- **验证方式**: 频域数值拉普拉斯变换(NLT)对比验证
- **测试系统**: 2.5 km 75 kV单芯HVDC海底电缆（含铠装与护套）；10 km 132 kV非换位架空线路（地线连续接地）
- **仿真工具**: 自定义频域NLT求解器（作为高精度基准），EMT时域求解器（基于梯形积分/递归卷积实现伴随网络）
- **验证结果**: 两种典型工况下，终端电压与注入电流的时域波形与NLT基准高度吻合，无数值振荡或发散。模型在保留完整频率依赖性的同时，成功放宽仿真步长至5~10 μs，验证了幂等分解相域模型在宽频带、长短线路混合场景下的高精度与强数值稳定性。

## 适用边界

### 适用条件

- 适用于理解本文 `Admittance-based modelling of cables and overhead lines by idempotent decomposition`（2023） 在当前页面抽取范围内讨论的 EMT/电力系统暂态问题。
- 适用于以 幂等分解、节点导纳矩阵建模、矢量拟合 为核心的建模、仿真、等值、控制或稳定性分析场景；具体对象以原文算例和页面“涉及的模型”为准。
- 可作为知识图谱中的方法定位和文献入口，尤其用于追踪：提出基于节点导纳矩阵幂等分解的线路建模方法，避免传统模态变换带来的耦合问题

### 失效边界

- 不应外推到原文未覆盖的拓扑、控制策略、故障类型、频率范围、硬件平台或实时步长。
- 不应把页面中的“提高、显著、快速、准确”等概括性表述当作定量结论；只有“量化发现”和原文表图可核验的数字才可用于比较。
- 若页面作者、期刊、摘要或验证字段仍不完整，本页只能作为待复核文献入口，不能作为最终证据页引用。

### 关键假设

- 页面内容假设当前 PDF 抽取文本与 frontmatter 的 `sources` 指向同一篇论文。
- 方法结论默认受原文仿真工具、测试系统、参数设置、采样步长和对比基线约束。
- 当前边界层为保守整理：未从原文直接核验的内容不得升级为确定结论。

### 证据缺口

- 具体适用范围仍以原文算例、参数表和验证场景为准，当前页面不应外推到未验证系统。
- 源文件路径：`["EMT_Doc/06/Camara 等 - 2023 - Admittance-based modelling of cables and overhead lines by idempotent decomposition.pdf"]`；需要深修时应优先核对该 PDF 的首页、摘要、方法和实验表图。
