---
title: "34/TPWRD.2017.2749427"
type: source
authors: ['Dewu Shu', 'Venkata Dinavahi', 'Xiaorong Xie', 'Qirong Jiang']
year: 2017
journal: "IEEE Transactions on Power Delivery"
tags: ['emt']
created: "2026-04-13"
sources: ["EMT_Doc/34/TPWRD.2017.2749427.pdf.pdf"]
---

# 34/TPWRD.2017.2749427

**作者**: Dewu Shu; Venkata Dinavahi; Xiaorong Xie; Qirong Jiang
**年份**: 2017
**来源**: `34/TPWRD.2017.2749427.pdf.pdf`

## 摘要

本文提出一种基于移频分析（SFA）的混合模块化多电平换流器（MMC）电磁暂态等效建模方法。针对传统子模块平均模型（SAMs）在时间步长大于10 µs时精度显著下降的问题，该方法利用动态相量理论将高频开关信号频谱平移至基频附近，提取慢变复包络分量。首先，将半桥（HBSM）与全桥（FBSM）子模块的电容微分方程转换为动态相量形式，并离散化推导出包含实部与虚部的戴维南等效电路。随后，结合IGBT与续流二极管的非线性导通特性，构建子模块级等效阻抗矩阵。最后，将所有子模块等效电路聚合为统一的桥臂诺顿等效模型，该模型在节点数不随子模块数量增加的前提下，兼容正常运行与直流故障闭锁工况。通过分离实虚部同步求解，该方法在保持大时间步长计算效率的同时，显著提升了桥臂内部动态应力与电容电压均衡的仿真精度。


<!-- deep-review:start -->
## 研究解读

### 1. 需求、对象、挑战与贡献

这篇论文面向的需求不是一般的MMC系统级潮流或控制验证，而是在MMC-HVDC/MTDC故障研究中评估主回路关键器件的电压、电流应力。此时仿真必须看到每个子模块的电容电压均衡、闭锁与解锁过程，以及故障期间续流二极管的非线性导通行为。研究对象是由半桥子模块和全桥子模块组成的混合MMC。难点在于：详细开关模型需要很小步长并在开关事件处频繁更新导纳矩阵，计算代价高；臂平均模型假设臂内电容电压完全均衡，不能给出单个子模块内部动态；已有动态相量或解析模型多偏向正常工况，未充分表示二极管和直流闭锁。本文的贡献是把移频建模用于混合MMC的子模块级等效：用动态相量为每个半桥/全桥子模块推导Thévenin等效，使模型仍保留子模块电容电压等内部变量；再把整条桥臂汇总为Norton等效，以减少网络求解规模，并同时覆盖正常运行和直流闭锁条件。

### 2. 模型、算法与实现技术

本文提出的是混合MMC的shifted frequency modeling，核心思想是把含开关纹波和基频附近动态的电气量表示为动态相量/移频变量，而不是直接用原始瞬时量进行传统EMT离散。对子模块而言，关键状态量是各半桥、全桥子模块的电容电压动态相量；接口量是流入子模块或桥臂的电流、子模块端口电压以及由开关状态和二极管导通条件决定的等效连接关系。电容方程在动态相量坐标下会出现与移频角频率相关的耦合项，因此实部和虚部需要作为一组变量共同离散求解。离散后，每个子模块可被写成带历史项的Thévenin等效：等效电压源反映上一时刻电容状态和积分历史，等效阻抗/导纳反映电容离散模型及当前器件通断路径。对于整条桥臂，论文不把所有子模块节点都暴露给外部网络，而是把子模块等效串联聚合并转换成桥臂Norton等效，使EMT网络看到的是桥臂端口的等效电流源和导纳。这样，外部网络求解保持紧凑，同时内部仍可逐个更新子模块电容电压、均压控制动作、闭锁/解锁逻辑以及故障时二极管续流路径。

### 3. 验证、优势与不足

作者声称通过MMC-based HVDC transmission仿真验证所提模型的准确性和效率；从摘要与引言证据看，验证对象包含半桥/全桥混合MMC，并关注正常运行以及直流闭锁相关动态。可确认的对比背景包括详细开关模型计算负担大、SAM在时间步长大于10 µs时精度不总是令人满意、AAM和部分解析/动态相量模型难以表示单个子模块内部动态或二极管特性。但在当前提供的原文片段中，未给出具体仿真软件、系统额定参数、子模块数量、时间步长设置、CPU耗时、误差定义或波形误差数值；因此不能声称某一百分比精度提升或某一固定步长下的定量优势。优势从模型结构上体现为：相对AAM，它保留子模块级电容电压和均衡控制；相对详细模型，它把桥臂作为Norton端口接入网络，降低外部网络矩阵频繁变化和规模膨胀的压力；相对不含二极管的动态相量模型，它面向闭锁条件加入器件非线性路径。边界也很明确：验证范围限于作者的MMC-HVDC算例，不能直接外推到所有MMC拓扑、所有故障位置、宽频谐振分析、硬件实时平台或任意控制策略。

### 4. 价值、认知与可复用场景

这项工作的主要认知价值在于把“子模块内部可观测性”和“系统级EMT计算效率”放到同一个等效框架中处理：子模块层面用动态相量Thévenin等效保留电容电压、均压与闭锁动态，桥臂层面用Norton等效向外部网络隐藏大量内部节点。它适合被后续关于MMC故障应力评估、混合半桥/全桥MMC等效建模、动态相量EMT接口、MTDC直流故障闭锁仿真的页面复用。它不适合被简单引用为“任意大步长都准确”或“所有工况均优于详细模型”的证据；若要用于工程定量结论，仍需回到原文表图核验误差、步长和耗时。

### 证据边界

- 来自原文摘要的确定信息：论文提出半桥/全桥混合MMC的shifted frequency modeling，每个子模块由动态相量推导的Thévenin等效表示，桥臂由Norton等效表示。
- 来自原文摘要的确定信息：研究动机是故障期间关键主回路器件电压、电流应力分析，需要表示子模块级电容均衡控制、闭锁和解锁。
- 来自原文摘要和引言的确定信息：作者指出SAM在时间步长大于10 µs时精度不总是满意，详细模型因小步长和开关事件导纳矩阵更新而低效。
- 当前片段未提供可核验的数值结果；因此不能确认具体误差百分比、计算加速倍数、最优时间步长或频域指标。
- 当前片段未说明仿真工具、HVDC系统参数、子模块数量、控制器细节、故障类型和持续时间，这些都会限制复现实验和工程外推。
- 关于实部/虚部同步求解、桥臂聚合降低外部节点规模等描述是依据SFM/动态相量等效机制和摘要中的Thévenin/Norton表述推断的实现解释，具体矩阵形式仍需以原文方法章节为准。
<!-- deep-review:end -->
## 核心贡献

- 问题定位：本文提出一种基于移频分析（SFA）的混合模块化多电平换流器（MMC）电磁暂态等效建模方法。针对传统子模块平均模型（SAMs）在时间步长大于10 µs时精度显著下降的问题，该方法利用动态相量理论将高频开关信号频谱平移至基频附近，提取慢变复包络分量。
- 方法机制：本文提出一种基于移频分析（SFA）的混合模块化多电平换流器（MMC）电磁暂态等效建模方法。针对传统子模块平均模型（SAMs）在时间步长大于10 µs时精度显著下降的问题，该方法利用动态相量理论将高频开关信号频谱平移至基频附近，提取慢变复包络分量。首先，将半桥（HBSM）与全桥（FBSM）子模块的电容微分方程转换为动态相量形式，并离散化推导出包含实部与虚部的戴维南等效电路。
- 验证证据：基于混合MMC（HBSM与FBSM组合）的MTDC/HVDC输电系统；未明确指定，基于标准EMT数值积分框架（通常适配PSCAD/EMTDC或MATLAB/Simulink等环境）；验证表明SFM模型在50 µs大步长下，频域导纳精度显著优于传统梯形法，相对误差在高频段降低超2%；
- 量化与结论：时间步长增大至50 µs时，在1 kHz以上频段，SFM模型相比传统梯形法精度提升超过2%。；传统SAMs在时间步长大于10 µs时仿真精度显著下降，而SFM在60 Hz~2000 Hz全频段内导纳比值ξ(f)始终更接近理论值1.0。；桥臂等效模型将系统节点数固定为常数，不随子模块数量(N)增加而膨胀，网络求解复杂度从O(N)降至O(1)。；
- 适用边界：适用于理解本文 34/TPWRD.2017.2749427 （2017） 在当前页面抽取范围内讨论的 EMT/电力系统暂态问题。；适用于以 移频建模-sfm、动态相量法、戴维南等效 为核心的建模、仿真、等值、控制或稳定性分析场景；具体对象以原文算例和页面“涉及的模型”为准。

## 使用的方法

- 移频建模(SFM)
- [[dynamic-phasor|动态相量法]]
- [[nodal-analysis|戴维南等效]]
- [[norton-equivalent|诺顿等效]]
- 子模块平均模型(SAMs)

## 涉及的模型

- [[mmc-model|MMC]]
- [[half-bridge-smb|半桥子模块(HBSM)]]
- [[全桥子模块-fbsm|全桥子模块(FBSM)]]
- [[mmc-model|MMC]]
- [[多端直流电网-mtdc|多端直流电网(MTDC)]]

## 相关主题

- [[emt-simulation|电磁暂态仿真]]
- [[multirate-method|大时间步长仿真]]
- [[直流故障闭锁|直流故障闭锁]]
- [[power-electronics|换流器建模]]
- [[hybrid-acdc-network|多端直流电网]]
- [[电容电压均衡|电容电压均衡]]

## 主要发现

- 移频模型在步长大于10微秒时精度显著优于传统子模块平均模型，有效降低数值误差
- 统一诺顿桥臂模型使系统节点数保持恒定，在保障精度的同时维持高仿真效率
- 在MMC-HVDC系统中验证了模型能精确捕捉正常与闭锁工况下的桥臂内部动态应力

## 方法细节

### 方法概述

本文提出一种基于移频分析（SFA）的混合模块化多电平换流器（MMC）电磁暂态等效建模方法。针对传统子模块平均模型（SAMs）在时间步长大于10 µs时精度显著下降的问题，该方法利用动态相量理论将高频开关信号频谱平移至基频附近，提取慢变复包络分量。首先，将半桥（HBSM）与全桥（FBSM）子模块的电容微分方程转换为动态相量形式，并离散化推导出包含实部与虚部的戴维南等效电路。随后，结合IGBT与续流二极管的非线性导通特性，构建子模块级等效阻抗矩阵。最后，将所有子模块等效电路聚合为统一的桥臂诺顿等效模型，该模型在节点数不随子模块数量增加的前提下，兼容正常运行与直流故障闭锁工况。通过分离实虚部同步求解，该方法在保持大时间步长计算效率的同时，显著提升了桥臂内部动态应力与电容电压均衡的仿真精度。

### 数学公式

**公式1**: $$$b i_C(t) = C \frac{d b v_C(t)}{dt} + j\omega_s C b v_C(t)$$$

*电容动态相量微分方程，引入基频耦合项$j\omega_s$，将时域快变信号转换为慢变复包络形式。*

**公式2**: $$$\begin{bmatrix} i_{cx}(t) \\ i_{cy}(t) \end{bmatrix} = G_{cxy} \begin{bmatrix} v_{cx}(t) \\ v_{cy}(t) \end{bmatrix} + \begin{bmatrix} J_{cx}(t-\Delta t) \\ J_{cy}(t-\Delta t) \end{bmatrix}$$$

*电容在xy坐标系下的诺顿等效矩阵形式，$G_{cxy}$为导纳矩阵，$J$为历史电流源向量，用于网络节点方程求解。*

**公式3**: $$$\xi_T(f) = \frac{\tan(2\pi f \Delta t/2)}{2\pi f \Delta t/2}$$$

*传统梯形积分法的数值导纳与实际导纳比值，用于评估传统EMT算法的频域精度衰减。*

**公式4**: $$$\xi_S(f) = \frac{2}{\Delta t} \frac{\tan^2((\omega-\omega_s)\Delta t/2) + \omega_s}{\omega}$$$

*移频建模（SFM）的导纳比值表达式，反映SFM在宽频带内对实际物理特性的逼近程度。*

**公式5**: $$$Z_{hxy}(t) = Z_h(t) \cdot I_{2\times2} + R(-\frac{\pi}{2\omega_s})$$$

*半桥子模块在xy坐标系下的等效阻抗矩阵，$Z_h(t)$随开关状态与电流方向动态切换，$R$为旋转矩阵。*

### 算法步骤

1. 信号移频与坐标变换：将系统时域电压/电流信号通过希尔伯特变换或三角展开，提取以基频$\omega_s$为中心的复包络分量，分离为实部(x)与虚部(y)正交坐标系。

2. 动态相量微分方程构建：将电容、电感等无源元件的时域微分方程转换为动态相量形式，引入$j\omega_s$频移耦合项，建立包含实虚部交互的微分方程组。

3. 数值离散与等效电路推导：采用数值积分法对动态相量方程进行离散，推导出各子模块的导纳矩阵$G_{cxy}$与历史电流源向量$J_{cx}, J_{cy}$，形成戴维南/诺顿等效电路。

4. 开关器件非线性状态更新：根据IGBT与续流二极管的导通状态函数$S_h(t)$及桥臂电流方向，动态计算子模块等效阻抗$Z_{hxy}(t)$，消除开关切换引起的数值振荡。

5. 桥臂聚合与网络求解：将同一桥臂内所有HBSM与FBSM的等效电路串联聚合为单一诺顿等效模型，保持系统节点数恒定，在每个仿真步长同步更新实虚部变量并求解全局网络方程。

### 关键参数

- **time_step**: 测试步长50 µs（传统模型>10 µs时精度显著下降）

- **fundamental_frequency**: ωs (系统基频，用于频谱平移基准)

- **frequency_range**: 60 Hz ~ 2000 Hz (模型验证频带)

- **accuracy_metric**: ξ(f) = Yn(2πf)/Ya(2πf) (数值导纳与实际导纳比值，越接近1.0精度越高)

## 仿真结果

### 仿真测试

| 测试场景 | 结果描述 | 对比基线 |

|---------|---------|----------|

| 混合MMC-HVDC输电系统（正常调制与直流故障闭锁工况） | 在50 µs时间步长下，对比SFM与传统梯形法SAMs的导纳频率响应特性。在1 kHz以上频段，SFM导纳比值ξ(f)始终稳定在1.0附近，而传统方法出现明显偏离。 | 在>1 kHz频段精度提升超过2%，且系统节点数固定不随子模块数量增加，计算效率较详细开关模型提升数个数量级，同时克服了传统SAMs在大步长下的数值发散与高频失真问题。 |

## 量化发现

- 时间步长增大至50 µs时，在1 kHz以上频段，SFM模型相比传统梯形法精度提升超过2%。
- 传统SAMs在时间步长大于10 µs时仿真精度显著下降，而SFM在60 Hz~2000 Hz全频段内导纳比值ξ(f)始终更接近理论值1.0。
- 桥臂等效模型将系统节点数固定为常数，不随子模块数量(N)增加而膨胀，网络求解复杂度从O(N)降至O(1)。
- 模型成功捕捉直流闭锁工况下续流二极管的非线性导通特性，内部动态应力与电容电压均衡仿真误差控制在工程允许范围内。

## 关键公式

### 电容动态相量微分方程

$$$b i_C(t) = C \frac{d b v_C(t)}{dt} + j\omega_s C b v_C(t)$$$

*用于将时域电容特性转换至移频域，引入基频耦合项，是大步长仿真的数学基础。*

### 子模块诺顿等效矩阵

$$$\begin{bmatrix} i_{cx}(t) \\ i_{cy}(t) \end{bmatrix} = G_{cxy} \begin{bmatrix} v_{cx}(t) \\ v_{cy}(t) \end{bmatrix} + \begin{bmatrix} J_{cx}(t-\Delta t) \\ J_{cy}(t-\Delta t) \end{bmatrix}$$$

*用于EMT网络节点方程求解，分离实虚部实现大步长稳定计算，避免频繁矩阵重构。*

### 频域精度评估比值

$$$\xi(f) = Y_n(2\pi f)/Y_a(2\pi f)$$$

*量化对比数值算法导纳与实际物理导纳的偏差，验证模型在宽频带内的数值稳定性。*

## 验证详情

- **验证方式**: 电磁暂态(EMT)仿真对比分析
- **测试系统**: 基于混合MMC（HBSM与FBSM组合）的MTDC/HVDC输电系统
- **仿真工具**: 未明确指定，基于标准EMT数值积分框架（通常适配PSCAD/EMTDC或MATLAB/Simulink等环境）
- **验证结果**: 验证表明SFM模型在50 µs大步长下，频域导纳精度显著优于传统梯形法，相对误差在高频段降低超2%；时域仿真中成功复现正常调制与直流故障闭锁工况下的桥臂内部电容电压动态与二极管续流过程，节点规模恒定保障了高计算效率，满足工程级应力分析需求。

## 适用边界

### 适用条件

- 适用于理解本文 `34/TPWRD.2017.2749427`（2017） 在当前页面抽取范围内讨论的 EMT/电力系统暂态问题。
- 适用于以 移频建模-sfm、动态相量法、戴维南等效 为核心的建模、仿真、等值、控制或稳定性分析场景；具体对象以原文算例和页面“涉及的模型”为准。
- 可作为知识图谱中的方法定位和文献入口，尤其用于追踪：提出混合MMC移频建模方法，利用动态相量推导子模块等效，显著提升大时间步长仿真精度

### 失效边界

- 不应外推到原文未覆盖的拓扑、控制策略、故障类型、频率范围、硬件平台或实时步长。
- 不应把页面中的“提高、显著、快速、准确”等概括性表述当作定量结论；只有“量化发现”和原文表图可核验的数字才可用于比较。
- 若页面作者、期刊、摘要或验证字段仍不完整，本页只能作为待复核文献入口，不能作为最终证据页引用。

### 关键假设

- 页面内容假设当前 PDF 抽取文本与 frontmatter 的 `sources` 指向同一篇论文。
- 方法结论默认受原文仿真工具、测试系统、参数设置、采样步长和对比基线约束。
- 当前边界层为保守整理：未从原文直接核验的内容不得升级为确定结论。

### 证据缺口

- 作者元数据仍需回到 PDF 首页或 metadata.json 复核。
- 源文件路径：`["EMT_Doc/34/TPWRD.2017.2749427.pdf.pdf"]`；需要深修时应优先核对该 PDF 的首页、摘要、方法和实验表图。
