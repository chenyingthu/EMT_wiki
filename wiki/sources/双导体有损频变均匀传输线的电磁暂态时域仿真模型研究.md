---
title: "双导体有损频变均匀传输线的电磁暂态时域仿真模型研究"
type: source
authors: ['刘刚 等']
year: 2023
journal: "电网技术"
tags: ['emt']
created: "2026-04-13"
sources: ["EMT_Doc/36/刘刚 等 - 2023 - 双导体有损频变均匀传输线的电磁暂态时域仿真模型研究.pdf"]
---

# 双导体有损频变均匀传输线的电磁暂态时域仿真模型研究

**作者**: 刘刚 等
**年份**: 2023
**来源**: `36/刘刚 等 - 2023 - 双导体有损频变均匀传输线的电磁暂态时域仿真模型研究.pdf`

## 摘要

特征阻抗和传输函数是多导体传输线电磁暂态时域仿真模型中的 2 个重要参数。该文以有损频变均匀双导体传输线为研究对象，采用相模变换实现传输线耦合量的解耦，在模态域利用 Pade 近似和 Foster 电路模型获得特征阻抗的时域等效电路。同时利用拉普拉斯反变换和 Prony 近似得到传输函数的近似函数。该文基于等效电路和近似函数通过 MATLAB 编程建立了双导体传输线的电磁暂态时域仿真模型。最后，用该时域仿真模型获得了阶跃激励下传输线首末端的电压响应，并与 PSCAD/EMTDC 软件仿真结果进行对比分析，验证了所提时域模型的有效性。


<!-- deep-review:start -->
## 研究解读

### 1. 需求、对象、挑战与贡献

工程需求来自电力系统短路、过电压等暂态过程：线路电压电流会在很短时间内快速变化，若传输线模型不能正确反映行波传播、反射、衰减和波形畸变，就会影响设备绝缘配合与暂态分析结论。本文研究对象限定为“双导体、有损、频变、均匀传输线”，且线路参数具有循环对称结构，导体间存在互感、互容耦合。难点在于：传统 Bergeron 模型便于时域计算，但通常不能充分考虑分布损耗和频率相关参数；已有频变线路模型常需处理加权函数、卷积积分或频域有理函数拟合，计算实现和拟合过程相对复杂。本文的贡献不是提出全新的传输线理论，而是在相模解耦后，把特征阻抗和传输函数这两个关键频域量分别转化为便于时域仿真的形式：用 Pade 近似和 Foster 电路实现特征阻抗的等效，用拉普拉斯反变换结合 Prony 近似表示传播传输函数，再嵌入 Bergeron 行波框架形成 MATLAB 时域仿真模型。

### 2. 模型、算法与实现技术

模型从双导体电报方程出发，接口量是线路两端导体电压、电流，输入包括单位长度 R、L、G、C 参数矩阵、线路长度、端部电源和负载条件以及暂态激励，输出为首端和末端电压响应。由于 L、C 中含互耦项，作者先采用相模变换把物理导体域中的耦合方程变为模态域中相互独立的单模线路问题；这样每个模态可分别计算特征阻抗 Z(s) 和传播相关的传输函数。对特征阻抗，Pade 近似把原本含平方根等频变特征的函数拟合成有理分式，再通过部分分式展开映射为 Foster 等效电路，即常数电阻与若干 RC 支路组合，使频变阻抗可以作为时域电路元件参与节点计算。对传输函数，作者通过拉普拉斯反变换得到时域响应，再用 Prony 近似表示为指数项叠加；这些指数项对应历史量的递推更新，用来描述行波沿线路传播后的延迟、衰减和畸变作用。最终在 Bergeron 等效电路框架下，特征阻抗部分由 Foster 电路承担，远端影响通过传输函数近似形成历史电流源或历史项，在 MATLAB 中逐步推进得到暂态波形。

### 3. 验证、优势与不足

作者采用对比仿真的方式验证模型：在双导体有损频变均匀传输线两端连接电源内阻和负载电阻，对线路施加阶跃电压激励；用本文 MATLAB 实现的时域模型计算首端、末端电压响应，并与 PSCAD/EMTDC 软件在相同工况下的仿真结果比较。验证对象主要是阶跃激励下电压波形是否能够复现行波传播及频变损耗引起的衰减、畸变。原文摘要明确说仿真结果与 PSCAD/EMTDC 对比用于验证有效性，但在当前给出的证据片段中未报告可核验的误差数值、计算耗时、步长敏感性或拟合阶数选择对结果的影响。其优势体现在机制上：相模变换降低双导体耦合求解难度；Pade-Foster 让频变特征阻抗进入时域电路；Prony 指数项使传输函数卷积影响可用递推历史量实现，避免直接处理复杂卷积。边界也很明确：验证只覆盖循环对称双导体均匀线路和阶跃激励对比，不能据此直接证明该方法适用于任意多导体非对称线路、非均匀线路、强非线性端接、电力电子开关网络、故障全过程或实时仿真。

### 4. 价值、认知与可复用场景

这项工作的主要价值在于把“频变线路模型为什么难以进入时域仿真”拆成两个可处理环节：特征阻抗等效为可接入电路求解的 Foster 网络，传播传输函数等效为 Prony 指数历史项。它适合被后续关于 EMT 线路建模、Bergeron 模型改进、频变参数近似、递推卷积实现、双导体或可模态解耦线路暂态仿真的页面复用，也可作为比较 Pade、Prony、矢量匹配等频域拟合方法时的一个简化实现案例。不适合被外推为通用多导体频变线路最优模型，也不能仅凭本文验证说明其在宽频带、复杂拓扑、非对称参数、实际故障录波或硬件实时平台中同样准确稳定。

### 证据边界

- 来自原文摘要和引言的确定信息：研究对象为有损频变均匀双导体传输线，关键词包括 Bergeron 模型、Pade 近似、Prony 近似和频变参数。
- 来自原文方法描述的确定信息：作者使用相模变换解耦，特征阻抗采用 Pade 近似与 Foster 电路模型，传输函数采用拉普拉斯反变换和 Prony 近似，并用 MATLAB 建立时域仿真模型。
- 来自原文验证描述的确定信息：算例为阶跃激励下线路首末端电压响应，并与 PSCAD/EMTDC 软件结果对比；当前证据未给出可核验的误差百分比、最大偏差、均方误差或运行时间。
- 关于 Foster 电路和 Prony 历史项如何便于时域递推的表述属于根据方法机制的解释性归纳；若需引用具体电路拓扑、阶数和参数，应回到原文公式、图和表核验。
- 当前证据未显示对不同线路长度、频率范围、拟合阶数、时间步长、端接类型、非对称参数或多导体扩展的系统敏感性测试。
- 本文与 PSCAD/EMTDC 的一致性只能说明在给定算例和建模条件下有效，不能替代与实测数据、其他频变线路模型或实时仿真平台的全面验证。
<!-- deep-review:end -->
## 核心贡献

- 问题定位：特征阻抗和传输函数是多导体传输线电磁暂态时域仿真模型中的 2 个重要参数。该文以有损频变均匀双导体传输线为研究对象，采用相模变换实现传输线耦合量的解耦，在模态域利用 Pade 近似和 Foster 电路模型获得特征阻抗的时域等效电路。同时利用拉普拉斯反变换和 Prony 近似得到传输函数的近似函数。
- 方法机制：本文提出了一种基于相模变换解耦的双导体有损频变传输线电磁暂态时域仿真方法。首先在复频域建立传输线电报方程，利用Clarke变换实现相模解耦；随后在模态域内，采用Pade近似将特征阻抗拟合为有理分式，并通过部分分式展开构建Foster等效电路（RC并联模块串联）；同时利用拉普拉斯反变换与Prony近似求解传输函数的指数级数形式；最终基于Bergeron行波理论建立时域等效电路，通过MATLAB编程实现仿真。
- 验证证据：对比仿真验证：将本文MATLAB实现的时域模型与商业电磁暂态仿真软件PSCAD/EMTDC进行同条件对比；双导体有损频变均匀传输线系统，含电源内阻Rdy、负载电阻RL，线路采用循环对称结构参数（自阻抗Rz、互电感Lh等），施加阶跃电压激励；MATLAB（本文算法实现）、PSCAD/EMTDC（基准验证）
- 量化与结论：Pade近似采用5阶有理分式时，特征阻抗拟合精度与标准值偏差达10^-8数量级；要达到与5阶Pade近似相同精度，传统泰勒级数展开需取11项，计算复杂度显著增加；相模变换矩阵T的列向量构成标准正交基，满足T^-1 = T^T，确保变换无损且可逆；Foster等效电路将特征阻抗分解为1个电阻与n个RC并联模块的串联，其中n为Pade近似阶数
- 适用边界：适用于理解本文 双导体有损频变均匀传输线的电磁暂态时域仿真模型研究 （2023） 在当前页面抽取范围内讨论的 EMT/电力系统暂态问题。；适用于以 prony、frequency-dependent 为核心的建模、仿真、等值、控制或稳定性分析场景；具体对象以原文算例和页面“涉及的模型”为准。

## 使用的方法

- [[prony]]
- [[frequency-dependent-modeling]]

## 涉及的模型

- [[transmission-line-model]]

## 相关主题

- [[transmission-line-model]]
- [[frequency-dependent-modeling]]

## 主要发现

- 所提时域模型能够准确复现阶跃激励下传输线首末端的电压动态响应
- 模型仿真结果与PSCAD/EMTDC商业软件高度吻合，验证了其在处理频变参数时的有效性与精度
- Pade近似结合Foster电路与Prony近似的方法可有效克服传统Bergeron模型忽略频变特性的局限

## 方法细节

### 方法概述

本文提出了一种基于相模变换解耦的双导体有损频变传输线电磁暂态时域仿真方法。首先在复频域建立传输线电报方程，利用Clarke变换实现相模解耦；随后在模态域内，采用Pade近似将特征阻抗拟合为有理分式，并通过部分分式展开构建Foster等效电路（RC并联模块串联）；同时利用拉普拉斯反变换与Prony近似求解传输函数的指数级数形式；最终基于Bergeron行波理论建立时域等效电路，通过MATLAB编程实现仿真。该方法克服了传统Bergeron模型忽略频变特性的局限，无需迭代计算，具有实现简单、精度高的特点。

### 数学公式

**公式1**: $$$-\frac{\partial u_{ab}}{\partial x} = R i_{ab} + L \frac{\partial i_{ab}}{\partial t}$$$

*时域电报方程（电压），描述传输线电压沿线路变化的微分关系，考虑电阻压降和电感感应电压*

**公式2**: $$$-\frac{\partial i_{ab}}{\partial x} = G u_{ab} + C \frac{\partial u_{ab}}{\partial t}$$$

*时域电报方程（电流），描述传输线电流沿线路变化的微分关系，考虑对地电导和电容电流*

**公式3**: $$$T = \frac{1}{2}\begin{bmatrix} 1 & 1 \\ 1 & -1 \end{bmatrix}, \quad T^{-1} = T^T = \frac{1}{2}\begin{bmatrix} 1 & 1 \\ 1 & -1 \end{bmatrix}$$$

*Clarke相模变换矩阵及其逆矩阵，用于将双导体耦合系统解耦为模态域（αβ坐标系）下的独立单导体系统*

**公式4**: $$$Z(s) = \sqrt{\frac{R + sL}{G + sC}}$$$

*特征阻抗频域表达式，为频变参数，是构建Bergeron模型的关键参数*

**公式5**: $$$Z_d(s) = \frac{y_1s^n + y_2s^{n-1} + \cdots + y_{n+1}}{x_1s^n + x_2s^{n-1} + \cdots + x_ns + 1} = k + \sum_{j=1}^{n} \frac{r_j}{s - p_j}$$$

*Pade近似有理分式及部分分式展开形式，用于拟合特征阻抗，其中k为电阻，rj为留数，pj为极点*

**公式6**: $$$Z_{R_jC_j}(s) = \frac{R_j}{1 + sR_jC_j}$$$

*Foster等效电路中第j个RC并联模块的阻抗表达式，对应部分分式中的每一项*

**公式7**: $$$g(t) = \sum_{j=1}^{n} r_j e^{p_j t}$$$

*Prony近似表达式，用于拟合传输函数的时域冲击响应，将频域传输函数转换为时域指数级数形式*

**公式8**: $$$I_{ks} = \left(\frac{U(m,s)}{Z(s)} + I(m,s)\right)e^{-\gamma(s)l}$$$

*Bergeron等效电路历史电流源表达式，表示线路末端反射波对首端的影响，其中γ(s)为传播常数，l为线路长度*

### 算法步骤

1. 建立双导体传输线频域电报方程（式4-5），输入线路分布参数矩阵R、L、G、C（循环对称结构）

2. 应用Clarke相模变换矩阵T将耦合方程解耦为模态域（αβ坐标系）下的独立单导体方程（式8-9）

3. 在模态域计算频变特征阻抗Z(s)和传播常数γ(s)，采样2n+1个频点构建方程组（式17）

4. 采用Pade近似求解特征阻抗有理分式系数矩阵B，得到Z_d(s)的分子分母多项式（式15-16）

5. 将Z_d(s)展开为部分分式形式（式18），提取常数项k（电阻）、极点pj和留数rj

6. 构建Foster等效电路：电阻k与n个RC并联模块串联，其中Rj = -rj/pj，Cj = -1/(rj*pj)（由式19推导）

7. 对传输函数G(s)=e^(-γ(s)l)进行拉普拉斯反变换得到时域函数g(t)，应用Prony近似拟合为指数级数（式21）

8. 求解Prony系数：选取2n个采样点，构建方程组（式26-27）求解极点zj和留数rj，进而得到pj = ln(zj)/T

9. 基于Bergeron行波理论构建时域等效电路：特征阻抗用Foster电路替代，传输函数用Prony近似历史电流源实现（式13-14）

10. 采用MATLAB编程实现离散化仿真，设置步长Δt，通过递归卷积或历史电流源更新机制计算首末端电压响应

### 关键参数

- **R_matrix**: 串联电阻分布参数矩阵，对角阵[Rz, 0; 0, Rz]

- **L_matrix**: 串联电感分布参数矩阵，对称阵[Lz, Lh; Lh, Lz]，含自感和互感

- **C_matrix**: 并联电容分布参数矩阵，对称阵[Cz, Ch; Ch, Cz]，含自电容和互电容

- **G_matrix**: 并联电导分布参数矩阵，对角阵[Gz, 0; 0, Gz]

- **line_length**: 传输线长度l，单位km

- **pade_order**: Pade近似阶数n，论文采用5阶可达到10^-8精度

- **prony_order**: Prony近似指数项数n

- **sampling_points**: 特征阻抗拟合采样点数2n+1

- **time_step**: 仿真步长T（未明确给出具体值，需根据频率特性选择）

- **source_impedance**: 电源内阻Rdy

- **load_impedance**: 负载电阻RL

## 仿真结果

### 仿真测试

| 测试场景 | 结果描述 | 对比基线 |

|---------|---------|----------|

| 双导体有损频变传输线阶跃响应 | 在传输线首端施加阶跃电压激励，仿真持续时间内记录首端和末端电压动态响应曲线。模型准确捕捉了行波传播、反射和折射过程，以及频变参数引起的波形畸变和衰减特性。 | 与PSCAD/EMTDC商业软件仿真结果高度吻合，波形重合度良好，验证了Pade近似结合Foster电路与Prony近似方法在处理频变参数时的准确性 |

| 特征阻抗拟合精度验证 | 对比5阶Pade近似与泰勒级数展开对特征阻抗的拟合精度 | 5阶Pade近似与标准值偏差在10^-8数量级，而泰勒级数需取11项才能达到相同精度，证明Pade近似具有更快的收敛速度和更高的计算效率 |

## 量化发现

- Pade近似采用5阶有理分式时，特征阻抗拟合精度与标准值偏差达10^-8数量级
- 要达到与5阶Pade近似相同精度，传统泰勒级数展开需取11项，计算复杂度显著增加
- 相模变换矩阵T的列向量构成标准正交基，满足T^-1 = T^T，确保变换无损且可逆
- Foster等效电路将特征阻抗分解为1个电阻与n个RC并联模块的串联，其中n为Pade近似阶数
- Prony近似将传输函数时域响应表示为n个指数项的叠加，通过2n个采样点即可确定所有极点pj和留数rj

## 关键公式

### 特征阻抗部分分式展开式

$$$Z_d(s) = k + \sum_{j=1}^{n} \frac{r_j}{s - p_j}$$$

*Pade近似后，用于构建Foster等效电路，将频域阻抗转换为可电路实现的电阻-电容网络*

### 传输线频域传输矩阵方程

$$$\begin{bmatrix} U(k,s) \\ I(k,s) \end{bmatrix} = \begin{bmatrix} \frac{e^{\gamma(s)l} + e^{-\gamma(s)l}}{2} & -Z(s)\frac{e^{\gamma(s)l} - e^{-\gamma(s)l}}{2} \\ -\frac{1}{Z(s)}\frac{e^{\gamma(s)l} - e^{-\gamma(s)l}}{2} & \frac{e^{\gamma(s)l} + e^{-\gamma(s)l}}{2} \end{bmatrix} \begin{bmatrix} U(m,s) \\ I(m,s) \end{bmatrix}$$$

*描述线路首端(k)和末端(m)电压电流关系，是推导Bergeron等效电路的基础*

## 验证详情

- **验证方式**: 对比仿真验证：将本文MATLAB实现的时域模型与商业电磁暂态仿真软件PSCAD/EMTDC进行同条件对比
- **测试系统**: 双导体有损频变均匀传输线系统，含电源内阻Rdy、负载电阻RL，线路采用循环对称结构参数（自阻抗Rz、互电感Lh等），施加阶跃电压激励
- **仿真工具**: MATLAB（本文算法实现）、PSCAD/EMTDC（基准验证）
- **验证结果**: 在阶跃激励下，本文模型计算的传输线首末端电压响应与PSCAD/EMTDC仿真结果高度一致，准确复现了考虑频变参数后的波形衰减和畸变特性，验证了所提时域模型在处理频变传输线电磁暂态问题时的有效性和精度

## 适用边界

### 适用条件

- 适用于理解本文 `双导体有损频变均匀传输线的电磁暂态时域仿真模型研究`（2023） 在当前页面抽取范围内讨论的 EMT/电力系统暂态问题。
- 适用于以 prony、frequency-dependent 为核心的建模、仿真、等值、控制或稳定性分析场景；具体对象以原文算例和页面“涉及的模型”为准。
- 可作为知识图谱中的方法定位和文献入口，尤其用于追踪：提出了一种针对双导体有损频变均匀传输线的电磁暂态时域仿真模型

### 失效边界

- 不应外推到原文未覆盖的拓扑、控制策略、故障类型、频率范围、硬件平台或实时步长。
- 不应把页面中的“提高、显著、快速、准确”等概括性表述当作定量结论；只有“量化发现”和原文表图可核验的数字才可用于比较。
- 若页面作者、期刊、摘要或验证字段仍不完整，本页只能作为待复核文献入口，不能作为最终证据页引用。

### 关键假设

- 页面内容假设当前 PDF 抽取文本与 frontmatter 的 `sources` 指向同一篇论文。
- 方法结论默认受原文仿真工具、测试系统、参数设置、采样步长和对比基线约束。
- 当前边界层为保守整理：未从原文直接核验的内容不得升级为确定结论。

### 证据缺口

- 作者元数据仍需回到 PDF 首页或 metadata.json 复核。
- 源文件路径：`["EMT_Doc/36/刘刚 等 - 2023 - 双导体有损频变均匀传输线的电磁暂态时域仿真模型研究.pdf"]`；需要深修时应优先核对该 PDF 的首页、摘要、方法和实验表图。
