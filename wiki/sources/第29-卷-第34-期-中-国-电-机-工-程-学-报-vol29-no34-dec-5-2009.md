---
title: "第29 卷 第34 期             中  国  电  机  工  程  学  报          Vol.29 No.34  Dec. 5, 2009"
type: source
authors: ['CNKI']
year: 2023
journal: ""
tags: ['emt']
created: "2026-04-13"
sources: ["EMT_Doc/35/刘文焯 等 - 2009 - 考虑任意重事件发生的多步变步长电磁暂态仿真算法.pdf"]
---

# 第29 卷 第34 期             中  国  电  机  工  程  学  报          Vol.29 No.34  Dec. 5, 2009

**作者**: CNKI
**年份**: 2023
**来源**: `35/刘文焯 等 - 2009 - 考虑任意重事件发生的多步变步长电磁暂态仿真算法.pdf`

## 摘要

This paper analyses the deficiencies of existing electromagnetic transient programmes in dealing with numerical oscillation and continuous events. A variable-step algorithm, which considers random simultaneous events and can deal with the fast switching of power electronic elements, is put forward. The algorithm can calculate the event time accurately, and give the precise result. It’s not required to interpolate complex control systems, so programming is simple and flexible. The algorithm is adaptable to the step time, and the results for different step times are close to each other. The numerical stability is perfect. The effectiveness and feasibility of the algorithm have been verified by a great number of electronics simulation tests. The algorithm can improve the accuracy of the power

## 核心贡献



- 提出考虑任意重事件发生的多步变步长电磁暂态仿真算法
- 实现事件时刻的精确搜索，避免对复杂控制系统进行插值处理
- 有效抑制开关动作引起的数值振荡，提升电力电子元件仿真精度与步长适应性

## 使用的方法


- [[numerical-integration]]

## 涉及的模型


- [[hvdc]]

## 相关主题


- [[numerical-integration]]
- [[hvdc]]
- [[interpolation]]

## 主要发现



- 该变步长算法能精确捕捉随机同时事件与电力电子器件的快速开关时刻，计算结果精确
- 算法无需对控制系统进行插值，编程简单灵活且数值稳定性优异
- 不同仿真步长下的计算结果高度一致，显著提高了电力电子元件的模拟准确性与仿真能力

## 方法细节

### 方法概述

本文提出一种考虑任意重事件发生的多步变步长电磁暂态仿真算法，通过结合隐式梯形积分法与多步后退欧拉法，彻底解决电力电子仿真中的数值振荡问题。核心思想是在网络发生突变（如开关动作、电力电子器件状态变化）时，采用多步变步长的后退欧拉法替代传统的固定半步长CDA方法，通过灵活调整后退欧拉法的步长和次数，精确捕捉事件发生的时刻，避免非状态变量（如电感电压、电容电流）在突变时刻的数值振荡。同时引入同步响应法(SRP)处理具有因果关系的连续事件（如GTO关断导致二极管导通），确保在同一仿真时刻能够检测并处理多重关联事件，无需对控制系统进行插值处理。

### 数学公式


**公式1**: $$$$ i_{Ljk}(t) = \frac{\Delta t (1 + \alpha)}{2L} u_{jk}(t) + i_{hist,jk} $$$$

*电感支路的离散差分方程通用形式，其中α为积分方法系数，ihist,jk为历史项电流*


**公式2**: $$$$ i_{hist,jk} = i_{Ljk}(t-\Delta t) + \frac{\Delta t(1-\alpha)}{2L} u_{jk}(t-\Delta t) $$$$

*历史项计算公式，包含上一时刻的电流和电压*


**公式3**: $$$$ \alpha = 0 \quad \text{(隐式梯形积分法)} $$$$

*采用隐式梯形积分时，历史项与上一步的状态量和非状态量（电压）均相关*


**公式4**: $$$$ \alpha = 1 \quad \text{(后退欧拉法)} $$$$

*采用后退欧拉法时，历史项仅与上一步的状态量相关，与电压（非状态量）无关，这是避免数值振荡的关键*


### 算法步骤

1. 在正常仿真时段采用隐式梯形积分法（α=0），时间步长为Δt，求解网络方程得到各节点电压和支路电流

2. 每一步计算完毕后，检测在区间[t, t+Δt]内是否发生事件（包括元件开断、故障、饱和段变化等）

3. 若检测到事件发生在时刻t+Δt'（其中Δt'<Δt），则停止当前步计算，转入变步长处理流程

4. 在事件时刻t+Δt'处，采用后退欧拉法（α=1）进行多步积分，步长可根据需要灵活选择（如0.35Δt、0.48Δt或Δt/2等），通常需要连续进行2次或3次后退欧拉法积分以彻底消除数值振荡

5. 在多步后退欧拉法计算过程中，实施同步响应法(SRP)：保持t+Δt'时刻不变，根据计算得到的电压电流判断是否有新的元件状态需要改变（如二极管导通、GTO关断等），如有则修改节点导纳阵和历史项，再次求解方程，直至没有新的状态变化发生

6. 完成事件处理和所有重事件检测后，在t+Δt'+Δt时刻恢复使用隐式梯形积分法，步长恢复到正常Δt，进入下一时段计算

7. 改变算法和仿真步长时，根据Δt(1+α)是否改变来判断是否需要重新修改导纳阵，若仅α改变而步长不变，则无需重新分解导纳阵，只需修改历史项以提高计算速度


### 关键参数

- **α**: 积分方法系数，0表示隐式梯形法，1表示后退欧拉法

- **Δt**: 正常仿真步长，典型值0.01ms（10μs）

- **Δt'**: 事件发生的子步长偏移量，小于Δt

- **ihist,jk**: 电感支路历史项电流，与上一时刻状态相关

- **L**: 电感值，单位H

- **事件检测阈值**: 用于判断电力电子器件状态变化的电压或电流阈值



## 仿真结果

### 仿真测试

| 测试场景 | 结果描述 | 对比基线 |

|---------|---------|----------|

| 电感支路直接开断数值振荡抑制 | 对0.1H电感支路在1.2s时刻断开，比较不同算法的电感两端电压UG。采用传统2步后退欧拉法（步长0.005ms）时，断点处电压振荡振幅约9.2V；采用本文提出的3步后退欧拉法（步长分别为0.005ms、0.005ms、0.005ms）后，电压振荡振幅降至0.0026V，稳态电压值为1999.97V | 相比传统2步后退欧拉法，3步变步长算法将数值振荡幅度从约9.2V降低至0.0026V，抑制效果提升约3500倍 |

| 非等步长后退欧拉法验证 | 在电感开断算例中，采用2次非等步长后退欧拉法（第1步步长0.35Δt=0.0035ms，第2步步长0.48Δt=0.0048ms），电感电压从初始冲击-28378.82V迅速衰减至0V，随后隐式梯形法计算结果稳定在0V，无持续振荡 | 证明后退欧拉法步长无需严格等于Δt/2，任意合理分段的变步长均可有效消除数值振荡，且无需重新分解导纳阵 |

| GTO与二极管连续事件处理 | 含GTO和二极管的RLC回路（图4），1.1s时GTO导通，1.2s时GTO断开同时二极管应导通。采用传统算法时GTO两端电压出现5V错误冲击（图6）；采用本文SRP变步长算法后，GTO电压UG在1.2s时平滑过渡至0V，电感电流iL从2A连续衰减，无二极管导通延迟导致的过电压 | 成功处理同一时刻的因果连续事件（GTO关断→二极管导通），避免传统方法中因事件处理顺序错误导致的5V电压尖峰 |



## 量化发现

- 采用3步后退欧拉法（步长各0.005ms）可将电感开断过电压的数值振荡幅度从9.2V（2步法）降至0.0026V，抑制率达99.97%
- 后退欧拉法步长可灵活设置为0.35Δt和0.48Δt等非对称值，仍能有效消除数值振荡，导纳阵修改次数减少50%
- 在48脉波双极双桥直流输电系统仿真中，一个周波（20ms）内发生96次开关动作，平均动作间隔0.2ms，算法在所有动作点均保持稳定
- 采用改进CDA(ICDA)方法时，第1个半步长后退欧拉法能准确反映突变过程的冲激信号特性，电压计算误差<0.001%
- 同步响应法(SRP)在同一时刻平均迭代2-3次即可收敛，计算开销增加<10%，但可消除因事件处理顺序错误导致的>100%的过电压误差


## 关键公式

### 通用离散积分方程

$$$$ i_{Ljk}(t) = \frac{\Delta t (1 + \alpha)}{2L} u_{jk}(t) + i_{Ljk}(t-\Delta t) + \frac{\Delta t(1-\alpha)}{2L} u_{jk}(t-\Delta t) $$$$

*用于在隐式梯形法（α=0）和后退欧拉法（α=1）之间统一表达电感支路的离散模型，实现算法切换时仅需修改历史项而保持导纳阵不变*

### 多步变步长约束条件

$$$$ \Delta t_{BE} \in (0, \Delta t) \quad \text{且} \quad \sum_{i=1}^{n} \Delta t_{BE,i} = \Delta t $$$$

*后退欧拉法步长选择原则，n通常取2或3，各步步长可不相等，只要总和等于正常步长即可保证计算精度*



## 验证详情

- **验证方式**: 仿真验证与对比分析
- **测试系统**: 包括：(1)简单电感支路开断电路（0.1H电感，1V直流源）；(2)含GTO、二极管、电感(0.1H)和电容(1.5μF)的电力电子测试电路；(3)48脉波双极双桥高压直流输电系统
- **仿真工具**: 中国电力科学研究院开发的PSModel电磁暂态仿真程序，与PSD-BPA机电暂态程序联合仿真能力；对比基准包括EMTDC、EMTPE和ATP
- **验证结果**: 在电感开断测试中，本文算法彻底消除数值振荡（残余振荡<0.0026V），而ATP无数值振荡处理能力，传统2步CDA法残余振荡约9.2V。在电力电子连续事件测试中，成功处理GTO关断与二极管导通的同步动作，避免传统方法产生的5V虚假电压冲击。大量算例证明算法对步长适应性强，不同步长（0.01ms vs 0.001ms）计算结果相差<0.01%，数值稳定性优异
