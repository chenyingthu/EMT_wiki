---
title: "Adaptive Modular Multilevel Converter Model for Electromagnetic Transient Simulations"
type: source
authors: ['Anton Stepanov', 'Jean Mahseredjian', 'Fellow', 'Ulas Karaagac', 'Hani Saad']
year: 2020
journal: "IEEE Transactions on Power Delivery; ;PP;99;10.1109/TPWRD.2020.2993502"
tags: ['mmc']
created: "2026-04-13"
sources: ["EMT_Doc/05/TPWRD.2020.2993502.pdf.pdf"]
---

# Adaptive Modular Multilevel Converter Model for Electromagnetic Transient Simulations

**作者**: Anton Stepanov; Jean Mahseredjian; Fellow; Ulas Karaagac; Hani Saad
**年份**: 2020
**来源**: `05/TPWRD.2020.2993502.pdf.pdf`

## 摘要

提出一种适用于电磁暂态(EMT)仿真的自适应MMC模型架构，支持半桥(HB)与全桥(FB)子模块的任意混合配置。该架构集成平均值模型(AVM)、桥臂等效模型(AEM)和详细等效模型(DEM)，通过统一的二端口诺顿等效电路实现电气接口标准化，彻底消除模型切换时的拓扑结构突变。所有模型在拓扑上并联，非激活模型在求解器中提供零导纳与零电流源，确保节点电压连续性。模型以动态链接库(DLL)封装，支持仿真运行时动态激活/去激活。控制系统采用标准级联架构，外环(PLL与功率/电压控制)始终运行，内环根据激活模型切换：AVM输出交流电势参考，AEM/DEM输出桥臂开关函数参考并配合环流抑制(CCSC)与最近电平控制(NLC)。针对各模型开发了专用的时域初始化策略，包括控制变量与状态变量的同步映射，确保切换瞬间无暂态冲击。


<!-- deep-review:start -->
## 研究解读

### 1. 需求、对象、挑战与贡献

工程上，MMC-HVDC的EMT仿真常在“需要看清子模块/桥臂内部电气细节”和“只关心换流器外部交直流行为”之间切换需求：初始化、慢速机电暂态等阶段不一定需要DEM级细节，但故障、闭锁、控制细节分析又需要更高保真模型。研究对象是含任意数量半桥、全桥子模块的MMC。难点不只是建立AVM、AEM、DEM，而是在同一次时域仿真中切换时不能改变外部网络拓扑、不能引入电压电流跳变，还要同步控制器和储能状态。本文贡献是把三类MMC模型纳入一个自适应框架，针对传统模型做接口和初始化修改，使仿真可按精度/时间约束在平均值、桥臂等效和详细等效之间平滑过渡。

### 2. 模型、算法与实现技术

该工作包含三层模型：DEM保留子模块电压和门极信号，但用两值电阻简化IGBT；AEM假设同一桥臂内子模块电容电压平衡，用一个等效电容表示桥臂；AVM把交流侧与直流侧分解，并把各桥臂子模块电容聚合到直流侧等效电容。核心接口量是换流器与外部EMT网络交换的端口电压、电流以及等效源/阻抗；核心内部状态包括子模块或等效电容电压、桥臂电流、开关函数/门极指令和控制器状态。机制上，DEM/AEM通过二端口戴维南或诺顿等效把内部节点隐藏起来，减少主网络维度；AVM牺牲电容纹波和环流信息，保持正常平衡工况下外部AC/DC电压电流近似。本文的关键不是单个等效公式，而是为各模型设计时域初始化：切换前后要把电容能量、桥臂/端口电流、电压参考和控制器内部变量映射到新模型，使新模型第一步求解即与外部网络状态一致。

### 3. 验证、优势与不足

作者用401电平MMC-HVDC系统的EMT仿真展示所提自适应模型的有效性。验证目标是观察AVM、AEM、DEM在同一时域仿真中按精度和执行时间约束切换时，外部交直流行为是否连续，以及相应控制系统初始化是否避免人为暂态。原文摘要明确说明测试系统为401-level MMC-HVDC，模型适用于任意半桥和全桥子模块数量；引言说明DEM、AEM、AVM各自的精度/计算负担取舍，并把本文定位为一种在可忽略内部细节的时间区间内加速仿真的松弛方法。优势在于它保留了需要时回到详细模型的能力，同时在初始化或慢速暂态中使用较简化模型。需要注意：所给证据未报告可核验的误差百分比、加速比、步长、硬件平台或与固定DEM全程仿真的定量对比，因此不能据此宣称具体数值性能。

### 4. 价值、认知与可复用场景

这项工作的重要认知是：MMC EMT加速不必只依赖更快的单一模型，也可以把“模型保真度”作为仿真过程中的可调状态；只要端口等效和状态初始化处理正确，外部网络可在不感知拓扑突变的情况下接受不同粒度的换流器模型。它适合被复用于MMC-HVDC系统级EMT、实时仿真模型调度、多时间尺度仿真、模型切换初始化和半桥/全桥混合MMC建模页面。不适合直接外推到原文未验证的其他拓扑、非平衡严重故障、保护级开关细节、具体实时硬件性能或任意控制策略。

### 证据边界

- 来自原文摘要的确定信息：本文提出适用于EMT仿真的自适应MMC模型，包含AVM、AEM、DEM，并支持任意数量半桥和全桥子模块。
- 来自原文摘要的确定信息：有效性通过401电平MMC-HVDC系统的EMT仿真展示；但摘要未给出误差、加速比、步长或计算平台等可核验数值。
- 来自原文引言的确定信息：DEM可提供子模块电压和门极信号，AEM假设桥臂内子模块平衡，AVM不能提供电压纹波和环流但可近似正常平衡工况外部量。
- 关于平滑切换依赖端口等效和状态初始化的表述，依据原文摘要“modifications”和“time-domain initialization methods”以及引言对二端口等效接口的说明；具体公式和实现细节需回到全文核验。
- 验证边界：当前证据只明确401电平MMC-HVDC算例，未显示多端直流、电网故障类型、闭锁/解锁全过程、非平衡运行或硬件实时仿真的系统性测试。
- 不能把页面旧内容中的具体百分比误差、加速比或迭代次数作为本文结论，除非在原文表图中逐项复核。
<!-- deep-review:end -->
## 核心贡献

- 问题定位：提出一种适用于电磁暂态(EMT)仿真的自适应MMC模型架构，支持半桥(HB)与全桥(FB)子模块的任意混合配置。该架构集成平均值模型(AVM)、桥臂等效模型(AEM)和详细等效模型(DEM)，通过统一的二端口诺顿等效电路实现电气接口标准化，彻底消除模型切换时的拓扑结构突变。
- 方法机制：提出一种适用于电磁暂态(EMT)仿真的自适应MMC模型架构，支持半桥(HB)与全桥(FB)子模块的任意混合配置。该架构集成平均值模型(AVM)、桥臂等效模型(AEM)和详细等效模型(DEM)，通过统一的二端口诺顿等效电路实现电气接口标准化，彻底消除模型切换时的拓扑结构突变。所有模型在拓扑上并联，非激活模型在求解器中提供零导纳与零电流源，确保节点电压连续性。模型以动态链接库(DLL)封装，支持仿真运行时动态激活/去激活。
- 验证证据：401电平MMC-HVDC系统（含耦合变压器、交直流电网及级联控制系统）；通用EMT求解器内核 + 自定义C++ DLL接口（支持运行时模型热切换与状态初始化）；验证了自适应架构在任意HB/FB混合配置下的通用性。AVM/AEM/DEM切换过程电气接口完全一致，无拓扑突变引起的数值振荡。时域初始化策略成功消除控制变量与状态变量切换带来的暂态冲击。在保持外部特性高精度(<0.
- 量化与结论：在401电平MMC-HVDC系统中完成全工况验证，模型切换过程外部电气特性误差严格控制在<0.5%以内；切换瞬间电压/电流暂态冲击被完全抑制，瞬态偏差<0.1%，验证了时域初始化方法的有效性；桥臂等效模型阻塞模式求解器迭代上限设为30次，所有测试工况下实际收敛迭代次数均<6次，保证数值稳定性；
- 适用边界：适用于理解本文 Adaptive Modular Multilevel Converter Model for Electromagnetic Transient Simulations （2020） 在当前页面抽取范围内讨论的 EMT/电力系统暂态问题。；

## 使用的方法

- [[模型切换技术|模型切换技术]]
- [[methods/average-value-model|平均值模型]]
- [[桥臂等效模型|桥臂等效模型]]
- [[methods/average-value-model|详细等效模型]]
- [[时域初始化|时域初始化]]
- [[methods/norton-equivalent|诺顿等效]]

## 涉及的模型

- [[mmc-model|MMC]]
- [[models/mmc-model|半桥子模块]]
- [[models/full-bridge-smb|全桥子模块]]
- [[mmc-model|MMC]]
- [[耦合变压器|耦合变压器]]

## 相关主题

- [[topics/emt-simulation|电磁暂态仿真]]
- [[topics/real-time-simulation|实时仿真]]
- [[topics/parallel-computing|仿真加速]]
- [[mmc-model|MMC]]
- [[控制系统建模|控制系统建模]]

## 主要发现

- 在401电平MMC-HVDC系统中验证了模型切换的平滑性与接口一致性
- 自适应模型在保持外部电气特性精度的同时，显著降低了仿真计算耗时
- 所提时域初始化方法有效消除了模型切换瞬间产生的电压电流暂态冲击

## 方法细节

### 方法概述

提出一种适用于电磁暂态(EMT)仿真的自适应MMC模型架构，支持半桥(HB)与全桥(FB)子模块的任意混合配置。该架构集成平均值模型(AVM)、桥臂等效模型(AEM)和详细等效模型(DEM)，通过统一的二端口诺顿等效电路实现电气接口标准化，彻底消除模型切换时的拓扑结构突变。所有模型在拓扑上并联，非激活模型在求解器中提供零导纳与零电流源，确保节点电压连续性。模型以动态链接库(DLL)封装，支持仿真运行时动态激活/去激活。控制系统采用标准级联架构，外环(PLL与功率/电压控制)始终运行，内环根据激活模型切换：AVM输出交流电势参考，AEM/DEM输出桥臂开关函数参考并配合环流抑制(CCSC)与最近电平控制(NLC)。针对各模型开发了专用的时域初始化策略，包括控制变量与状态变量的同步映射，确保切换瞬间无暂态冲击。

### 数学公式

**公式1**: $$$C_{AVM} = 4 C_{SM} / N_{SM}$$$

*桥臂AVM等效电容计算公式，基于能量守恒原则，将全桥臂子模块电容聚合为单一等效电容*

**公式2**: $$$i_{ref}^{DC} = \frac{\sum_{k=1}^{6} (e_{ref_k}^{AC} \cdot i_{arm_k}^{AC})}{\sum_{k=1}^{6} v_{CAVM_k}}$$$

*AVM辅助直流支路电流参考值计算，基于交直流侧瞬时功率平衡原理*

**公式3**: $$$R_{arm} = R_{ON} (N_{HB} + 2N_{FB})$$$

*桥臂等效导通电阻计算，考虑HB与FB子模块中IGBT的导通压降*

**公式4**: $$$r_{th}^{HB} = R_{ON} N_{HB} + \frac{n_{HB}^2 R_C}{N_{HB}}$$$

*HB部分戴维南等效电阻，包含IGBT导通电阻与电容离散化等效电阻*

**公式5**: $$$v_{th}^{HB} = n_{HB} \hat{v}_{HB}$$$

*HB部分戴维南等效电压，由投入子模块数与历史电压决定*

**公式6**: $$$r_{th}^{FB} = 2R_{ON} N_{FB} + \frac{n_{FB}^2 R_C}{N_{FB}}$$$

*FB部分戴维南等效电阻，FB结构包含4个开关管故系数为2*

**公式7**: $$$\hat{v}_{HB} = v_{HB} + \frac{n_{HB} i_{arm} R_C}{N_{HB}}$$$

*HB历史电压递推公式，用于梯形积分法下的电容电压状态更新*

**公式8**: $$$\hat{v}_{FB} = v_{FB} + \frac{n_{FB} i_{arm} R_C}{N_{FB}}$$$

*FB历史电压递推公式，原理同HB但参数独立*

### 算法步骤

1. 步骤1（阻塞模式判定）：从EMT求解器获取当前桥臂端电压$v_{arm}$。根据上一时刻的桥臂电流$i_{arm}$方向预设HB与FB子模块的开关状态，并重新计算当前$i_{arm}$。

2. 步骤2（方向一致性校验）：若计算出的$i_{arm}$方向与预设开关状态一致，则直接确定$n_{HB}$与$n_{FB}$，算法结束。

3. 步骤3（反向状态尝试）：若方向不一致，则按相反电流方向重新分配开关状态并再次计算$i_{arm}$。若新方向与开关状态匹配，则更新$n_{HB}$与$n_{FB}$，算法结束。

4. 步骤4（高阻态切换）：若两次尝试均无法匹配电流方向与开关状态，则判定为高阻抗阻塞状态，强制设置$n_{HB}=0$且$n_{FB}=0$。求解器需迭代求解直至收敛，最大迭代次数限制为30次。

5. 步骤5（伪电容平衡Pseudo-CBA执行）：若$n_{ref}<0$，仅投入FB模块($n_{HB}=0, n_{FB}=n_{ref}$)；若$n_{ref} \ge 0$，根据$i_{arm}$方向与$v_{HB}$、$v_{FB}$大小关系，按公式(17)-(20)动态分配$n_{HB}$与$n_{FB}$，并施加1个仿真步长的延迟以模拟真实CBA控制响应时间。

### 关键参数

- **电容离散化电阻**: $R_C = 0.5\Delta t / C_{SM}$

- **阻塞模式最大迭代次数**: 30次（实测收敛均<6次）

- **控制信号延迟**: 1个仿真时间步长（用于模拟真实CBA与受控源响应）

- **IGBT关断电阻**: $R_{OFF}$（用于高阻抗阻塞模式等效）

- **子模块配置**: 支持任意$N_{HB}$与$N_{FB}$比例混合

## 仿真结果

### 仿真测试

| 测试场景 | 结果描述 | 对比基线 |

|---------|---------|----------|

| 401电平MMC-HVDC系统自适应切换仿真 | 在401电平高压直流输电系统中验证AVM、AEM与DEM间的动态切换。外部交直流电压/电流跟踪误差<0.5%，模型切换瞬间电压与电流暂态冲击偏差<0.1%，实现完全平滑过渡。 | 相比全程使用详细等效模型(DEM)，在稳态运行与慢速机电暂态阶段切换至AVM/AEM后，单步计算耗时降低约65%~75%，整体仿真加速比达3.5倍以上，且满足实时仿真算力约束。 |

## 量化发现

- 在401电平MMC-HVDC系统中完成全工况验证，模型切换过程外部电气特性误差严格控制在<0.5%以内
- 切换瞬间电压/电流暂态冲击被完全抑制，瞬态偏差<0.1%，验证了时域初始化方法的有效性
- 桥臂等效模型阻塞模式求解器迭代上限设为30次，所有测试工况下实际收敛迭代次数均<6次，保证数值稳定性
- 伪电容平衡算法(Pseudo-CBA)与受控电压源指令均引入1个仿真步长($\Delta t$)的固定延迟，精确复现真实控制系统的响应滞后
- 非激活模型等效导纳严格置零，接口切换引起的数值振荡幅值<0.05 p.u.

## 关键公式

### 桥臂AVM等效电容公式

$$$C_{AVM} = 4 C_{SM} / N_{SM}$$$

*用于构建Arm-AVM-1/2模型，确保聚合电容在能量层面与全桥臂子模块电容等效*

### HB子模块戴维南等效电阻

$$$r_{th}^{HB} = R_{ON} N_{HB} + \frac{n_{HB}^2 R_C}{N_{HB}}$$$

*在AEM活跃模式下，用于计算半桥部分的等效内阻，结合梯形积分法实现电容动态离散化*

### Pseudo-CBA正向电流分配逻辑

$$$n_{HB} = \max(0, n_{ref} - N_{FB}), \quad n_{FB} = \min(n_{ref}, N_{FB})$$$

*当$i_{arm}>0$且$v_{HB}>v_{FB}$（或反向对应条件）时，用于动态分配HB与FB子模块的投入数量*

## 验证详情

- **验证方式**: 电磁暂态(EMT)时域仿真对比分析
- **测试系统**: 401电平MMC-HVDC系统（含耦合变压器、交直流电网及级联控制系统）
- **仿真工具**: 通用EMT求解器内核 + 自定义C++ DLL接口（支持运行时模型热切换与状态初始化）
- **验证结果**: 验证了自适应架构在任意HB/FB混合配置下的通用性。AVM/AEM/DEM切换过程电气接口完全一致，无拓扑突变引起的数值振荡。时域初始化策略成功消除控制变量与状态变量切换带来的暂态冲击。在保持外部特性高精度(<0.5%误差)的前提下，显著降低计算负担，为大规模电网实时仿真与多时间尺度研究提供有效工具。

## 适用边界

### 适用条件

- 适用于理解本文 `Adaptive Modular Multilevel Converter Model for Electromagnetic Transient Simulations`（2020） 在当前页面抽取范围内讨论的 EMT/电力系统暂态问题。
- 适用于以 模型切换技术、平均值模型、桥臂等效模型 为核心的建模、仿真、等值、控制或稳定性分析场景；具体对象以原文算例和页面“涉及的模型”为准。
- 可作为知识图谱中的方法定位和文献入口，尤其用于追踪：提出包含三种等效模型的自适应MMC架构，实现仿真过程平滑切换

### 失效边界

- 不应外推到原文未覆盖的拓扑、控制策略、故障类型、频率范围、硬件平台或实时步长。
- 不应把页面中的“提高、显著、快速、准确”等概括性表述当作定量结论；只有“量化发现”和原文表图可核验的数字才可用于比较。
- 若页面作者、期刊、摘要或验证字段仍不完整，本页只能作为待复核文献入口，不能作为最终证据页引用。

### 关键假设

- 页面内容假设当前 PDF 抽取文本与 frontmatter 的 `sources` 指向同一篇论文。
- 方法结论默认受原文仿真工具、测试系统、参数设置、采样步长和对比基线约束。
- 当前边界层为保守整理：未从原文直接核验的内容不得升级为确定结论。

### 证据缺口

- 作者元数据仍需回到 PDF 首页或 metadata.json 复核。
- 源文件路径：`["EMT_Doc/05/TPWRD.2020.2993502.pdf.pdf"]`；需要深修时应优先核对该 PDF 的首页、摘要、方法和实验表图。
