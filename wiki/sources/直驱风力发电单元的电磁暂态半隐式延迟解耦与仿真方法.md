---
title: "直驱风力发电单元的电磁暂态半隐式延迟解耦与仿真方法"
type: source
authors: ['CNKI']
year: 2022
journal: ""
tags: ['emt']
created: "2026-04-13"
sources: ["EMT_Doc/16/姚蜀军 等 - 2022 - 直驱风力发电单元的电磁暂态半隐式延迟解耦与仿真方法.pdf"]
---

# 直驱风力发电单元的电磁暂态半隐式延迟解耦与仿真方法

**作者**: CNKI
**年份**: 2022
**来源**: `16/姚蜀军 等 - 2022 - 直驱风力发电单元的电磁暂态半隐式延迟解耦与仿真方法.pdf`

## 摘要

Due to the large scale of existing wind farms, the number of wind turbines and the difficulty of modeling and simulation of wind turbines, the efficiency of electromagnetic transient simulation (EMT) of wind farms is low. In order to solve the above problems, a fine-grained decoupling and parallel simulation method for direct-drive wind power generation unit was proposed based on the semi-implicit latency decoupling (SILDP) method to realize efficient EMT of direct-drive wind power generation unit. Firstly, based on the matrix splitting principle, the direct-drive wind power generation unit was divided into several subsystems according to the state variables. The SILDP was used to decouple the time delay between different state variables of direct-drive wind power generation unit. Based on

## 核心贡献



- 提出基于半隐式延迟解耦(SILDP)的直驱风力发电单元细粒度解耦与并行仿真方法
- 基于矩阵分裂原理与状态变量分组构建解耦模型，并设计高效计算时序与流程

## 使用的方法


- [[parallel]]
- [[state-space]]
- [[numerical-integration]]

## 涉及的模型


- [[pmsm-model]]
- [[vsc-model]]

## 相关主题


- [[wind-farm]]
- [[parallel]]
- [[harmonic]]

## 主要发现



- 半隐式延迟解耦方法能有效实现直驱风机不同状态变量组间的细粒度解耦与并行计算
- 该方法在显著提升电磁暂态仿真计算效率的同时，兼顾了较高的仿真精度

## 方法细节

### 方法概述

基于半隐式延迟解耦(SILDP)的细粒度并行仿真方法。首先利用矩阵分裂原理，将直驱风力发电单元按状态变量（电感电流、电容电压）分解为多个子系统。通过引入半步时延（half-step delay），实现不同状态变量组之间的时间延迟解耦，将原始耦合的大规模系统转化为可并行计算的解耦子系统。该方法应用于背靠背VSC换流器、PMSM发电机及滤波网络的联合仿真，在保持数值稳定性的同时实现并行计算加速。

### 数学公式


**公式1**: $$$S_n = \begin{cases} 1, & \text{上桥臂导通} \\ 0, & \text{上桥臂关断} \end{cases} \quad (n=a,b,c)$$$

*IGBT/二极管开关组状态函数，用于描述VSC桥臂导通状态*


**公式2**: $$$\begin{cases} L_a\frac{di_a}{dt} - L_b\frac{di_b}{dt} = -R_{on}(i_a - i_b) - (S_a - S_b)U_c + (u_a - u_b) \\ L_b\frac{di_b}{dt} - L_c\frac{di_c}{dt} = -R_{on}(i_b - i_c) - (S_b - S_c)U_c + (u_b - u_c) \\ L_c\frac{di_c}{dt} - L_a\frac{di_a}{dt} = -R_{on}(i_c - i_a) - (S_c - S_a)U_c + (u_c - u_a) \\ C\frac{dU_c}{dt} = S_a i_a + S_b i_b + S_c i_c + i_d \end{cases}$$$

*VSC换流器状态方程，基于KVL和KCL建立，以电感电流和电容电压为状态变量*


**公式3**: $$$\mathbf{A}\dot{\mathbf{x}} = \mathbf{B}\mathbf{x} + \mathbf{u}$$$

*状态空间标准形式，其中A为电感/电容矩阵，B包含导通电阻和开关函数耦合项*


**公式4**: $$$\mathbf{A}\frac{\mathbf{x}^{n+1/2} - \mathbf{x}^{n-1/2}}{\Delta t} = \mathbf{B}_1\mathbf{x}^{n} + \mathbf{B}_2\mathbf{x}^{n-1/2} + \mathbf{u}^{n}$$$

*半隐式延迟解耦递推格式，通过矩阵分裂将系统分为显式和隐式两部分，实现半步长延迟解耦*


### 算法步骤

1. 状态变量分组：将直驱风力发电单元的状态变量按电感电流（ia, ib, ic）和电容电压（Uc）进行分组，建立状态空间模型

2. 矩阵分裂：将系统矩阵B分裂为B1和B2两部分，分别对应当前时步和延迟时步的耦合关系

3. 半步时延解耦：采用半步长（Δt/2）延迟策略，将不同状态变量组的计算在时间上解耦，使各子系统可独立求解

4. 子系统并行计算：机侧换流器、网侧换流器及机械系统作为独立子系统，利用多核CPU或GPU并行求解各子系统的状态方程

5. 接口变量更新：通过受控源等效（附录B.1）实现解耦子系统间的信息交互，更新等效电压源和电流源

6. 时序同步：采用预测-校正机制或插值方法，确保半步长延迟下的数值同步和精度


### 关键参数

- **Ron**: 0.01 Ω（IGBT导通电阻）

- **La, Lb, Lc**: 交流侧各相电感（机侧和网侧滤波电感）

- **C**: 直流侧电容（背靠背换流器中间电容）

- **Sa, Sb, Sc**: 三相开关函数，取值为0或1

- **k_uab, kubc, kuca**: 线电压开关系数，定义为Sab=Sa-Sb等

- **Δt**: 仿真步长，半步长延迟为Δt/2



## 仿真结果

### 仿真测试

| 测试场景 | 结果描述 | 对比基线 |

|---------|---------|----------|

| 直驱风力发电单元电磁暂态仿真 | 在PSCAD/EMTDC中构建包含PMSM、背靠背VSC换流器、LC滤波器和集电线路的完整直驱风机模型，验证SILDP方法的准确性和计算效率 | 与传统串行电磁暂态仿真方法相比，计算效率显著提高，同时保持较高的仿真精度（文本未提供具体加速比数值） |

| 背靠背换流器解耦验证 | 分别对机侧换流器（k=1）和网侧换流器（k=2）进行解耦仿真，验证半步时延解耦在开关动作时的数值稳定性 | 避免了传统前向欧拉法一步延时导致的非状态量突变数值振荡问题 |



## 量化发现

- 导通电阻Ron设置为0.01Ω，用于计及IGBT导通损耗
- 采用半步长（half-step）延迟实现解耦，时间延迟为Δt/2
- 状态变量分组数：至少4组（三相电流ia, ib, ic和直流电压Uc）
- 解耦后子系统间通过受控源等效连接（等效电压源Ueq和电流源）
- 矩阵分裂后，各子系统可并行计算，适合GPU加速
- 文本片段未提供具体的仿真加速倍数（如'提升X倍'）和精度误差百分比（如'误差<Y%'）的数值结果


## 关键公式

### VSC状态空间方程

$$$$\begin{bmatrix} L_a & -L_b & 0 & 0 \\ 0 & L_b & -L_c & 0 \\ -L_a & 0 & L_c & 0 \\ 0 & 0 & 0 & C \end{bmatrix} \begin{bmatrix} \dot{i}_a \\ \dot{i}_b \\ \dot{i}_c \\ \dot{U}_c \end{bmatrix} = \begin{bmatrix} -R_{on} & R_{on} & 0 & -k_{uab} \\ 0 & -R_{on} & R_{on} & -k_{ubc} \\ R_{on} & 0 & -R_{on} & -k_{uca} \\ k_{ia} & k_{ib} & k_{ic} & 0 \end{bmatrix} \begin{bmatrix} i_a \\ i_b \\ i_c \\ U_c \end{bmatrix} + \begin{bmatrix} u_a-u_b \\ u_b-u_c \\ u_c-u_a \\ i_d \end{bmatrix}$$$$

*描述两电平VSC换流器的电磁暂态行为，用于构建半隐式延迟解耦的基础模型*

### SILDP递推格式

$$$$\mathbf{x}^{n+1/2} = \mathbf{x}^{n-1/2} + \frac{\Delta t}{2}\mathbf{A}^{-1}(\mathbf{B}_1\mathbf{x}^{n} + \mathbf{B}_2\mathbf{x}^{n-1/2} + \mathbf{u}^{n})$$$$

*半步长延迟解耦的核心算法，实现状态变量的分组递推计算*



## 验证详情

- **验证方式**: 仿真验证与对比分析
- **测试系统**: 直驱风力发电单元完整系统，包含：永磁同步风力发电机(PMSM)、机侧和网侧背靠背两电平VSC换流器、LC/LCL滤波器、升压变压器、集电线路
- **仿真工具**: PSCAD/EMTDC电磁暂态仿真软件
- **验证结果**: 所提方法在提高直驱风力发电单元电磁暂态仿真计算效率的基础上兼顾了较高的仿真精度，有效解决了大规模风电场仿真效率低下的问题。解耦模型避免了传统一步延时方法引起的数值振荡，适用于含频繁开关动作的电力电子装置仿真。
