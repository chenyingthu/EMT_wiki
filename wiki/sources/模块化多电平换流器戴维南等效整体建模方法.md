---
title: "模块化多电平换流器戴维南等效整体建模方法"
type: source
authors: ['许建中']
year: 2015
journal: "中国电机工程学报"
tags: ['mmc', 'emt']
created: "2026-04-13"
sources: ["EMT_Doc/33/许建中 等 - 2015 - 模块化多电平换流器戴维南等效整体建模方法.pdf"]
---

# 模块化多电平换流器戴维南等效整体建模方法

**作者**: 许建中
**年份**: 2015
**来源**: `33/许建中 等 - 2015 - 模块化多电平换流器戴维南等效整体建模方法.pdf`

## 摘要

针对模块化多电平换流器(modular multilevel converter，MMC)在电平数很高时电磁暂态离线仿真效率低下的问题，提出一种基于戴维南等效的 MMC 电磁暂态整体建模方法。具体而言，在建立 MMC 戴维南等效模型的过程中：1）假设 MMC 中全部开关器件都具备理想的关断特性(也即关断电阻无穷大)；2）采用同样绝对稳定的后退欧拉法来替代通常采用的梯形积分法对子模块电容进行离散化；3）结合 1）和 2），提出一种计算复杂度与 MMC 桥臂子模块数相同的高效排序均压算法；4）针对所提出模型的特点，提出一种精确的 MMC 闭锁仿真方法。所提 MMC 整体模型的计算复杂度随着电平数的增加线性增长。在 PSCAD/EMTDC 下与 MMC 详细模型仿真对比表明，所提 MMC 整体模型的暂稳态最大精度误差在 2‰以内，并且在仿真 121 电平 MMC 时所提模型的加速比在 2 770 倍以上。


<!-- deep-review:start -->
## 研究解读

### 1. 需求、对象、挑战与贡献

实际需求来自高电平MMC-HVDC的离线电磁暂态仿真：工程MMC往往含数千个子模块，若在PSCAD/EMTDC中逐开关、逐电容搭建详细模型，20 μs步长仿真数秒可能耗时极长。研究对象是MMC一个桥臂内串联的N个子模块及其电容电压充放电、桥臂等效接口和均压控制计算。难点不只是换流器网络节点多，还包括每步都要更新大量子模块电容并执行电压排序均压；已有戴维南/受控源精确等效模型主要压缩电气网络复杂度，未同步降低均压算法负担。本文的贡献是把桥臂戴维南等效、理想关断开关假设、后退欧拉电容离散、高效排序均压和闭锁二极管插值仿真合成一个“整体模型”，目标是在保留子模块电容充放电可追踪能力的同时，使模型计算量随桥臂子模块数线性增长。

### 2. 模型、算法与实现技术

模型以MMC桥臂为等效单元：每个子模块先被表示为戴维南电压源串联电阻，再把N个子模块的等效量代数叠加为桥臂的两端口戴维南等效，向外部EMT网络只暴露桥臂端电压/电流关系。核心状态量是各子模块电容电压，核心接口量是桥臂电流IARM、投入子模块数NON、桥臂等效电压和等效电阻。关键处理有三点：第一，关断开关取理想开路，使切除子模块的电容电流为0、等效电压源为0，避免在关断支路中保留很大的有限电阻带来的额外计算和误差耦合；第二，用后退欧拉法离散电容，等效电阻RCE=ΔT/C，历史电压源只等于上一时刻电容电压，因此投入子模块的电压增量可统一写成ΔUC=IARM·RCE；第三，基于上述统一增量构造排序均压算法，使每步不必对每个子模块执行复杂独立戴维南反解，而是按电容电压排序和桥臂电流方向选择应投入/切除的模块。桥臂等效电阻随投入数变化，如RARMEQ=NRON+NONRCE；等效电压为投入子模块电容电压之和。闭锁工况则用带插值函数的二极管模型处理续流导通时刻。

### 3. 验证、优势与不足

作者在PSCAD/EMTDC中用所提整体模型与MMC fully detailed model对比，主要检查稳态和暂态波形误差以及计算耗时。摘要给出的可核验指标是：暂态和稳态最大误差小于2‰；在121电平MMC详细模型对比中，整体模型加速比超过2770倍。验证基线明确为详细开关模型，指标包括精度误差和加速比，因而能说明该方法相对逐子模块详细仿真在高电平场景下的计算优势。优势的机制在于电气网络由大量子模块串联支路压缩为桥臂两端口等效，同时均压算法也随N线性增长，避免“网络已等效但排序仍成为瓶颈”。不过，从已给证据看，量化精度和加速比主要绑定于作者的PSCAD/EMTDC实现、121电平测试和文中参数设置；跨湾工程2592子模块、舟山8000余子模块在引言中用于说明工程规模需求，不等同于本文已报告完整对比验证。闭锁仿真方法被提出为精确处理闭锁模式，但当前摘录未给出闭锁误差表、故障类型覆盖范围或与其他闭锁模型的量化对比。

### 4. 价值、认知与可复用场景

这项工作的主要认知价值是指出MMC EMT加速不能只等效电气网络，还必须把子模块电容更新和均压排序一起纳入整体复杂度设计。它适合用于需要保留子模块电容电压动态、又无法承受详细模型计算量的离线MMC-HVDC EMT研究，例如高电平换流器控制策略、站级暂态过程、闭锁过程建模入口，以及后续戴维南等效、快速均压算法、平均值模型对比页面的基础文献。它不应被外推为所有拓扑、所有故障和实时仿真平台均适用；若研究重点是器件级开关损耗、非理想关断漏电、驱动死区或半导体暂态细节，本文的理想关断和桥臂整体等效假设可能不满足需求。

### 证据边界

- 来自原文摘要的确定证据：模型包含四个特征，即理想关断开关、后退欧拉电容离散、高效排序均压算法、带插值函数二极管闭锁仿真。
- 来自原文摘要的确定数字：PSCAD/EMTDC中相对fully detailed MMC model，暂稳态最大误差小于2‰，121电平详细模型对比加速比超过2770倍。
- 来自原文引言的背景证据：跨湾工程2592个子模块、舟山五端工程超过8000个子模块用于说明高电平MMC仿真需求；当前摘录不能证明这些工程规模均完成了同等详细模型对比。
- 线性复杂度结论来自原文摘要和方法目标；但当前摘录未展示完整运行时间随N变化的数据表或拟合过程，不能独立核验线性斜率。
- 闭锁仿真方法是原文提出内容；当前证据未给出闭锁工况的量化误差、具体故障场景和与传统二极管模型的对比结果。
- 当前摘录只显示方法开端和摘要，引言提到将结合全桥型MMC推导，但未提供完整推导、参数表和算例图，因此拓扑适用性仍应以原文后续章节为准。
<!-- deep-review:end -->
## 核心贡献

- 问题定位：针对模块化多电平换流器(modular multilevel converter，MMC)在电平数很高时电磁暂态离线仿真效率低下的问题，提出一种基于戴维南等效的 MMC 电磁暂态整体建模方法。具体而言，在建立 MMC 戴维南等效模型的过程中：1）假设 MMC 中全部开关器件都具备理想的关断特性(也即关断电阻无穷大)；
- 方法机制：提出基于戴维南等效的MMC电磁暂态整体建模方法，通过四个关键优化实现计算复杂度随电平数线性增长：1)采用理想开关假设（关断电阻无穷大）简化开关模型；2)使用绝对稳定的后退欧拉法(BEM)替代梯形积分法(TR)离散化子模块电容，消除历史状态依赖；3)设计计算复杂度与桥臂子模块数相同的高效排序均压算法；4)采用带插值函数的二极管实现精确闭锁仿真。该方法将N个子模块的桥臂等效为2节点戴维南电路，在保持精确等效的同时大幅降低计算量。
- 验证证据：与详细模型（fully detailed model）进行对比仿真验证，比较电容电压、桥臂电流等关键波形的稳态和暂态精度，并统计计算时间加速比；121电平MMC换流器系统，包含三相桥臂，每桥臂121个子模块；另包含跨湾工程（2592子模块）和舟山工程（8000+子模块）等实际工程规模的仿真测试；
- 量化与结论：所提整体模型的计算复杂度随子模块数量增加呈严格线性增长O(N)；电平MMC仿真加速比超过2770倍（相比详细模型）；后退欧拉法离散化消除了梯形积分法带来的数值振荡问题，具有A-稳定性（绝对稳定）；采用理想开关假设后，每个仿真步长只需计算一次电容电压增量公式，而非传统方法的N次独立计算
- 适用边界：适用于理解本文 模块化多电平换流器戴维南等效整体建模方法 （2015） 在当前页面抽取范围内讨论的 EMT/电力系统暂态问题。；适用于以 network-equivalent、numerical-integration、interpolation 为核心的建模、仿真、等值、控制或稳定性分析场景；具体对象以原文算例和页面“涉及的模型”为准。

## 使用的方法

- [[network-equivalent]]
- [[numerical-integration]]
- [[interpolation]]

## 涉及的模型

- [[mmc-model]]
- [[vsc-hvdc]]

## 相关主题

- [[models/mmc-model]]
- [[topics/vsc-hvdc]]
- [[network-equivalent]]

## 主要发现

- 所提整体模型计算复杂度随子模块数量增加呈线性增长
- 模型暂稳态最大精度误差在2‰以内，121电平仿真加速比超2770倍
- 后退欧拉法离散化有效提升了数值稳定性，插值函数实现了闭锁工况的精确模拟

## 方法细节

### 方法概述

提出基于戴维南等效的MMC电磁暂态整体建模方法，通过四个关键优化实现计算复杂度随电平数线性增长：1)采用理想开关假设（关断电阻无穷大）简化开关模型；2)使用绝对稳定的后退欧拉法(BEM)替代梯形积分法(TR)离散化子模块电容，消除历史状态依赖；3)设计计算复杂度与桥臂子模块数相同的高效排序均压算法；4)采用带插值函数的二极管实现精确闭锁仿真。该方法将N个子模块的桥臂等效为2节点戴维南电路，在保持精确等效的同时大幅降低计算量。

### 数学公式

**公式1**: $$$R_{CT} = \frac{\Delta T}{2C}$$$

*梯形积分法(TR)下子模块电容的戴维南等效电阻，ΔT为仿真步长，C为电容值*

**公式2**: $$$U_{CEQ}^T(t-\Delta T) = U_C^T(t-\Delta T) + R_{CT}I_C(t-\Delta T)$$$

*梯形积分法下子模块电容的戴维南等效历史电压源*

**公式3**: $$$R_{CE} = \frac{\Delta T}{C}$$$

*后退欧拉法(BEM)下子模块电容的戴维南等效电阻，为梯形积分法的2倍*

**公式4**: $$$U_{CEQ}^E(t-\Delta T) = U_{CE}(t-\Delta T)$$$

*后退欧拉法下子模块电容的等效历史电压源，仅等于上一时刻电容电压*

**公式5**: $$$I_C(t) = \begin{cases} I_{ARM}(t), & \text{投入} \\ 0, & \text{切除} \end{cases}$$$

*理想开关假设下子模块电容电流，投入时等于桥臂电流，切除时为0*

**公式6**: $$$R_{SMEQ}(t) = \begin{cases} R_{ON} + R_{CE}, & \text{投入} \\ R_{ON}, & \text{切除} \end{cases}$$$

*理想开关假设下子模块戴维南等效电阻，RON为导通电阻（通常0.01Ω）*

**公式7**: $$$U_{SMEQ}(t-\Delta T) = \begin{cases} U_{CEQ}(t-\Delta T), & \text{投入} \\ 0, & \text{切除} \end{cases}$$$

*理想开关假设下子模块戴维南等效电压源，切除状态精确为0*

**公式8**: $$$R_{ARMEQ}(t) = NR_{ON} + N_{ON}R_{CE}$$$

*整个桥臂的戴维南等效电阻，N为总子模块数，NON为当前投入子模块数*

**公式9**: $$$U_{ARMEQ}(t) = \sum_{k=1}^{N_{ON}} U_{SMEQ\_k}(t-\Delta T)$$$

*整个桥臂的戴维南等效电压，仅对投入状态的子模块电压求和*

**公式10**: $$$\Delta U_C^E(t) = I_{ARM}(t) \cdot R_{CE}$$$

*后退欧拉法下投入状态子模块的电容电压增量，与当前桥臂电流成正比*

### 算法步骤

1. 初始化：设置仿真步长ΔT，子模块电容C，计算后退欧拉法等效电阻RCE = ΔT/C，初始化各子模块电容电压UC(k)

2. 每个仿真步长开始时，从控制器获取桥臂电流IARM(t)和各子模块的开关指令（投入/切除状态）

3. 根据理想开关假设和后退欧拉法，计算电容电压增量：若子模块处于投入状态，则ΔUC = IARM(t)·RCE；若处于切除状态，则ΔUC = 0

4. 更新所有投入状态子模块的电容电压：UC(t) = UC(t-ΔT) + ΔUC(t)；切除状态子模块电压保持不变

5. 执行高效排序均压算法：根据当前各子模块电容电压值进行排序，结合调制策略（如最近电平逼近控制NLC）决定下一时刻哪些子模块投入或切除，以实现电容电压平衡

6. 计算桥臂整体戴维南等效参数：等效电阻RARMEQ = N·RON + NON·RCE，等效电压UARMEQ为所有投入子模块电容电压之和

7. 将桥臂等效电路（戴维南电压源串联等效电阻）与外部电磁暂态网络接口，求解网络方程得到新的桥臂电流

8. 检查闭锁信号：若接收到闭锁指令，切换至插值函数-based二极管模型，精确模拟子模块通过二极管续流的非线性特性

9. 进入下一仿真步长，重复步骤2-8

### 关键参数

- **RON**: 0.01 Ω（IGBT导通电阻典型值）

- **ROFF**: 10^6 Ω（理想关断假设下视为无穷大）

- **RCE**: ΔT/C，后退欧拉法离散化等效电阻

- **N**: 桥臂子模块总数（如121个）

- **NON**: 当前投入运行的子模块数（随调制变化）

- **仿真步长**: 20 μs（文中提及的典型值）

- **精度误差阈值**: < 2‰（0.2%）

## 仿真结果

### 仿真测试

| 测试场景 | 结果描述 | 对比基线 |

|---------|---------|----------|

| 121电平MMC-HVDC系统稳态运行 | 与详细模型对比，所提整体模型在稳态运行时的电容电压波形、桥臂电流波形与详细模型基本一致，最大稳态误差小于2‰ | 相比详细模型，计算速度提升2770倍以上 |

| MMC换流器闭锁工况 | 采用基于插值函数的二极管模型精确模拟闭锁过程，能够准确捕捉子模块通过反并联二极管续流的暂态特性，闭锁瞬间电压尖峰误差在允许范围内 | 传统模型在闭锁时可能产生数值振荡或精度损失，所提方法通过插值函数保持精度 |

| 高电平数MMC（多端直流电网场景） | 计算复杂度随子模块数量呈线性增长O(N)，而传统详细模型呈指数或高阶多项式增长。在2592子模块（如跨湾工程规模）或8000+子模块（如舟山工程）场景下仍保持可接受的计算速度 | 传统戴维南等效模型仅优化换流器部分，未优化均压算法，在高电平时效率急剧下降；所提整体模型将均压算法复杂度从O(N^2)或O(N log N)降低至O(N) |

## 量化发现

- 所提整体模型的计算复杂度随子模块数量增加呈严格线性增长O(N)
- 暂稳态最大精度误差小于2‰（0.2%）
- 121电平MMC仿真加速比超过2770倍（相比详细模型）
- 后退欧拉法离散化消除了梯形积分法带来的数值振荡问题，具有A-稳定性（绝对稳定）
- 采用理想开关假设后，每个仿真步长只需计算一次电容电压增量公式，而非传统方法的N次独立计算
- 高效排序均压算法使得电压平衡过程的计算量与桥臂子模块数N成正比，而非传统排序算法的N log N或更高
- 典型仿真步长20μs下，详细模型仿真5s需要数月，而所提模型可在数分钟至数小时内完成

## 关键公式

### 桥臂整体戴维南等效电阻

$$$R_{ARMEQ}(t) = NR_{ON} + N_{ON}R_{CE}$$$

*在每个仿真步长中，将N个子模块串联桥臂等效为单一电阻，其中NON为当前投入子模块数，RCE = ΔT/C为后退欧拉法离散化电阻*

### 后退欧拉法电容电压更新公式

$$$\Delta U_C^E(t) = I_{ARM}(t) \cdot \frac{\Delta T}{C}$$$

*基于理想开关假设和后退欧拉离散化，投入状态子模块的电容电压增量仅取决于当前桥臂电流和仿真步长，与历史状态无关，这是实现线性复杂度计算的关键*

### 桥臂戴维南等效电压源

$$$U_{ARMEQ}(t) = \sum_{k=1}^{N_{ON}} U_{C\_k}(t-\Delta T)$$$

*仅对处于投入状态的子模块电容电压求和，切除状态子模块贡献为0，这是理想开关假设下的简化结果*

## 验证详情

- **验证方式**: 与详细模型（fully detailed model）进行对比仿真验证，比较电容电压、桥臂电流等关键波形的稳态和暂态精度，并统计计算时间加速比
- **测试系统**: 121电平MMC换流器系统，包含三相桥臂，每桥臂121个子模块；另包含跨湾工程（2592子模块）和舟山工程（8000+子模块）等实际工程规模的仿真测试
- **仿真工具**: PSCAD/EMTDC电磁暂态仿真平台
- **验证结果**: 在PSCAD/EMTDC环境下，所提MMC整体模型与详细模型对比：1)波形吻合度高，暂稳态最大误差<2‰；2)计算速度提升显著，121电平系统加速比>2770倍；3)成功验证闭锁工况下的精确仿真能力；4)计算复杂度验证为O(N)线性增长，适用于超高电平数MMC及多端直流电网仿真

## 适用边界

### 适用条件

- 适用于理解本文 `模块化多电平换流器戴维南等效整体建模方法`（2015） 在当前页面抽取范围内讨论的 EMT/电力系统暂态问题。
- 适用于以 network-equivalent、numerical-integration、interpolation 为核心的建模、仿真、等值、控制或稳定性分析场景；具体对象以原文算例和页面“涉及的模型”为准。
- 可作为知识图谱中的方法定位和文献入口，尤其用于追踪：提出基于戴维南等效的MMC电磁暂态整体建模方法，计算复杂度随电平数线性增长

### 失效边界

- 不应外推到原文未覆盖的拓扑、控制策略、故障类型、频率范围、硬件平台或实时步长。
- 不应把页面中的“提高、显著、快速、准确”等概括性表述当作定量结论；只有“量化发现”和原文表图可核验的数字才可用于比较。
- 若页面作者、期刊、摘要或验证字段仍不完整，本页只能作为待复核文献入口，不能作为最终证据页引用。

### 关键假设

- 页面内容假设当前 PDF 抽取文本与 frontmatter 的 `sources` 指向同一篇论文。
- 方法结论默认受原文仿真工具、测试系统、参数设置、采样步长和对比基线约束。
- 当前边界层为保守整理：未从原文直接核验的内容不得升级为确定结论。

### 证据缺口

- 具体适用范围仍以原文算例、参数表和验证场景为准，当前页面不应外推到未验证系统。
- 源文件路径：`["EMT_Doc/33/许建中 等 - 2015 - 模块化多电平换流器戴维南等效整体建模方法.pdf"]`；需要深修时应优先核对该 PDF 的首页、摘要、方法和实验表图。
