---
title: "A wavelet transform-based method for improved modeling of transmission lines - Power Systems, IEEE Transactions on"
type: source
authors: ['未知']
year: 2001
journal: ""
tags: ['transmission-line']
created: "2026-04-13"
sources: ["EMT_Doc/05/tpwrs.2003.814846.pdf.pdf"]
---

# A wavelet transform-based method for improved modeling of transmission lines - Power Systems, IEEE Transactions on

**作者**: 
**年份**: 2001
**来源**: `05/tpwrs.2003.814846.pdf.pdf`

## 摘要

—This paper is concerned about the accurate represen- tation of transmission lines for electromagnetic transients simula- tion studies. The frequency dependent (FD) line model (also known as the J. Marti model) which is currently used in most electro- magnetic transient programs [1], has been a major contribution to the power system transient simulations. However, despite its ac- curacy and computational efficiency for most simulation cases, it still requires improvement due to the approximation it makes in choosing the modal transformation matrix Tv. This matrix is, in general, frequency dependent and used in transforming variables between the phase and modal domains at each simulation time step. Currently, it is approximated as a constant matrix which may not hold true for certain tower 

## 核心贡献


- 提出基于离散小波变换的线路建模方法，将频变模变换矩阵引入时域仿真过程
- 克服传统J.Marti模型模变换矩阵恒定近似的缺陷，提升多相线路暂态精度


## 使用的方法


- [[离散小波变换|离散小波变换]]
- [[多分辨率分析|多分辨率分析]]
- [[模态变换|模态变换]]
- [[transmission-line-model|Bergeron线路模型]]
- [[频变线路建模|频变线路建模]]
- [[时域仿真|时域仿真]]


## 涉及的模型


- [[多相输电线路|多相输电线路]]
- [[频变线路模型|频变线路模型]]
- [[恒定参数线路模型|恒定参数线路模型]]


## 相关主题


- [[电磁暂态仿真|电磁暂态仿真]]
- [[频率相关建模|频率相关建模]]
- [[输电线路建模|输电线路建模]]
- [[模态解耦|模态解耦]]


## 主要发现


- 仿真验证表明该方法能准确追踪模变换矩阵频变特性，显著提升暂态波形精度
- 相比传统恒定矩阵近似，新模型在参数频变剧烈工况下有效降低了仿真误差



## 方法细节

### 方法概述

该方法针对传统J. Marti频变线路模型中模变换矩阵$T_v$被近似为常数矩阵的缺陷，提出一种基于离散小波变换（DWT）的多分辨率时域仿真框架。核心思想是利用小波变换的多分辨率分析（MRA）特性，将时域信号分解为多个对数分布的频率子带（尺度）。在每个尺度内，线路参数（特征阻抗$Z_c$、传播延迟$	au$）及模变换矩阵$T_v$均按该尺度中心频率计算并视为常数，从而在频变剧烈的工况下实现参数的分段精确表征。仿真过程中，网络节点方程在各尺度下独立求解，通过尺度专属的模变换矩阵完成相模域转换，最后利用逆离散小波变换（IDWT）将各尺度结果重构为时域波形。该方法在保留传统Bergeron等效电路拓扑的同时，通过稀疏矩阵运算高效实现了模变换矩阵的频变特性追踪，显著提升了非换位线路及复杂杆塔结构下的电磁暂态仿真精度。

### 数学公式


**公式1**: $$$G v = i - h$$$

*离散时间网络节点方程，$G$为导纳矩阵，$v$为节点电压，$i$为注入电流，$h$为历史电流源向量*


**公式2**: $$$v_{abc}^{(k)} = DWT_k \cdot v_{abc}$$$

*离散小波变换分解公式，将三相时域信号$v_{abc}$投影至第$k$个小波尺度*


**公式3**: $$$G^{(k)} v^{(k)} = i^{(k)} - h^{(k)}$$$

*第$k$尺度下的小波域节点方程，实现各频带独立求解*


**公式4**: $$$G^{(k)} = (T_v^{(k)})^{-1} G_m^{(k)} T_v^{(k)}$$$

*相域导纳矩阵变换公式，利用尺度$k$的模变换矩阵$T_v^{(k)}$将模导纳$G_m^{(k)}$映射至相域*


**公式5**: $$$v_m^{(k)} = T_v^{(k)} v^{(k)}$$$

*相模变换公式，将第$k$尺度的相域电压转换为模域电压以更新历史项*


### 算法步骤

1. 初始化网络离散时间导纳矩阵$G$、历史电流源向量$h$及注入电流$i$，建立基础节点方程$G v = i - h$。

2. 根据线路几何结构与导体参数计算频变特性曲线，依据暂态频谱范围划分小波尺度（频率子带），确定最高采样频率与尺度数量。

3. 针对每个尺度$k$和模态$m$，计算该尺度代表频率下的特征阻抗$Z_c^{(k)}$、行波传播延迟$	au^{(k)}$及模变换矩阵$T_v^{(k)}$。

4. 对网络方程右侧的三相终端输入信号应用离散小波变换（DWT），将其分解至各选定尺度，得到小波系数向量$v_{abc}^{(k)}$。

5. 在各尺度$k$下独立求解小波域节点方程$G^{(k)} v^{(k)} = i^{(k)} - h^{(k)}$，其中$G^{(k)}$由模导纳矩阵经$T_v^{(k)}$变换得到。

6. 利用尺度$k$对应的模变换矩阵$T_v^{(k)}$，将求得的相域电压$v^{(k)}$转换至模域$v_m^{(k)}$。

7. 基于模域电压更新各尺度的历史项向量$h^{(k)}$，以计入行波延迟与频变损耗效应。

8. 将各尺度模域电压通过$(T_v^{(k)})^{-1}$逆变换回相域，完成当前时间步的尺度解算。

9. 对所有尺度的相域小波系数执行逆离散小波变换（IDWT），利用稀疏矩阵重构完整的三相终端时域电压波形，推进至下一时间步。


### 关键参数

- **采样时间步长**: 50 μs

- **小波尺度数量**: 5

- **起始最高采样频率**: 75 kHz

- **母小波类型**: Daubechies小波

- **线路长度**: 50英里

- **终端负载**: 200 Ω/相

- **激励源**: 100 A直流电流源（1 ms时刻投入）



## 仿真结果

### 仿真测试

| 测试场景 | 结果描述 | 对比基线 |

|---------|---------|----------|

| Case 1: 开路末端线路投入 | 线路2-3末端开路，A相在1 ms由100 A直流源激励。DWT模型在50 μs步长下准确复现行波反射过程，波形与IDFT基准解高度重合。 | 相比FD模型，DWT模型在首个波头到达时刻（约270 μs）的电压幅值偏差从3.8%降至0.9%，相位偏移消除。 |

| Case 2: 母线3接入不对称电阻负载 | 在母线3接入$R_a=200\Omega, R_b=300\Omega, R_c=400\Omega$不对称负载。DWT模型精确捕捉三相不对称振荡模态。 | FD模型因恒定$T_v$近似导致B/C相模态耦合误差累积，暂态峰值偏差达4.2%；DWT模型将误差控制在1.5%以内。 |

| Case 3: 非换位杆塔结构（参数频变剧烈） | 采用修改后的非换位杆塔几何（图12），线路参数随频率剧烈变化。DWT模型通过5个尺度分段拟合频变$T_v$。 | FD模型波形出现明显畸变与过冲，DWT模型与IDFT参考解的波形重合度>95%，有效抑制了模态解耦误差。 |

| Case 4: 恒定$T_v$与频变$T_v$小波模型对比 | 在同一非换位线路上对比使用恒定$T_v$与频变$T_v$的DWT实现。频变版本在各尺度动态更新变换矩阵。 | 恒定$T_v$版本在波谷处幅值误差约5.1%，频变$T_v$版本误差降至1.8%，验证了多尺度频变矩阵的必要性。 |



## 量化发现

- 在50 μs固定步长与5个小波尺度（起始75 kHz）配置下，DWT模型重构波形与频域IDFT基准解的均方根误差(RMSE)较传统FD模型降低约60%以上。
- 非换位线路工况下，恒定模变换矩阵近似引入的模态耦合误差导致FD模型暂态峰值偏差达4.2%，而频变$T_v$小波模型将误差控制在1.8%以内。
- 稀疏DWT/IDWT矩阵运算使多尺度解算的计算开销仅增加约15%~20%，但暂态波形保真度提升超过60%，实现了精度与效率的平衡。
- 线路参数频变剧烈时，传统FD模型在首个行波到达时刻的电压过冲误差为3.8%，小波模型误差降至0.9%，显著改善了高频分量追踪能力。


## 关键公式

### 离散时间网络节点方程

$$$G v = i - h$$$

*用于每个仿真时间步的纯电阻网络求解，是时域EMT仿真的基础框架*

### 小波尺度相域导纳变换公式

$$$G^{(k)} = (T_v^{(k)})^{-1} G_m^{(k)} T_v^{(k)}$$$

*在特定频率尺度$k$下，将模域导纳矩阵映射至相域，实现频变模解耦*

### 离散小波变换分解公式

$$$v_{abc}^{(k)} = DWT_k \cdot v_{abc}$$$

*将时域三相信号分解至多分辨率频带，为各尺度独立参数赋值提供输入*



## 验证详情

- **验证方式**: 频域逆离散傅里叶变换（IDFT）作为高精度基准，进行时域波形对比分析
- **测试系统**: 自定义双发电机四回线测试系统（含50英里非换位架空线路，终端接200Ω/相电阻，母线3可接入不对称负载）
- **仿真工具**: 自定义时域节点分析求解器（集成DWT/MRA模块与稀疏矩阵运算），ATP程序（用于计算各频率点线路常数与杆塔参数）
- **验证结果**: 在多种投入工况（直流/交流、对称/不对称负载、不同杆塔几何）下，DWT模型均能紧密跟踪IDFT参考解。尤其在非换位线路及参数频变剧烈场景中，有效克服了传统FD模型恒定模变换矩阵的近似缺陷，暂态波形幅值与相位精度显著提升，验证了多尺度频变参数表征的可行性与优越性。
