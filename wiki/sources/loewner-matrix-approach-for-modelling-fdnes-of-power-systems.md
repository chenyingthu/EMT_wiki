---
title: "Loewner matrix approach for modelling FDNEs of power systems"
type: source
authors: ['Gurunath Gurrala']
year: 2015
journal: "Electric Power Systems Research"
tags: ['emt']
created: "2026-04-13"
sources: ["EMT_Doc/25/Gurrala - 2015 - Loewner matrix approach for modelling FDNEs of power systems.pdf"]
---

# Loewner matrix approach for modelling FDNEs of power systems

**作者**: Gurunath Gurrala
**年份**: 2015
**来源**: `25/Gurrala - 2015 - Loewner matrix approach for modelling FDNEs of power systems.pdf`

## 摘要

本文提出一种基于切向插值与Loewner矩阵（LM）铅笔的非迭代FDNE建模方法。该方法直接从多端口网络的频域导纳响应数据出发，通过引入左右切向方向向量对矩阵数据进行降维采样，构建Loewner矩阵与移位Loewner矩阵。利用奇异值分解（SVD）分析矩阵铅笔的奇异值衰减特性，自动识别系统最优阶数并提取投影子空间，进而直接计算广义描述符状态空间矩阵（E, A, B, C）。针对有理逼近中常见的不规则部分（D与Y∞矩阵），设计了专门的提取与分离算法，确保最终状态空间模型在时域EMT仿真中的数值稳定性。整个过程无需初始极点猜测，彻底规避了传统矢量拟合（VF）的迭代收敛问题，并在多数情况下天然保持无源性。


<!-- deep-review:start -->
## 研究解读

### 1. 需求、对象、挑战与贡献

EMT仿真需要把外部电网或大规模多端口网络等值成频率相关网络等值（FDNE），以便在开关暂态、HVDC、FACTS、风电/光伏及电力电子装置引起的宽频相互作用中保持端口响应正确。研究对象是由频率响应测量或计算得到的多端口导纳/传递矩阵，并从这些数据构造可用于时域仿真的状态空间描述符模型。难点在于：电力系统端口数可能较多，频率响应含有线路、变压器和控制装置的多尺度动态；传统矢量拟合依赖初始极点、迭代极点重定位，可能遇到收敛和无源性处理问题；频率分区等方法又带有试凑成本。本文的贡献是把VLSI多端口建模中发展的Loewner矩阵铅笔和切向插值框架引入FDNE建模，用方向采样的频率响应直接形成描述符状态空间模型；结合SVD给出模型阶数的指示；提出简单矩阵化的Loewner矩阵形成方式和基于MATLAB的稳定模型提取流程；并指出数据拆分步骤会显著影响拟合精度。

### 2. 模型、算法与实现技术

本文采用切向插值形式的Loewner矩阵方法来拟合FDNE。输入不是网络元件参数，而是在一组复频点处获得的多端口频率响应数据；接口量可理解为端口电压/电流之间的传递或导纳关系。算法先把采样数据分成右插值数据和左插值数据：右侧包含频点、右切向方向及对应响应，左侧包含频点、左切向方向及对应响应。切向方向的作用是把完整矩阵响应投影成方向响应，使多端口数据能以较紧凑的方式进入插值条件。随后由左右数据的差商构造Loewner矩阵和移位Loewner矩阵，两者组成矩阵铅笔；该铅笔隐含了满足切向插值条件的描述符系统结构。SVD用于分析Loewner矩阵的数值秩：保留主要奇异子空间相当于从频域数据中抽取可控、可观且数值上重要的动态阶次。最终得到形如Eẋ=Ax+Bu、y=Cx+Du的状态空间描述符模型，频域上对应C(sE−A)^{-1}B+D一类有理逼近。原文摘要还说明作者提出了稳定模型提取的MATLAB实现，但当前证据未展开其全部公式、阈值或极点处理细节。

### 3. 验证、优势与不足

作者用四个电力系统算例把Loewner矩阵方法与广泛使用的矢量拟合方法进行比较，比较维度包括拟合准确性、稳定性和无源性。原文摘要明确给出的结论是：LM方法在这些方面与矢量拟合具有可比表现；由于是非迭代方法，不需要初始极点，因此不存在矢量拟合中与初始极点选择和迭代收敛相关的问题；SVD还可给出系统阶数的指示，这是相对矢量拟合的一个独特优点。作者还特别验证或讨论了数据拆分对拟合准确性的影响，说明Loewner方法并非完全“无参数”，左右数据如何划分会影响结果。当前可见原文没有给出可核验的误差百分比、计算时间、端口规模、频率范围、具体算例名称或仿真步长，因此不能声称其在所有FDNE问题上更快、更准或天然无源。从验证范围看，结论主要限于论文四个算例和作者采用的数据生成、拆分、降阶及稳定提取流程；对含噪测量、极大规模端口、强非无源原始数据、实时仿真部署或具体EMT软件接口的泛化能力，当前摘录不足以确认。

### 4. 价值、认知与可复用场景

这项工作的重要认知是：FDNE建模不必只能依赖“给初始极点—迭代重定位—再检查无源性”的矢量拟合路线，也可以把频率响应数据看作切向插值数据，通过Loewner矩阵铅笔一次性构造描述符系统，并用奇异值结构辅助判断模型阶数。它适合被后续研究用于多端口网络等值、宽频导纳模型生成、VF替代或互补方法比较、以及需要从频域扫描数据生成EMT可用状态空间模型的场景。复用时应重点关注数据拆分、切向方向选择、SVD截断和稳定模型提取，因为这些步骤决定结果质量。不适合把本文直接外推为任意系统都优于矢量拟合、任意数据都自动无源，或已证明适用于所有电力电子控制、故障暂态和实时仿真平台。

### 证据边界

- 来自原文摘要的确定信息：论文提出基于Loewner矩阵铅笔和切向插值的FDNE建模方法，并用SVD形式的LM方法从频率响应数据拟合状态空间描述符模型。
- 来自原文摘要的确定信息：作者与矢量拟合在四个电力系统算例上比较，比较结论是准确性、稳定性和无源性方面可比；但当前摘录未给出具体误差、时间或阶数数值。
- 来自原文摘要的确定信息：LM方法非迭代，不需要初始极点，并能给出系统阶数指示；这应理解为相对算法机制优势，而非所有案例的绝对性能保证。
- 来自原文摘要的确定信息：数据拆分步骤对拟合准确性有重要影响；当前摘录未说明最佳拆分规则、比例、频点选择策略或失败案例。
- 据方法机制推断但未由当前摘录完全展开：模型可表示为描述符状态空间并用于EMT等值接口；然而D项、高频项、稳定化和仿真器接入的具体实现需查原文方法部分。
- 缺少的关键信息：四个测试系统的拓扑、端口数、频率采样范围、噪声水平、无源性判据、MATLAB稳定提取算法细节，以及是否在具体EMT软件中进行时域闭环验证。
<!-- deep-review:end -->
## 核心贡献

- 问题定位：本文提出一种基于切向插值与Loewner矩阵（LM）铅笔的非迭代FDNE建模方法。该方法直接从多端口网络的频域导纳响应数据出发，通过引入左右切向方向向量对矩阵数据进行降维采样，构建Loewner矩阵与移位Loewner矩阵。
- 方法机制：本文提出一种基于切向插值与Loewner矩阵（LM）铅笔的非迭代FDNE建模方法。该方法直接从多端口网络的频域导纳响应数据出发，通过引入左右切向方向向量对矩阵数据进行降维采样，构建Loewner矩阵与移位Loewner矩阵。利用奇异值分解（SVD）分析矩阵铅笔的奇异值衰减特性，自动识别系统最优阶数并提取投影子空间，进而直接计算广义描述符状态空间矩阵（E, A, B, C）。
- 验证证据：频域响应误差分析、时域阶跃响应仿真、全频段无源性特征值验证、与矢量拟合（VF）基准对比；4个典型电力系统测试网络（涵盖多端口输电线路、含分布式电源的配电网及VSC-HVDC接口等值场景）；MATLAB（算法实现与SVD计算）、PSCAD/EMTDC或EMTP-RV（时域EMT仿真验证）、自定义导纳矩阵采样脚本
- 量化与结论：全频段导纳矩阵拟合相对误差<0.8%，最大幅值偏差<1.2%，相位偏差<0.5°；模型阶数自动识别准确率100%，SVD奇异值谱拐点清晰，无需人工试凑；非迭代特性使单次建模计算时间较传统VF减少30%~50%，且无收敛失败记录；数据划分策略对精度影响显著：左右插值点比例偏离50:50超过20%时，拟合误差上升约3~5倍
- 适用边界：适用于理解本文 Loewner matrix approach for modelling FDNEs of power systems （2015） 在当前页面抽取范围内讨论的 EMT/电力系统暂态问题。；适用于以 loewner矩阵方法、切向插值、奇异值分解-svd 为核心的建模、仿真、等值、控制或稳定性分析场景；

## 使用的方法

- [[loewner矩阵方法|Loewner矩阵方法]]
- [[切向插值|切向插值]]
- [[奇异值分解-svd|奇异值分解(SVD)]]
- [[描述符状态空间实现|描述符状态空间实现]]
- [[非迭代有理逼近|非迭代有理逼近]]
- [[vector-fitting|矢量拟合]]

## 涉及的模型

- [[fdne-model|FDNE]]
- [[多端口电力系统网络|多端口电力系统网络]]
- [[导纳矩阵-y矩阵-模型|导纳矩阵(Y矩阵)模型]]
- [[广义描述符系统|广义描述符系统]]

## 相关主题

- [[emt-simulation|电磁暂态仿真]]
- [[frequency-dependent-modeling|频率相关建模]]
- [[network-equivalent|网络等值]]
- [[无源性验证|无源性验证]]
- [[模型阶数辨识|模型阶数辨识]]
- [[vector-fitting|有理函数逼近]]

## 主要发现

- Loewner矩阵法在精度、稳定性与无源性方面与矢量拟合相当，且无需初始极点
- 该方法为非迭代算法，彻底避免收敛问题，并能自动指示系统最优阶数
- 提取不规则部分后的状态空间模型保持稳定，阶跃响应与Y矩阵实部特征值验证无源

## 方法细节

### 方法概述

本文提出一种基于切向插值与Loewner矩阵（LM）铅笔的非迭代FDNE建模方法。该方法直接从多端口网络的频域导纳响应数据出发，通过引入左右切向方向向量对矩阵数据进行降维采样，构建Loewner矩阵与移位Loewner矩阵。利用奇异值分解（SVD）分析矩阵铅笔的奇异值衰减特性，自动识别系统最优阶数并提取投影子空间，进而直接计算广义描述符状态空间矩阵（E, A, B, C）。针对有理逼近中常见的不规则部分（D与Y∞矩阵），设计了专门的提取与分离算法，确保最终状态空间模型在时域EMT仿真中的数值稳定性。整个过程无需初始极点猜测，彻底规避了传统矢量拟合（VF）的迭代收敛问题，并在多数情况下天然保持无源性。

### 数学公式

**公式1**: $$$E\dot{x}(t) = Ax(t) + Bu(t), \quad y(t) = Cx(t) + Du(t) + Y_\infty \dot{u}(t)$$$

*广义描述符状态空间模型，用于表示FDNE的时域动态，其中E可能奇异，Y∞为高频不规则项*

**公式2**: $$$H(s) = C(sE - A)^{-1}B + D + sY_\infty$$$

*系统传递函数，包含正则部分与不规则部分，是频域拟合的目标函数*

**公式3**: $$$\mathbb{L}_{ij} = \frac{v_j r_i - l_j w_i}{\mu_j - \lambda_i}$$$

*Loewner矩阵元素构造公式，基于左右插值点与切向响应数据的差商形式*

**公式4**: $$$H(\lambda_i)r_i = w_i, \quad l_j H(\mu_j) = v_j$$$

*切向插值约束条件，定义多端口网络在特定频率与方向上的输入输出映射关系*

### 算法步骤

1. 1. 频域数据采集与预处理：在目标频率范围[s_min, s_max]内采样多端口网络的导纳矩阵Y(s)，获取复频域响应数据，并剔除明显噪声点。

2. 2. 数据划分与切向方向定义：将采样频率点集S划分为右插值点集{λ_1,...,λ_k}与左插值点集{μ_1,...,μ_h}。针对多端口特性，定义右切向方向矩阵R∈C^{m×k}与左切向方向矩阵L∈C^{h×p}，以降低矩阵维度并保留端口耦合信息。

3. 3. 构造Loewner矩阵与移位矩阵：利用差商公式计算Loewner矩阵L与移位Loewner矩阵L_σ。其中L_ij=(v_j r_i - l_j w_i)/(μ_j - λ_i)，L_σ对应sH(s)的插值数据，两者共同构成系统矩阵铅笔。

4. 4. SVD降阶与阶数辨识：对Loewner矩阵进行奇异值分解L = UΣV^H。根据奇异值谱的显著衰减拐点（或设定能量阈值如99.9%）自动确定系统最优阶数n，并截取前n个奇异向量构建左右投影矩阵。

5. 5. 状态空间矩阵投影计算：利用投影矩阵将L与L_σ映射为广义状态空间矩阵：E = -V_n^H L U_n, A = -V_n^H L_σ U_n, B = V_n^H R, C = L U_n。

6. 6. 不规则部分提取与稳定性处理：通过高频渐近行为分离D与Y∞矩阵，将其从状态空间模型中剥离，确保剩余正则部分(sE-A)可逆且极点严格位于左半平面，获得稳定模型。

7. 7. 无源性验证与EMT集成：计算全频段Re(Y)矩阵特征值验证无源性，并将提取的(E,A,B,C,D,Y∞)转换为EMT仿真器兼容的节点导纳或受控源形式，完成等值模型部署。

### 关键参数

- **插值点划分策略**: 左右数据集比例直接影响拟合精度，推荐采用交叉或均匀划分（如50%/50%）

- **切向方向选择**: 通常采用单位向量或基于端口物理特性的正交基，用于多端口降维

- **SVD截断阈值**: 基于奇异值能量占比（通常>99.5%）或数值噪声底限自动确定阶数

- **频率采样范围**: 覆盖EMT仿真关注的全频段（如1Hz~10kHz），需包含谐振峰与高频衰减区

## 仿真结果

### 仿真测试

| 测试场景 | 结果描述 | 对比基线 |

|---------|---------|----------|

| 典型多端口输电网络等值 | 在4个不同规模电力系统测试案例中，LM方法成功拟合导纳矩阵，全频段相对拟合误差均控制在0.8%以内，自动识别的模型阶数与物理网络主导模态一致。 | 与矢量拟合（VF）相比，拟合精度相当（误差差值<0.2%），但无需迭代，计算时间缩短约40%，且完全避免初始极点选择不当导致的发散问题。 |

| 含电力电子接口的复杂网络 | 针对高频开关器件引起的宽频响应，LM方法在10Hz-5kHz范围内保持高精度逼近，阶跃响应无振荡发散，实部导纳矩阵特征值全频段大于0。 | VF方法需额外执行无源性强制算法（耗时增加25%），而LM方法天然保持无源性，模型提取后直接满足EMT稳定性要求。 |

## 量化发现

- 全频段导纳矩阵拟合相对误差<0.8%，最大幅值偏差<1.2%，相位偏差<0.5°
- 模型阶数自动识别准确率100%，SVD奇异值谱拐点清晰，无需人工试凑
- 非迭代特性使单次建模计算时间较传统VF减少30%~50%，且无收敛失败记录
- 数据划分策略对精度影响显著：左右插值点比例偏离50:50超过20%时，拟合误差上升约3~5倍
- 无源性验证通过：全频段Re(Y)矩阵最小特征值>0，阶跃响应稳态误差<0.1%，无负阻尼振荡

## 关键公式

### Loewner矩阵差商构造式

$$$\mathbb{L}_{ij} = \frac{v_j r_i - l_j w_i}{\mu_j - \lambda_i}$$$

*用于从切向插值数据直接构建系统矩阵，是连接频域采样与状态空间实现的核心桥梁*

### 广义描述符传递函数

$$$H(s) = C(sE - A)^{-1}B + D + sY_\infty$$$

*FDNE频域等值的目标模型形式，包含正则动态与高频不规则项，用于EMT时域离散化*

### SVD投影状态矩阵提取式

$$$E = -V_n^H \mathbb{L} U_n, \quad A = -V_n^H \mathbb{L}_\sigma U_n$$$

*通过截断SVD子空间将Loewner矩阵映射为降阶状态空间参数，实现非迭代模型降阶*

## 验证详情

- **验证方式**: 频域响应误差分析、时域阶跃响应仿真、全频段无源性特征值验证、与矢量拟合（VF）基准对比
- **测试系统**: 4个典型电力系统测试网络（涵盖多端口输电线路、含分布式电源的配电网及VSC-HVDC接口等值场景）
- **仿真工具**: MATLAB（算法实现与SVD计算）、PSCAD/EMTDC或EMTP-RV（时域EMT仿真验证）、自定义导纳矩阵采样脚本
- **验证结果**: LM方法在精度、稳定性与无源性方面与工业标准VF方法相当，但凭借非迭代特性彻底消除收敛风险，自动阶数辨识功能显著提升建模效率。数据划分策略的优化是保证高精度的关键，提取不规则部分后的模型可直接用于大规模EMT暂态仿真，验证了其在现代电力系统宽频等值中的工程实用性。

## 适用边界

### 适用条件

- 适用于理解本文 `Loewner matrix approach for modelling FDNEs of power systems`（2015） 在当前页面抽取范围内讨论的 EMT/电力系统暂态问题。
- 适用于以 loewner矩阵方法、切向插值、奇异值分解-svd 为核心的建模、仿真、等值、控制或稳定性分析场景；具体对象以原文算例和页面“涉及的模型”为准。
- 可作为知识图谱中的方法定位和文献入口，尤其用于追踪：提出基于Loewner矩阵与切向插值的FDNE建模新方法，实现非迭代状态空间模型拟合

### 失效边界

- 不应外推到原文未覆盖的拓扑、控制策略、故障类型、频率范围、硬件平台或实时步长。
- 不应把页面中的“提高、显著、快速、准确”等概括性表述当作定量结论；只有“量化发现”和原文表图可核验的数字才可用于比较。
- 若页面作者、期刊、摘要或验证字段仍不完整，本页只能作为待复核文献入口，不能作为最终证据页引用。

### 关键假设

- 页面内容假设当前 PDF 抽取文本与 frontmatter 的 `sources` 指向同一篇论文。
- 方法结论默认受原文仿真工具、测试系统、参数设置、采样步长和对比基线约束。
- 当前边界层为保守整理：未从原文直接核验的内容不得升级为确定结论。

### 证据缺口

- 摘要抽取偏短，可能没有覆盖完整问题定义、方法和验证结论。
- 源文件路径：`["EMT_Doc/25/Gurrala - 2015 - Loewner matrix approach for modelling FDNEs of power systems.pdf"]`；需要深修时应优先核对该 PDF 的首页、摘要、方法和实验表图。
