---
title: "Efficient steady state analysis of the grid using electromagnetic transient models"
type: source
authors: ['Aayushya Agarwal']
year: 2022
journal: "Electric Power Systems Research"
tags: ['emt']
created: "2026-04-13"
sources: ["EMT_Doc/15/Efficient steady state analysis of the grid using electromagnetic transient models_Agarwal和Pileggi_2022.pdf"]
---

# Efficient steady state analysis of the grid using electromagnetic transient models

**作者**: Aayushya Agarwal
**年份**: 2022
**来源**: `15/Efficient steady state analysis of the grid using electromagnetic transient models_Agarwal和Pileggi_2022.pdf`

## 摘要

提出时域稳态（TDSS）分析方法，受谐波平衡与波形松弛理论启发。该方法摒弃传统EMT的离散时间步长逐步积分，转而以完整周期波形为状态变量进行全局迭代。利用替代定理将电网严格解耦为线性输电网络与非线性设备子系统，彻底避免构建和求解大规模雅可比矩阵。线性网络采用数值积分伴随矩阵法与预计算LU分解进行高效前代回代求解；非线性设备在给定端口电压波形下独立并行仿真，并通过线性插值同步端口电流。通过动态检测电压过零点估算实际基频周期，逐周期推进波形直至相邻周期状态差满足容差，实现超线性收敛，在保留EMT全频带精度的同时突破大规模系统计算瓶颈。


<!-- deep-review:start -->
## 研究解读

### 1. 需求、对象、挑战与贡献

工程需求是：在含逆变器、频率相关控制、三相不平衡和谐波注入的现代电网中，获得与EMT动态模型一致的稳态，而不是只在额定单频下求一个潮流近似解。研究对象是由线性输电网络、变压器以及非线性时域设备组成的电网稳态响应，重点是14节点系统示例。难点在于：若直接用传统EMT求稳态，理论上要把暂态积分到足够长时间，刚性ODE、小步长和大量时间步会使计算代价很高；而传统潮流虽然快，却把复杂设备压缩为PQ/PV等单频模型，可能与真实时域稳态不一致。本文贡献不是改进某个潮流模型，而是提出Time-Domain Steady State（TDSS）：直接用传统暂态模型寻找周期稳态波形，把求解对象从单个时间点状态转为完整三相时间波形，从而保留EMT模型的一致性，同时避免单纯长时间积分到稳态。

### 2. 模型、算法与实现技术

TDSS的核心思想受谐波平衡启发，但在时域中工作：它不把系统只投影到若干频率分量，也不按传统EMT一路向前积分到稳态，而是迭代整个稳态波形。系统可被看成线性N端口网络与若干非线性设备的互联；接口量是设备端口电压波形和注入线性网络的端口电流波形。完整系统可抽象为C(x,t)ẋ=f(x,t)一类ODE；解耦后，线性网络在给定端口电流下求端口电压，非线性设备在给定端口电压下用其原有时域模型产生电流响应。算法机制上，一次迭代先把当前端口电压波形施加到各设备模型，得到端口电流波形；再把这些电流作为N端口网络激励，求得更新后的电压波形；然后比较相邻波形是否达到周期稳态一致。这样，非线性设备仍按EMT模型描述控制和开关等时域行为，线性网络则作为可重复求解的网络方程处理。关键作用在于把“大系统整体非线性长时间积分”转化为“端口波形之间的固定点迭代”。

### 3. 验证、优势与不足

作者用14-bus系统的若干场景介绍和验证TDSS，并把结果与传统power-flow所得稳态进行对比；摘要明确强调该方法用于展示power-flow稳态与使用EMT模型所得真实时域稳态之间的差异。验证对象包括三相稳态波形、非线性设备影响以及潮流单额定频率假设可能遗漏的动态成分。优势主要体现在两个层面：第一，模型一致性更强，因为TDSS直接复用暂态模型，能保留三相、控制和谐波等时域信息；第二，求解目标更贴近稳态本身，因为它迭代周期波形，而不是靠长时间暂态仿真等待衰减过程结束。需要注意的是，给定摘录没有提供可核验的运行时间、迭代次数、误差范数、频率偏移数值或表图数据，因此不能据此声称具体加速倍数或某一数值精度。验证范围从摘录看主要是14节点示例和与power-flow的概念性/算例性对照，尚不能外推到大规模实际电网、强开关电力电子系统、保护动作、故障暂态或实时仿真硬件上的性能。

### 4. 价值、认知与可复用场景

这项工作的关键认知是：现代电网的“稳态”不一定等同于额定频率下的潮流解，而可以被定义为与完整EMT模型一致的周期时域波形。它适合用于连接潮流分析与EMT分析之间的断层，尤其适合作为后续研究中稳态初始化、含逆变器设备的三相稳态评估、频率相关控制影响分析、谐波稳态分析和波形松弛类电网算法的入口文献。它不适合被直接外推为通用实时EMT加速器，也不应在缺少原文数值表图时引用为已证明具有某个固定加速倍数或规模复杂度优势。

### 证据边界

- 原文摘录明确给出的证据包括：论文题名、作者Aayushya Agarwal与Larry Pileggi、关键词、TDSS受谐波平衡启发、目标是用传统暂态模型高效求稳态、并以14-bus系统若干场景说明方法。
- 原文摘录明确指出power-flow基于单一额定频率，可能无法代表含非线性设备和控制的真实稳态；TDSS则迭代完整三相时域波形以捕捉稳态动态。
- 线性N端口网络与非线性设备端口波形迭代的描述来自页面内容和原文引言中对线性网络/非线性设备划分的表述；具体数值积分格式、LU预分解等实现细节需回原文方法部分核验。
- 当前给定原文摘录未报告可核验的数值结果，例如运行时间、迭代次数、误差、频率偏移或加速倍数；这些不应在未查表图前作为确定结论引用。
- 验证边界从摘录看主要限于14-bus系统和power-flow对比；尚缺少对大规模系统、多类型电力电子装置、故障后稳态、强不平衡工况和不同控制器参数的系统性证据。
- 方法有效性依赖稳态可表示为可收敛的周期波形；若系统存在准周期、混沌、持续开关事件、保护离散动作或无稳定周期稳态，TDSS固定点迭代的适用性需要另行验证。
<!-- deep-review:end -->
## 核心贡献

- 问题定位：提出时域稳态（TDSS）分析方法，受谐波平衡与波形松弛理论启发。该方法摒弃传统EMT的离散时间步长逐步积分，转而以完整周期波形为状态变量进行全局迭代。利用替代定理将电网严格解耦为线性输电网络与非线性设备子系统，彻底避免构建和求解大规模雅可比矩阵。线性网络采用数值积分伴随矩阵法与预计算LU分解进行高效前代回代求解；
- 方法机制：提出时域稳态（TDSS）分析方法，受谐波平衡与波形松弛理论启发。该方法摒弃传统EMT的离散时间步长逐步积分，转而以完整周期波形为状态变量进行全局迭代。利用替代定理将电网严格解耦为线性输电网络与非线性设备子系统，彻底避免构建和求解大规模雅可比矩阵。线性网络采用数值积分伴随矩阵法与预计算LU分解进行高效前代回代求解；非线性设备在给定端口电压波形下独立并行仿真，并通过线性插值同步端口电流。
- 验证证据：仿真对比验证（与传统EMT逐步积分、MatPower潮流计算进行基准对比）；修改版IEEE 14节点测试系统（低惯量配置，含光伏阵列）；MATLAB（TDSS核心算法与线性网络求解）、Simulink（非线性设备并行仿真与EMT基准）、MatPower（潮流计算基准）
- 量化与结论：低惯量系统实际稳态基频为58Hz，偏离额定60Hz达3.33%，传统潮流计算因单频假设无法捕捉此频偏。；传统EMT仿真需20000个时间步，每步执行牛顿-拉夫逊迭代；TDSS仅需10000次迭代（3个周期推进），且每次迭代仅执行矩阵乘法，无大规模非线性求解。；潮流计算运行时间为0.28秒，但基于单频平衡假设，其稳态电压幅值与相角与真实EMT/TDSS结果存在显著偏差。；
- 适用边界：适用于理解本文 Efficient steady state analysis of the grid using electromagnetic transient models （2022） 在当前页面抽取范围内讨论的 EMT/电力系统暂态问题。；

## 使用的方法

- 谐波平衡法
- 波形松弛法
- 替代定理
- [[parallel-computing|网络解耦]]
- 时域波形迭代
- 线性N端口建模

## 涉及的模型

- 线性输电网络
- 非线性源
- [[synchronous-machine-model|同步调相机]]
- 非线性负荷
- 发电机
- [[transformer-model|变压器]]
- [[transmission-line-model|输电线路]]
- 14节点测试系统

## 相关主题

- 稳态分析
- [[emt-simulation|电磁暂态仿真]]
- 潮流计算对比
- [[parallel-computing|网络解耦]]
- [[parallel-computing|并行计算]]
- [[时域建模|时域建模]]

## 主要发现

- 所提时域稳态方法比传统电磁暂态逐步积分收敛更快，有效突破大规模系统计算瓶颈
- 潮流计算因单频与平衡假设，其稳态结果与真实电磁暂态模型存在显著偏差
- 线性与非线性子系统解耦可直接支持并行计算，在保持模型精度的同时提升求解速度

## 方法细节

### 方法概述

提出时域稳态（TDSS）分析方法，受谐波平衡与波形松弛理论启发。该方法摒弃传统EMT的离散时间步长逐步积分，转而以完整周期波形为状态变量进行全局迭代。利用替代定理将电网严格解耦为线性输电网络与非线性设备子系统，彻底避免构建和求解大规模雅可比矩阵。线性网络采用数值积分伴随矩阵法与预计算LU分解进行高效前代回代求解；非线性设备在给定端口电压波形下独立并行仿真，并通过线性插值同步端口电流。通过动态检测电压过零点估算实际基频周期，逐周期推进波形直至相邻周期状态差满足容差，实现超线性收敛，在保留EMT全频带精度的同时突破大规模系统计算瓶颈。

### 数学公式

**公式1**: $$$C(x(t), t)\dot{x}(t) = f(x(t), t)$$$

*电网刚性常微分方程，描述包含非线性设备与线性网络的完整系统动态*

**公式2**: $$$C_{lin}\dot{x}_{lin} = Y_{lin} x_{lin} + J_{lin} + I_p$$$

*解耦后的线性输电网络ODE，$I_p$为替代定理引入的端口电流源*

**公式3**: $$$\dot{x}_{nlin} = f_{nlin}(x_{nlin}, V_p)$$$

*解耦后的非线性设备ODE，$V_p$为线性网络提供的端口电压激励*

**公式4**: $$$x_{lin}(t+\Delta t) = (I - k_1 \Delta t C_{lin}^{-1} Y_{lin})^{-1} [x_{lin}(t) + k_2 \Delta t C_{lin}^{-1} Y_{lin} x_{lin}(t) + \Delta t C_{lin}^{-1} (k_1 I_p(t+\Delta t) + k_2 I_p(t))]$$$

*基于梯形积分的线性网络状态更新公式，利用常数伴随矩阵避免重复矩阵求逆*

### 算法步骤

1. 初始化完整周期波形向量 $x^0$，设定最小仿真时间步长 $\Delta t_{min}$，并确定初始时间窗 $[t_0, t_f]$。

2. 进入主迭代循环：提取当前线性网络端口电压波形 $V_p^k$，将其作为激励源分别施加于各解耦的非线性设备子模型。

3. 并行执行各非线性设备仿真：若某端口电压波形较上一迭代未发生变化，则直接复用历史端口电流波形 $I_p^{k-1}$ 以节省计算；否则调用EMT求解器独立计算该设备响应，输出新端口电流波形 $I_p^k$。

4. 将同步后的端口电流波形 $I_p^k$ 作为已知电流源注入线性输电网络，利用预计算的LU分解与常数伴随矩阵，以 $\Delta t_{min}$ 为步长递推求解线性ODE，得到新时间窗内的端口电压波形 $V_p^{k+1}$。

5. 通过线性插值检测电压波形过零点，计算相邻过零点时间差以动态估算当前系统实际基频周期 $T$，并将仿真时间窗向前推进一个周期 $[t_f, t_f+T]$。

6. 执行收敛性检查：若当前周期波形与上一周期波形在所有状态变量上的差异小于预设容差，则判定达到稳态并终止迭代；否则更新波形初始条件并返回步骤2继续推进。

### 关键参数

- **最小时间步长**: 5e-6 s

- **积分常数**: k1=k2=0.5 (对应梯形积分法)

- **收敛判据**: 相邻基频周期内完整状态波形差值小于设定容差

- **初始波形来源**: 历史调度数据或已知近似稳态波形

## 仿真结果

### 仿真测试

| 测试场景 | 结果描述 | 对比基线 |

|---------|---------|----------|

| 修改版IEEE 14节点系统（低惯量配置，移除3/8号发电机，8号母线接入光伏阵列） | 系统实际稳态基频偏移至58Hz。TDSS在3个周期迭代内收敛，总迭代次数为10000次，全程仅执行矩阵乘法与前代回代。传统EMT需20000个时间步，每步均需执行牛顿-拉夫逊迭代。潮流计算耗时0.28s，4次迭代收敛，但强制假设60Hz额定频率与无限大平衡节点。 | TDSS避免了EMT中每步的雅可比矩阵构建与求逆，计算复杂度从O(n^1.8)降至线性求解；相比潮流计算，TDSS准确捕捉了58Hz低频偏移与多相谐波，稳态电压幅值与相角与真实EMT结果高度一致，彻底消除单频平衡假设带来的显著偏差。 |

## 量化发现

- 低惯量系统实际稳态基频为58Hz，偏离额定60Hz达3.33%，传统潮流计算因单频假设无法捕捉此频偏。
- 传统EMT仿真需20000个时间步，每步执行牛顿-拉夫逊迭代；TDSS仅需10000次迭代（3个周期推进），且每次迭代仅执行矩阵乘法，无大规模非线性求解。
- 潮流计算运行时间为0.28秒，但基于单频平衡假设，其稳态电压幅值与相角与真实EMT/TDSS结果存在显著偏差。
- 线性网络阻抗矩阵因π型线路对地电容与电感特性满足对角占优，确保波形松弛法在TDSS框架下实现超线性收敛。
- 非线性设备并行仿真结合端口电压不变跳过机制，使计算耗时与系统规模解耦，仅取决于最大设备响应时间。

## 关键公式

### 线性网络伴随矩阵状态更新方程

$$$x_{lin}(t+\Delta t_{min}) = (I - k_1 \Delta t_{min} C_{lin}^{-1} Y_{lin})^{-1} \left[ x_{lin}(t) + k_2 \Delta t_{min} C_{lin}^{-1} Y_{lin} x_{lin}(t) + \Delta t_{min} C_{lin}^{-1} (k_1 I_p(t+\Delta t_{min}) + k_2 I_p(t)) \right]$$$

*在已知端口电流波形且时间步长恒定时，用于高效求解线性输电网络状态，避免重复构建雅可比矩阵。*

### 时域稳态收敛判据

$$$x^k = x^{k-1} \equiv \tilde{x} \quad \forall t \in [t_i, t_i+T]$$$

*当相邻基频周期内的完整状态波形差异小于容差时，判定系统已达到电磁暂态稳态。*

## 验证详情

- **验证方式**: 仿真对比验证（与传统EMT逐步积分、MatPower潮流计算进行基准对比）
- **测试系统**: 修改版IEEE 14节点测试系统（低惯量配置，含光伏阵列）
- **仿真工具**: MATLAB（TDSS核心算法与线性网络求解）、Simulink（非线性设备并行仿真与EMT基准）、MatPower（潮流计算基准）
- **验证结果**: TDSS在3个周期内收敛至与EMT一致的稳态波形，准确反映58Hz基频偏移与多相谐波；计算过程无需牛顿-拉夫逊迭代，仅依赖预分解矩阵乘法，验证了其在保持EMT精度的同时大幅提升大规模系统稳态求解效率的可行性。

## 适用边界

### 适用条件

- 适用于理解本文 `Efficient steady state analysis of the grid using electromagnetic transient models`（2022） 在当前页面抽取范围内讨论的 EMT/电力系统暂态问题。
- 适用于以 谐波平衡法、波形松弛法、替代定理 为核心的建模、仿真、等值、控制或稳定性分析场景；具体对象以原文算例和页面“涉及的模型”为准。
- 可作为知识图谱中的方法定位和文献入口，尤其用于追踪：提出基于完整波形迭代的时域稳态分析方法，避免离散时间步长，实现快速收敛

### 失效边界

- 不应外推到原文未覆盖的拓扑、控制策略、故障类型、频率范围、硬件平台或实时步长。
- 不应把页面中的“提高、显著、快速、准确”等概括性表述当作定量结论；只有“量化发现”和原文表图可核验的数字才可用于比较。
- 若页面作者、期刊、摘要或验证字段仍不完整，本页只能作为待复核文献入口，不能作为最终证据页引用。

### 关键假设

- 页面内容假设当前 PDF 抽取文本与 frontmatter 的 `sources` 指向同一篇论文。
- 方法结论默认受原文仿真工具、测试系统、参数设置、采样步长和对比基线约束。
- 当前边界层为保守整理：未从原文直接核验的内容不得升级为确定结论。

### 证据缺口

- 具体适用范围仍以原文算例、参数表和验证场景为准，当前页面不应外推到未验证系统。
- 源文件路径：`["EMT_Doc/15/Efficient steady state analysis of the grid using electromagnetic transient models_Agarwal和Pileggi_2022.pdf"]`；需要深修时应优先核对该 PDF 的首页、摘要、方法和实验表图。
