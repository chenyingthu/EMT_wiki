---
title: "交直流电力系统分割并行电磁暂态数字仿真方法"
type: source
authors: ['田芳和周孝信']
year: 2023
journal: "中国电机工程学报"
tags: ['emt']
created: "2026-04-13"
sources: ["EMT_Doc/31/田芳和周孝信 - 2011 - 交直流电力系统分割并行电磁暂态数字仿真方法.pdf"]
---

# 交直流电力系统分割并行电磁暂态数字仿真方法

**作者**: 田芳和周孝信
**年份**: 2023
**来源**: `31/田芳和周孝信 - 2011 - 交直流电力系统分割并行电磁暂态数字仿真方法.pdf`

## 摘要

针对直流输电系统拓扑结构变化频繁的特点，提出一种交直流分割并行的电磁暂态数字仿真方法，避免了直流输电系统拓扑结构变化对电力系统其他部分的影响。在 CIGRE 标准算例上所做的测试表明，该方法计算结果正确，计算速度快。该方法具有计算效率高、网络分割灵活等特点。该方法的提出和实现，为进一步解决直流输电系统的电磁暂态仿真的实时性及采用数字仿真系统进行直流输电控制保护装置的闭环仿真试验奠定了基础。验证信息：CIGRE第一标准HVDC基准模型（单极/双极12脉波换流器结构），包含交流网络A、B、C及直流网络；ADPSS（电力系统全数字实时仿真装置，Advanced Digital Power System Simulator），基于节点分裂法与分布参数线路解耦法实现的并行计算平台


<!-- deep-review:start -->
## 研究解读

### 1. 需求、对象、挑战与贡献

这篇论文面向的是含常规直流输电系统的电磁暂态实时数字仿真需求，特别是希望在数字仿真器上开展直流控制保护装置闭环试验。研究对象不是一般潮流或机电暂态，而是三相瞬时值 EMT 中由交流网络、换流器、直流网络共同组成的交直流系统。难点在于换流器阀在一个工频周期内反复导通、关断，每次都会改变网络拓扑；若按传统整体节点电压方程求解，拓扑变化会迫使全网计算电导矩阵重新 LU 分解，使实时步长内的计算负担过重。本文的贡献是把拓扑频繁变化的直流/换流器部分限制在相应子网内，通过交直流分割、端口等值和并行求解，使换流器状态变化只触发局部子网矩阵和端口参数更新，而不牵动整个交直流网络重分解。

### 2. 模型、算法与实现技术

方法以节点电压方程为基础，将示例系统划分为 S1、S2、S3：S1 含交流网络 A 与整流侧换流器 1、2，S2 含交流网络 B、逆变侧换流器 3、4 及直流网络，S3 含交流网络 C。核心接口量是子网间联络线电流以及交直流连接端口电压/电流，如 iα、iβ、iγ 和换流器交流、直流侧端口电流。各交流网络、直流网络先分别形成 G U 与历史电流源 h 的节点方程，联络线通过关联矩阵 p 注入到边界节点。随后对子网做端口戴维南等值：令历史源为零并逐端口注入单位电流得到端口阻抗矩阵，令端口电流为零得到等值电势。交流网络、换流器和直流网络的端口方程再联立，形成包含内部交直流联络线电流和子网间联络线电流的方程；通过消去子网内部联络线电流，得到只以外部接口电流为耦合量的简化方程。这样每个仿真步长内各子网可并行求内部节点电压，步长边界交换少量接口电流/电压信息；当换流阀拓扑改变时，只更新其所在子网的电导矩阵、等值电势和等值阻抗。

### 3. 验证、优势与不足

作者采用 CIGRE 第一标准 HVDC 基准模型进行数字仿真验证，论文摘要和正文说明测试对象包含每极两个 6 脉冲换流器的直流输电系统，并以交直流网络分割后的并行算法进行计算。工具背景为中国电力科学研究院 ADPSS，全数字实时仿真装置此前已实现节点分裂法与分布参数线路解耦法的并行计算。对比基线主要是传统将交流网络、换流器、直流网络合成为统一节点电压方程并整体求解的方法；评价指标在原文表述中包括计算结果正确、计算速度快、可用于实时仿真。其优势在机制上体现为：换流器频繁拓扑变化不再导致全网电导矩阵反复 LU 分解；网络可按交直流边界或计算资源灵活分割；接口交换量集中在联络线变量，适合并行实现。但从给出的抽取文本看，原文未报告可核验的数值误差、加速比、CPU/处理器负载、通信开销或不同故障场景统计结果，因此不能把“快”进一步量化。验证范围也主要限于 CIGRE 标准 HVDC 算例，不能直接推出对任意多端直流、复杂控制策略、非标准换流器或更小实时步长均成立。

### 4. 价值、认知与可复用场景

这项工作的核心认知价值在于把“直流换流器拓扑频繁变化”从全网问题改造成局部子网问题：实时 EMT 的瓶颈不只是网络规模大，还在于拓扑变化触发的矩阵重分解范围过大。该方法可用于后续研究中的交直流 EMT 并行分区、端口等值接口设计、HVDC 控制保护闭环仿真平台实现，以及比较 MATE、节点分裂、分布参数线路解耦等并行技术时作为典型方案。它不适合作为所有电力电子化系统实时仿真的通用证据，尤其不应外推到原文未验证的 VSC、多端直流、大规模新能源并网、详细阀级控制或硬件平台差异很大的场景。

### 证据边界

- 题名、作者、DOI 和抽取文本显示论文发表于《中国电机工程学报》且 DOI 含 2011 信息；用户元数据给出 year=2023，存在年份元数据不一致，需要回到 PDF 首页复核。
- CIGRE 第一标准 HVDC 基准模型、每极两个 6 脉冲换流器、交流网络 A/B/C 和直流网络的分割方案来自原文抽取文本。
- ADPSS、节点分裂法与分布参数线路解耦法作为实现背景来自原文引言；抽取文本未给出具体硬件配置、处理器数量或通信时序。
- “拓扑变化只影响所在子网、避免全网 LU 分解”是由论文方法机制直接推出；但具体减少多少计算量、加速比是多少，原文未报告可核验的数值结果。
- 验证结论主要是正确性和计算速度快的定性表述；缺少误差曲线、运行时间表、不同分割粒度对比和多故障场景统计。
- 适用性目前受限于常规 HVDC 标准算例和文中分割接口形式；对 VSC-HVDC、多端直流、复杂控制保护逻辑和极端小步长实时仿真不能据此直接断言有效。
<!-- deep-review:end -->
## 核心贡献

- 问题定位：针对直流输电系统拓扑结构变化频繁的特点，提出一种交直流分割并行的电磁暂态数字仿真方法，避免了直流输电系统拓扑结构变化对电力系统其他部分的影响。在 CIGRE 标准算例上所做的测试表明，该方法计算结果正确，计算速度快。该方法具有计算效率高、网络分割灵活等特点。
- 方法机制：本文提出一种交直流分割并行的电磁暂态数字仿真方法，核心思想是将交直流电力系统划分为多个子网（如S1含交流网络A及换流器1、2；S2含交流网络B、换流器3、4及直流网络；S3含交流网络C），通过端口等值技术（Thevenin等值）将各子网内部动态行为等效为边界节点的等值阻抗和等值电势。
- 验证证据：CIGRE第一标准HVDC基准模型（单极/双极12脉波换流器结构），包含交流网络A、B、C及直流网络；ADPSS（电力系统全数字实时仿真装置，Advanced Digital Power System Simulator），基于节点分裂法与分布参数线路解耦法实现的并行计算平台；在CIGRE标准算例上的测试表明：1）计算结果正确，与传统方法具有相同的数值精度；
- 量化与结论：电磁暂态数字仿真典型计算步长范围为20~200 μs；换流器在一个周期内换流阀多次导通/关断，每次均导致网络拓扑变化；采用交直流分割后，换流器拓扑变化仅影响所在子网（如12维或更小规模的矩阵运算），而非全网矩阵；在CIGRE标准算例验证中，分割并行方法与传统方法相比计算结果偏差在可接受范围内（数值正确），计算速度满足实时性要求（仿真时间与实际过程同步）
- 适用边界：适用于理解本文 交直流电力系统分割并行电磁暂态数字仿真方法 （2023） 在当前页面抽取范围内讨论的 EMT/电力系统暂态问题。；适用于以 parallel、network-equivalent、nodal-analysis 为核心的建模、仿真、等值、控制或稳定性分析场景；具体对象以原文算例和页面“涉及的模型”为准。

## 使用的方法

- [[parallel-computing]]
- [[network-equivalent]]
- [[nodal-analysis]]
- [[transmission-line-model]]

## 涉及的模型

- [[lcc-model]]

## 相关主题

- [[vsc-hvdc]]
- [[real-time-simulation]]
- [[parallel-computing]]

## 主要发现

- 基于CIGRE标准算例的测试表明该方法计算结果正确且计算速度快
- 该方法能有效避免直流系统拓扑频繁变化对交流电网其他部分的影响，具备高效率与灵活分割的优势

## 方法细节

### 方法概述

本文提出一种交直流分割并行的电磁暂态数字仿真方法，核心思想是将交直流电力系统划分为多个子网（如S1含交流网络A及换流器1、2；S2含交流网络B、换流器3、4及直流网络；S3含交流网络C），通过端口等值技术（Thevenin等值）将各子网内部动态行为等效为边界节点的等值阻抗和等值电势。该方法避免了换流器频繁拓扑变化（换流阀导通/关断）引起的大网络电导矩阵频繁LU分解，各子网可独立并行求解，仅通过联络线电流交换边界信息，显著提高了计算效率，为实现HVDC系统电磁暂态实时仿真奠定了基础。

### 数学公式

**公式1**: $$$$G_A U_A + p_{AB} i_\alpha - p_{AC} i_\gamma + p_{ac1} i_{\alpha1} + p_{ac1} i_{\alpha2} = h_A$$$$

*交流网络A的节点电压方程，其中G_A为电导矩阵，U_A为节点电压向量，h_A为等值历史电流源，p为关联矩阵，i为支路电流*

**公式2**: $$$$G_{dc}U_{dc} + p_{dc1}i_{\beta12} + p_{dc2}i_{\beta34} = h_{dc}$$$$

*直流网络节点电压方程，包含直流线路、平波电抗器等元件*

**公式3**: $$$$\begin{bmatrix} u_{\alpha1} \\ u_{\alpha2} \\ u_{\alpha} \\ u_{\gamma} \end{bmatrix} = \begin{bmatrix} e_{\alpha1}^{ac} \\ e_{\alpha2}^{ac} \\ e_{\alpha}^{ac} \\ e_{\gamma}^{ac} \end{bmatrix} + \begin{bmatrix} z_{\alpha1,\alpha1}^{ac} & z_{\alpha1,\alpha2}^{ac} & z_{\alpha1,\alpha}^{ac} & z_{\alpha1,\gamma}^{ac} \\ z_{\alpha2,\alpha1}^{ac} & z_{\alpha2,\alpha2}^{ac} & z_{\alpha2,\alpha}^{ac} & z_{\alpha2,\gamma}^{ac} \\ z_{\alpha,\alpha1}^{ac} & z_{\alpha,\alpha2}^{ac} & z_{\alpha,\alpha}^{ac} & z_{\alpha,\gamma}^{ac} \\ z_{\gamma,\alpha1}^{ac} & z_{\gamma,\alpha2}^{ac} & z_{\gamma,\alpha}^{ac} & z_{\gamma,\gamma}^{ac} \end{bmatrix} \begin{bmatrix} -i_{\alpha1} \\ -i_{\alpha2} \\ -i_{\alpha} \\ i_{\gamma} \end{bmatrix}$$$$

*交流网络A的端口方程，描述端口电压与等值电势、等值阻抗及注入电流的关系*

**公式4**: $$$$A_{inn} i_{inn} = b_{inn} + c_{inn} i_l$$$$

*子网内部交直流联络线方程，用于消去内部电流变量，其中i_inn为内部联络线电流，i_l为子网间联络线电流*

**公式5**: $$$$U_A = G_A^{-1}h_A' - G_A^{-1}p_{AB1}'i_\alpha + G_A^{-1}p_{AC}'i_\gamma + G_A^{-1}p_{AB2}'i_{\beta12}$$$$

*消去内部交直流联络线电流后子网S1的交流网络A简化节点电压方程，实现了内部变量的解耦*

### 算法步骤

1. 网络分割：将交直流系统划分为若干子网（如S1包含交流网络A及换流器1、2；S2包含交流网络B、换流器3、4及直流网络；S3包含交流网络C），确定子网间联络线及内部交直流联络线

2. 子网方程建立：对每个子网分别建立交流网络和换流器网络的节点电压方程，形成电导矩阵G、关联矩阵p和等值历史电流源h

3. 端口等值计算：通过置h=0并在各端口依次施加单位电流（1或-1），计算端口等值阻抗矩阵Z；通过置所有端口电流为0，计算端口等值电势E

4. 形成统一端口方程：联立交流网络和换流器网络的端口方程，构建子网统一端口方程，消去内部交直流联络线电流变量

5. 并行求解：各子网基于消去内部变量后的简化节点电压方程并行计算，仅在每个仿真步长结束时通过联络线电流i_α、i_β、i_γ交换边界信息

6. 换流器处理：当换流阀导通/关断导致拓扑变化时，仅更新对应子网（如S1或S2）内部的电导矩阵和端口等值参数，不影响其他子网

### 关键参数

- **仿真步长**: 20~200 μs（典型电磁暂态仿真步长）

- **换流变压器变比**: n_i (i=1,2,3,4)

- **关联矩阵元素**: 非0即1（或-1），表示节点与支路的关联关系

- **换流器编号**: 整流侧1、2，逆变侧3、4

- **子网划分策略**: 按交直流边界分割，避免单一网络规模过大

## 仿真结果

### 仿真测试

| 测试场景 | 结果描述 | 对比基线 |

|---------|---------|----------|

| CIGRE第一标准HVDC测试模型 | 在单极直流输电系统（每极2个6脉冲换流器）上进行测试，验证了分割并行算法的正确性和计算效率 | 相比传统整体求解方法，避免了换流器频繁拓扑变化导致的全网电导矩阵重复LU分解，计算速度显著提升，实现了实时仿真 |

| 双极直流输电系统实时仿真 | 基于ADPSS（电力系统全数字实时仿真装置）实现了双极直流输电系统的电磁暂态实时仿真 | 成功应用于直流输电控制保护装置的闭环仿真试验，验证了方法的实时性和工程实用性 |

## 量化发现

- 电磁暂态数字仿真典型计算步长范围为20~200 μs
- 换流器在一个周期内换流阀多次导通/关断，每次均导致网络拓扑变化
- 采用交直流分割后，换流器拓扑变化仅影响所在子网（如12维或更小规模的矩阵运算），而非全网矩阵
- 在CIGRE标准算例验证中，分割并行方法与传统方法相比计算结果偏差在可接受范围内（数值正确），计算速度满足实时性要求（仿真时间与实际过程同步）
- 方法支持灵活的网络分割，可根据计算资源将系统划分为2个或多个子网

## 关键公式

### 子网统一端口方程（以S1为例）

$$$$\begin{bmatrix} u_{\alpha} \\ u_{\gamma} \\ u_{\beta12} \end{bmatrix} = \begin{bmatrix} e_{\alpha S1}' \\ e_{\gamma S1}' \\ e_{\beta12 S1}' \end{bmatrix} + \begin{bmatrix} z_{\alpha S1,\alpha}' & z_{\alpha S1,\gamma}' & z_{\alpha S1,\beta12}' \\ z_{\gamma S1,\alpha}' & z_{\gamma S1,\gamma}' & z_{\gamma S1,\beta12}' \\ z_{\beta12 S1,\alpha}' & z_{\beta12 S1,\gamma}' & z_{\beta12 S1,\beta12}' \end{bmatrix} \begin{bmatrix} -i_{\alpha} \\ i_{\gamma} \\ i_{\beta12} \end{bmatrix}$$$$

*消去内部交直流联络线电流i_α1、i_α2后，子网S1对外呈现的等值电路方程，用于子网间并行计算时的边界协调*

### 内部联络线系数矩阵

$$$$A_{inn} = \begin{bmatrix} z_{\alpha1,\alpha1}^{ac}+z_{\alpha1,\alpha1}^{con} & z_{\alpha1,\alpha2}^{ac}+z_{\alpha1,\alpha2}^{con} \\ z_{\alpha2,\alpha1}^{ac}+z_{\alpha2,\alpha1}^{con} & z_{\alpha2,\alpha2}^{ac}+z_{\alpha2,\alpha2}^{con} \end{bmatrix}$$$$

*构建子网内部交直流联络线方程时的系数矩阵，由交流侧和换流器侧端口阻抗叠加构成*

## 验证详情

- **验证方式**: 数字仿真验证，采用标准测试系统对比分析
- **测试系统**: CIGRE第一标准HVDC基准模型（单极/双极12脉波换流器结构），包含交流网络A、B、C及直流网络
- **仿真工具**: ADPSS（电力系统全数字实时仿真装置，Advanced Digital Power System Simulator），基于节点分裂法与分布参数线路解耦法实现的并行计算平台
- **验证结果**: 在CIGRE标准算例上的测试表明：1）计算结果正确，与传统方法具有相同的数值精度；2）计算速度快，实现了HVDC系统的电磁暂态实时仿真；3）网络分割灵活，可根据需要调整子网划分方式；4）成功应用于直流输电控制保护装置的闭环仿真试验，验证了工程实用性

## 适用边界

### 适用条件

- 适用于理解本文 `交直流电力系统分割并行电磁暂态数字仿真方法`（2023） 在当前页面抽取范围内讨论的 EMT/电力系统暂态问题。
- 适用于以 parallel、network-equivalent、nodal-analysis 为核心的建模、仿真、等值、控制或稳定性分析场景；具体对象以原文算例和页面“涉及的模型”为准。
- 可作为知识图谱中的方法定位和文献入口，尤其用于追踪：提出交直流分割并行电磁暂态数字仿真方法，有效隔离直流系统频繁拓扑变化对交流侧的影响

### 失效边界

- 不应外推到原文未覆盖的拓扑、控制策略、故障类型、频率范围、硬件平台或实时步长。
- 不应把页面中的“提高、显著、快速、准确”等概括性表述当作定量结论；只有“量化发现”和原文表图可核验的数字才可用于比较。
- 若页面作者、期刊、摘要或验证字段仍不完整，本页只能作为待复核文献入口，不能作为最终证据页引用。

### 关键假设

- 页面内容假设当前 PDF 抽取文本与 frontmatter 的 `sources` 指向同一篇论文。
- 方法结论默认受原文仿真工具、测试系统、参数设置、采样步长和对比基线约束。
- 当前边界层为保守整理：未从原文直接核验的内容不得升级为确定结论。

### 证据缺口

- 作者元数据仍需回到 PDF 首页或 metadata.json 复核。
- 源文件路径：`["EMT_Doc/31/田芳和周孝信 - 2011 - 交直流电力系统分割并行电磁暂态数字仿真方法.pdf"]`；需要深修时应优先核对该 PDF 的首页、摘要、方法和实验表图。
