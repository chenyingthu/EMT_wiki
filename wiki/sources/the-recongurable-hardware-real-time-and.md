---
title: "The Reconﬁgurable-Hardware Real-Time and"
type: source
authors: ['Electromagnetic transients', 'modeling', 'network equivalents', 'real-time simulation.']
year: 2012
journal: "IEEE Transactions on Power Delivery"
tags: ['real-time']
created: "2026-04-13"
sources: ["EMT_Doc/37/tpwrd.2012.2229723.pdf.pdf"]
---

# The Reconﬁgurable-Hardware Real-Time and

**作者**: Electromagnetic transients; modeling; network equivalents; real-time simulation.
**年份**: 2012
**来源**: `37/tpwrd.2012.2229723.pdf.pdf`

## 摘要

RH-RTS采用基于FPGA的可重构硬件架构，通过将电力系统模型分解为适合并行实现的独立方程组，利用数值积分将微分方程转换为代数方程，并采用节点分析法求解。核心思想是通过硬件重构（reconfigurability）动态改变底层硬件架构以适应不同电力系统拓扑，实现计算引擎与求解算法的深度匹配。该方法利用细粒度并行性（fine-grained parallelism）在原始操作级别实现并行计算，避免传统多处理器架构中的通信延迟和内存访问开销。


<!-- deep-review:start -->
## 研究解读

### 1. 需求、对象、挑战与贡献

实际需求是对故障、开关等引起的电磁暂态进行时域仿真，并在投运前对真实控制/保护装置做硬件在环闭环测试；同时，统计开关研究需要大量重复算例，要求仿真能实时甚至超实时运行。研究对象是面向大规模电力系统EMT的FPGA可重构硬件实时仿真器RH-RTS。难点在于EMT频率可从直流到数MHz，较宽频带要求更小步长和更精细模型，而传统多处理器/集群实时仿真器受计算时间、通信延迟和模型复杂度约束，典型步长在10–80微秒量级，频带约到10 kHz。本文贡献不是单纯更快，而是把仿真器底层硬件结构按待仿真电网数学模型定制和重构，利用FPGA大规模并行硬件把求解过程映射到专用计算结构，从而在实际规模系统上报告低至24 ns的每步计算时间，并同时支持实时HIL和超实时统计研究。

### 2. 模型、算法与实现技术

本文的核心实现是RH-RTS：一种基于FPGA的实时仿真平台，其“模型—硬件”关系不是用通用CPU顺序执行，而是根据目标电力系统拓扑和数学方程生成定制并行硬件结构。按页面整理的EMT求解流程，电力元件的微分方程先经数值积分离散化，形成伴随电路中的等效电导和历史电流源；网络方程再以节点分析形式组装为导纳矩阵方程，输入量包括外部电流源、历史项和元件参数，核心输出是每个时间步的节点电压及由此更新的支路电流/历史项。其机制意义是：离散化把连续动态转为每步代数更新，节点方程把网络耦合集中到线性求解，历史项递推保留电感、电容、线路等动态记忆；FPGA则把这些矩阵运算、元件历史项更新和可并行的算术操作固化为细粒度并行电路，减少通用多处理器中的通信和存储访问开销。需要注意，给定摘录主要说明RH-RTS的可重构硬件思想和性能，具体矩阵规模、定点/浮点格式、综合流程和资源占用未在摘录中展开。

### 3. 验证、优势与不足

作者声明通过案例研究验证和性能评估RH-RTS，在同一电力系统上展示实时运行和快于实时运行能力。测试对象被描述为realistic size systems，应用目标包括闭环HIL控制/保护测试和统计开关研究；工具为基于FPGA的可重构硬件仿真器，而不是传统多处理器或计算机集群架构。主要基线来自文献和引言中的现有实时仿真器能力：传统生产级实时仿真器受最小步长、频率带宽和模型精度折中限制，典型步长10–80微秒，频带约到10 kHz；多速率方案通常只对含电力电子的小部分系统采用百纳秒到数微秒步长。本文报告的关键指标是每仿真步计算时间最低24 ns，并称为当时文献中最低。优势体现在可用统一的纳秒级计算步支持更宽EMT频带、更高实时裕度，以及既可实时HIL也可超实时批量研究。从验证范围看，摘录未给出完整测试系统拓扑、节点数、元件模型清单、误差曲线、与离线EMT程序的波形对比或FPGA资源消耗，因此不能仅凭该页断言所有大电网、所有控制器接口或所有高频模型都能达到24 ns。

### 4. 价值、认知与可复用场景

这项工作的重要认知是：EMT实时仿真的瓶颈不只在算法，也在计算架构与电网方程结构是否匹配。RH-RTS把“为模型定制硬件”作为实时仿真路线，使宽频带EMT、保护控制HIL和大量开关统计研究有可能摆脱微秒级步长约束。它适合作为后续研究中FPGA-EMT、可重构仿真器、节点方程硬件映射、超实时暂态扫描和硬件在环测试平台的入口文献。它不适合被外推为任意系统都可24 ns运行，也不应替代对具体模型精度、接口延迟、数值稳定性和硬件资源约束的逐案验证。

### 证据边界

- 原文摘录明确给出：RH-RTS是基于FPGA的可重构硬件实时仿真器，核心思想是改变底层硬件架构以适应不同电力系统拓扑。
- 原文摘录明确给出：RH-RTS面向大规模/实际规模电力系统，报告每仿真步计算时间低至24 ns，并支持实时和快于实时运行。
- 原文摘录明确给出：既有实时仿真器典型步长为10–80微秒，频率带宽约到10 kHz；多速率方案只在小部分系统使用百纳秒到数微秒步长。
- 节点分析、数值积分、历史项递推和伴随电路流程来自当前页面整理及EMT常规求解框架；在所给原文摘录中尚未看到对应公式和实现细节，应回查正文方法部分核验。
- 摘录未提供测试系统的具体拓扑、规模、元件参数、FPGA型号/资源占用、数值格式、I/O延迟和波形误差指标，因此24 ns不能无条件推广到其他算例。
- 摘录中的作者元数据和标题存在抽取错误：作者字段被索引词替代，标题也被截断；正式引用前必须回到PDF首页或IEEE记录核对。
<!-- deep-review:end -->
## 核心贡献

- 问题定位：RH-RTS采用基于FPGA的可重构硬件架构，通过将电力系统模型分解为适合并行实现的独立方程组，利用数值积分将微分方程转换为代数方程，并采用节点分析法求解。核心思想是通过硬件重构（reconfigurability）动态改变底层硬件架构以适应不同电力系统拓扑，实现计算引擎与求解算法的深度匹配。
- 方法机制：RH-RTS采用基于FPGA的可重构硬件架构，通过将电力系统模型分解为适合并行实现的独立方程组，利用数值积分将微分方程转换为代数方程，并采用节点分析法求解。核心思想是通过硬件重构（reconfigurability）动态改变底层硬件架构以适应不同电力系统拓扑，实现计算引擎与求解算法的深度匹配。
- 验证证据：案例研究验证（Case Study）与性能评估，通过与现有技术文献对比验证计算速度，通过实时和超实时运行验证功能；realistic size systems（具有实际规模的电力系统），包含输电线路、电缆及多种电力系统元件，系统被划分为研究区和外部区（study zone and external zone）；
- 量化与结论：单步计算时间（computation-time per simulation time-step）低至24纳秒，为技术文献报道的最低水平；传统实时仿真器典型时间步长范围为10-80微秒（μs），而RH-RTS可达数十纳秒（ns）级别，时间步长缩小约1000倍；传统实时仿真器频率带宽限制在约10 kHz以下，RH-RTS可支持DC至数MHz的电磁暂态频率范围；
- 适用边界：适用于理解本文 The Reconﬁgurable-Hardware Real-Time and （2012） 在当前页面抽取范围内讨论的 EMT/电力系统暂态问题。；适用于以 parallel、numerical-integration、nodal-analysis 为核心的建模、仿真、等值、控制或稳定性分析场景；

## 使用的方法

- [[parallel-computing]]
- [[numerical-integration]]
- [[nodal-analysis]]

## 涉及的模型

- [[network-equivalent]]

## 相关主题

- [[real-time-simulation]]
- [[co-simulation]]
- [[harmonic-analysis-methods]]

## 主要发现

- RH-RTS单步计算时间可低至24纳秒，为目前技术文献报道的最低水平
- 该仿真器有效突破了传统实时仿真器在最小时间步长、频率带宽和模型精度上的限制，可高效支持硬件在环测试与统计开关研究

## 方法细节

### 方法概述

RH-RTS采用基于FPGA的可重构硬件架构，通过将电力系统模型分解为适合并行实现的独立方程组，利用数值积分将微分方程转换为代数方程，并采用节点分析法求解。核心思想是通过硬件重构（reconfigurability）动态改变底层硬件架构以适应不同电力系统拓扑，实现计算引擎与求解算法的深度匹配。该方法利用细粒度并行性（fine-grained parallelism）在原始操作级别实现并行计算，避免传统多处理器架构中的通信延迟和内存访问开销。

### 数学公式

**公式1**: $$$\frac{dx}{dt} = f(x,t)$$$

*电力系统元件的一阶常微分方程(ODE)基本形式，描述电压电流动态关系*

**公式2**: $$$x(t) = k \cdot f(x,t) + h(t)$$$

*数值积分后的代数方程，将ODE转换为离散时间代数关系，其中k为常数，h(t)为历史项*

**公式3**: $$$h(t) = \alpha x(t-\Delta t) + \beta h(t-\Delta t)$$$

*历史项递推计算公式，α和β为由元件参数和时间步长确定的常数，用于伴随电路中的等效电流源更新*

**公式4**: $$$G \cdot v(t) = i(t) + h(t)$$$

*节点电压方程（Nodal Equation），G为节点导纳矩阵，v(t)为节点电压向量，i(t)为电流源向量，h(t)为历史电流源向量*

**公式5**: $$$A \cdot x = b$$$

*线性方程组标准形式，对应节点分析中的矩阵求解问题*

### 算法步骤

1. 系统分解与模型离散化：将电力系统分解为研究区和外部区，对研究区内各元件采用数值积分方法（如梯形法）将微分方程转换为伴随电路模型，生成等效电导和电流源

2. 构建节点导纳矩阵：基于所有元件的伴随电路表示，组装恒定不变的节点导纳矩阵G，该矩阵在固定时间步长仿真中保持常量

3. 并行求解节点电压：在每个时间步，求解线性方程组 G·v(t) = i(t) + h(t) 计算当前时刻所有节点电压，利用FPGA的细粒度并行架构同时处理多个节点方程

4. 更新历史电流源：根据计算的节点电压值，使用公式 h(t) = α·x(t-Δt) + β·h(t-Δt) 更新各元件伴随电路中的历史项（等效电流源），该步骤必须在电压计算完成后顺序执行

5. 时间推进与迭代：将仿真时间推进一个步长Δt，重复步骤3-4直到仿真结束，支持实时（real-time）和超实时（faster-than-real-time）两种运行模式

### 关键参数

- **time_step**: 24 ns（最低计算时间）至数百纳秒范围

- **matrix_G**: 节点导纳矩阵（常量）

- **history_term_constants**: α和β，由元件参数和时间步长Δt确定

- **frequency_bandwidth**: 突破传统10 kHz限制，支持DC至数MHz范围

## 仿真结果

### 仿真测试

| 测试场景 | 结果描述 | 对比基线 |

|---------|---------|----------|

| 大规模电力系统实时仿真 | 在 realistic size systems（实际规模系统）上实现单步计算时间低至24纳秒，支持闭环硬件在环(HIL)测试配置 | 相比传统基于多处理器或计算机集群的实时仿真器（典型时间步长10-80 μs），计算速度提升约1000倍（从微秒级降至纳秒级） |

| 超实时统计开关研究 | 实现快于实时的仿真运行模式，可在短时间内完成大量开关场景(statistical switching studies)的仿真测试 | 相比传统实时仿真器，总运行时间显著缩短，支持大规模统计研究 |

## 量化发现

- 单步计算时间（computation-time per simulation time-step）低至24纳秒，为技术文献报道的最低水平
- 传统实时仿真器典型时间步长范围为10-80微秒（μs），而RH-RTS可达数十纳秒（ns）级别，时间步长缩小约1000倍
- 传统实时仿真器频率带宽限制在约10 kHz以下，RH-RTS可支持DC至数MHz的电磁暂态频率范围
- 现有基于多处理器架构的仿真器存在5-10微秒的通信延迟（communication latency），而RH-RTS通过定制硬件架构消除了此开销
- 采用多速率仿真时，传统方法对含电力电子变换器的小部分系统采用数百纳秒至数微秒步长，RH-RTS可在整个系统实现统一纳秒级步长

## 关键公式

### 节点电压方程（Nodal Analysis Equation）

$$$G \cdot v(t) = i(t) + h(t)$$$

*在每个仿真时间步求解系统节点电压的核心方程，其中G为恒定导纳矩阵，h(t)为基于历史值的等效电流源向量*

### 历史项递推公式（History Term Update）

$$$h(t) = \alpha x(t-\Delta t) + \beta h(t-\Delta t)$$$

*用于更新伴随电路中等效电流源的历史值，基于前一时间步的电压/电流值递归计算*

### 数值积分离散化方程

$$$x(t) = k \cdot f(x,t) + h(t)$$$

*将连续时间ODE通过数值积分（如梯形法）转换为离散代数形式，构成伴随电路的基础*

## 验证详情

- **验证方式**: 案例研究验证（Case Study）与性能评估，通过与现有技术文献对比验证计算速度，通过实时和超实时运行验证功能
- **测试系统**: realistic size systems（具有实际规模的电力系统），包含输电线路、电缆及多种电力系统元件，系统被划分为研究区和外部区（study zone and external zone）
- **仿真工具**: 基于Xilinx FPGA的可重构硬件平台（Field-Programmable Gate Array），采用定制并行硬件架构实现，未使用传统商业仿真软件如PSCAD/EMTDC或RTDS
- **验证结果**: 验证了24 ns的单步计算时间，确认RH-RTS能够突破传统实时仿真器在最小时间步长、频率带宽和模型精度方面的限制，成功支持硬件在环(HIL)测试和统计开关研究

## 适用边界

### 适用条件

- 适用于理解本文 `The Reconﬁgurable-Hardware Real-Time and`（2012） 在当前页面抽取范围内讨论的 EMT/电力系统暂态问题。
- 适用于以 parallel、numerical-integration、nodal-analysis 为核心的建模、仿真、等值、控制或稳定性分析场景；具体对象以原文算例和页面“涉及的模型”为准。
- 可作为知识图谱中的方法定位和文献入口，尤其用于追踪：提出基于FPGA的可重构硬件实时仿真器(RH-RTS)，实现底层硬件架构动态重构以适应不同电网拓扑

### 失效边界

- 不应外推到原文未覆盖的拓扑、控制策略、故障类型、频率范围、硬件平台或实时步长。
- 不应把页面中的“提高、显著、快速、准确”等概括性表述当作定量结论；只有“量化发现”和原文表图可核验的数字才可用于比较。
- 若页面作者、期刊、摘要或验证字段仍不完整，本页只能作为待复核文献入口，不能作为最终证据页引用。

### 关键假设

- 页面内容假设当前 PDF 抽取文本与 frontmatter 的 `sources` 指向同一篇论文。
- 方法结论默认受原文仿真工具、测试系统、参数设置、采样步长和对比基线约束。
- 当前边界层为保守整理：未从原文直接核验的内容不得升级为确定结论。

### 证据缺口

- 作者元数据仍需回到 PDF 首页或 metadata.json 复核。
- 源文件路径：`["EMT_Doc/37/tpwrd.2012.2229723.pdf.pdf"]`；需要深修时应优先核对该 PDF 的首页、摘要、方法和实验表图。
