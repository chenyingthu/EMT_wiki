---
title: "Parallel computation of power system EMTs through Polyphase-QMF filter banks"
type: source
authors: ['J.R. Zuluaga']
year: 2021
journal: "Electric Power Systems Research"
tags: ['emt']
created: "2026-04-13"
sources: ["EMT_Doc/30/j.epsr.2021.107317.pdf.pdf"]
---

# Parallel computation of power system EMTs through Polyphase-QMF filter banks

**作者**: J.R. Zuluaga
**年份**: 2021
**来源**: `30/j.epsr.2021.107317.pdf.pdf`

## 摘要

本文提出了一种基于拉普拉斯域长卷积和并行处理的电磁暂态仿真新方法。该方法首先在拉普拉斯域建立系统的节点导纳矩阵，利用Kron降阶法将网络矩阵缩减至仅包含开关节点和观测节点的低维形式。通过数值拉普拉斯逆变换(INLT)将降阶后的阻抗矩阵转换到时域，采用辅助电流源模拟开关操作（闭合/断开）引起的暂态事件。核心计算负担来自长卷积运算，通过多相正交镜像滤波器组(QMF)结构实现并行计算，从而支持超实时仿真。该方法避免了传统有理函数逼近导致的无源性破坏问题，适用于统计性绝缘协调研究等需要大量重复仿真的场景。


<!-- deep-review:start -->
## 研究解读

### 1. 需求、对象、挑战与贡献

工程需求来自电力系统EMT研究中大量重复仿真的场景，特别是绝缘配合等统计性暂态研究：同一网络、同类开关暂态需要在不同条件下反复计算，传统EMTP类时域逐步积分通常是顺序推进，计算负担高。本文研究对象不是任意控制系统仿真，而是可由开关状态变化触发的电力网络电磁暂态，关注节点包括发生状态变化的开关节点和需要观测的电压/电流节点。难点在于：全网节点导纳矩阵维度可能较高；暂态响应需要覆盖较宽频带和较长记忆；若用网络等值的有理函数近似，可能引入无源性破坏；若依赖线路延时做并行解耦，则并行粒度受网络结构限制。本文的贡献是把网络先在拉普拉斯域建模并用Kron降阶只保留事件和观测相关节点，再把每次开关事件等效为辅助源，其响应由降阶阻抗列向量的时域冲激响应与辅助信号做长卷积得到；最重的卷积环节用多相QMF滤波器组结构并行执行，并给出FPGA基本实现以说明硬件并行可行性。

### 2. 模型、算法与实现技术

方法从拉普拉斯域节点方程出发，将全网导纳矩阵按保留节点与消去节点分块，通过Kron公式得到降阶导纳矩阵，并求逆形成降阶阻抗矩阵。保留节点是算法的接口：一类对应开关状态变化，另一类对应观测量；外部注入源和开关辅助源是输入，节点电压暂态波形是输出。初始稳态由稳态阻抗矩阵与外部注入电流计算得到；若只有基波源，则在额定角频率处求值，若初始状态含谐波，则需在各谐波频率上叠加。开关闭合或断开不直接通过小步长拓扑重构逐点积分处理，而是用辅助源表示：闭合可等效为施加抵消开关端电压的源，断开可等效为注入抵消原支路电流的源。每次事件发生后，降阶矩阵按新状态更新并反演，取与该开关事件相关的阻抗矩阵列组合成状态向量Z(p)，再经数值逆拉普拉斯变换得到离散时域响应z(p)。事件贡献由z(p)与开关辅助信号做长卷积得到，多事件响应通过叠加递推累积。由于长卷积是主要计算瓶颈，作者采用多相QMF滤波器组的分析/综合结构把卷积计算拆成可并行的子运算，使算法更适合FPGA等并行硬件实现。

### 3. 验证、优势与不足

作者用一个17母线网络的暂态仿真作为算例验证方法。结果与PSCAD/EMTDC以及常规拉普拉斯变换方法进行对比，验证目标是电压暂态波形的一致性和并行实现的可行性；同时给出了FPGA上的基本实现，用于说明多相QMF滤波器组结构适合并行处理。优势主要体现在三个方面：第一，Kron降阶使显式计算只围绕开关节点和观测节点展开，避免每次事件都在全网维度上处理；第二，拉普拉斯域建模加数值逆变换避免了把外部网络拟合成有理函数时可能出现的无源性问题；第三，把主要负担集中为长卷积后，可用多相QMF结构做并行化，而不是依赖线路传播延时自然解耦，因此并行划分更受算法控制。需要注意的是，原文摘要和给定证据未报告可核验的误差数值、加速倍率、FPGA资源占用或最大可仿真时长，因此不能把“更快”解释成确定倍数。验证范围也只显示了17母线网络和开关暂态，未证明该方法适用于包含详细电力电子开关器件、复杂控制器、长时间机电-电磁混合过程或大规模实际电网的全场景仿真。

### 4. 价值、认知与可复用场景

这项工作的关键认知是：对于由开关事件驱动的EMT问题，可以把网络频域特性预先压缩到少量事件/观测端口，再把事件影响转化为端口冲激响应与辅助源的卷积；因此，计算并行化不必完全依赖网络物理分区，而可围绕信号处理结构组织。它适合被后续关于数值拉普拉斯变换EMT、端口降阶暂态等值、统计性开关暂态、FPGA并行卷积和超实时候选算法的页面复用。使用时应把它定位为面向开关操作暂态的加速仿真框架，而不是通用EMT求解器替代品；不应外推到原文未验证的保护控制闭环、电力电子详细模型、故障电弧模型或任意大规模系统性能结论。

### 证据边界

- 来自原文的证据：论文明确说明网络在拉普拉斯域综合、用Kron方法降阶，只保留观测节点和发生变化的节点，并用辅助信号模拟暂态事件。
- 来自原文的证据：论文明确说明卷积由基于多相QMF滤波器组的并行算法执行，并给出了FPGA基本实现以展示并行处理适用性。
- 来自原文的证据：验证对象为17母线网络，结果与PSCAD/EMTDC和常规Laplace Transform方法比较；但给定文本未提供可核验的误差、运行时间或资源占用数值。
- 据方法推断的边界：该框架最自然适用于可抽象为开关状态变化和端口响应叠加的暂态；对于详细半导体器件、强非线性控制闭环或连续拓扑变化过程，给定证据不足。
- 缺少的关键信息：数值逆拉普拉斯变换的采样参数、频率上限、阻尼常数选择、QMF滤波器阶数、并行通道数和FPGA平台规格在当前证据中未列出。
- 缺少的验证场景：当前证据未显示在更大规模网络、多类型故障、含分布式电源/电力电子控制、长时程仿真或统计批量仿真中的系统性评估。
<!-- deep-review:end -->
## 核心贡献

- 问题定位：本文提出了一种基于拉普拉斯域长卷积和并行处理的电磁暂态仿真新方法。该方法首先在拉普拉斯域建立系统的节点导纳矩阵，利用Kron降阶法将网络矩阵缩减至仅包含开关节点和观测节点的低维形式。通过数值拉普拉斯逆变换(INLT)将降阶后的阻抗矩阵转换到时域，采用辅助电流源模拟开关操作（闭合/断开）引起的暂态事件。
- 方法机制：本文提出了一种基于拉普拉斯域长卷积和并行处理的电磁暂态仿真新方法。该方法首先在拉普拉斯域建立系统的节点导纳矩阵，利用Kron降阶法将网络矩阵缩减至仅包含开关节点和观测节点的低维形式。通过数值拉普拉斯逆变换(INLT)将降阶后的阻抗矩阵转换到时域，采用辅助电流源模拟开关操作（闭合/断开）引起的暂态事件。核心计算负担来自长卷积运算，通过多相正交镜像滤波器组(QMF)结构实现并行计算，从而支持超实时仿真。
- 验证证据：对比验证（与商业仿真软件PSCAD/EMTDC及传统拉普拉斯变换方法对比）；17节点配电网络测试系统，包含多段线路、负载和开关设备，用于验证开关操作引起的电磁暂态现象；PSCAD/EMTDC（作为参考基准）、MATLAB（算法原型实现）、FPGA开发板（并行架构硬件验证）
- 量化与结论：计算复杂度降低：通过Kron降阶将原网络节点数缩减至（仅含开关和观测节点），矩阵求逆计算量从降至，其中（典型配电网中可达数千而仅数十）；仿真加速比：利用多相QMF滤波器组并行计算长卷积，实现百倍于实时的超实时仿真速度（>100x real-time），适用于需要数百次重复仿真的统计性暂态分析；
- 适用边界：适用于理解本文 Parallel computation of power system EMTs through Polyphase-QMF filter banks （2021） 在当前页面抽取范围内讨论的 EMT/电力系统暂态问题。；

## 使用的方法

- [[numerical-integration|数值拉普拉斯变换]]
- Kron网络降阶
- 长卷积计算
- 多相QMF滤波器组
- 并行处理算法
- 分段线性化建模
- [[nodal-analysis|节点分析法]]

## 涉及的模型

- 17节点测试电网
- [[ideal-switch-model|理想开关模型]]
- 辅助电压电流源
- 分段线性非线性元件

## 相关主题

- [[emt-simulation|电磁暂态仿真]]
- [[parallel-computing|并行计算]]
- [[real-time-simulation|超实时仿真]]
- 网络等值降阶
- [[fpga-real-time-simulation|FPGA硬件加速]]
- 统计性暂态分析

## 主要发现

- 17节点电网仿真结果与PSCAD及传统拉氏变换高度吻合，验证了算法精度
- 多相QMF并行卷积架构显著提升计算效率，实现百倍于实时的超快速暂态仿真
- FPGA实现验证了并行架构的硬件适用性，但该方法暂不适用于长时或详细电力电子仿真

## 方法细节

### 方法概述

本文提出了一种基于拉普拉斯域长卷积和并行处理的电磁暂态仿真新方法。该方法首先在拉普拉斯域建立系统的节点导纳矩阵，利用Kron降阶法将网络矩阵缩减至仅包含开关节点和观测节点的低维形式。通过数值拉普拉斯逆变换(INLT)将降阶后的阻抗矩阵转换到时域，采用辅助电流源模拟开关操作（闭合/断开）引起的暂态事件。核心计算负担来自长卷积运算，通过多相正交镜像滤波器组(QMF)结构实现并行计算，从而支持超实时仿真。该方法避免了传统有理函数逼近导致的无源性破坏问题，适用于统计性绝缘协调研究等需要大量重复仿真的场景。

### 数学公式

**公式1**: $$$I = Y_{bus} V$$$

*节点导纳方程，描述网络在拉普拉斯域的电压电流关系*

**公式2**: $$$Y_{red} = Y_{11} - Y_{12}Y_{22}^{-1}Y_{21}$$$

*Kron降阶公式，将高维节点导纳矩阵缩减为仅含关注节点的低维矩阵*

**公式3**: $$$Z_{red} = [Y_{red}]^{-1}$$$

*降阶阻抗矩阵，用于计算暂态响应*

**公式4**: $$$Z_{SS} = -Z_{red}Y_{12}Y_{22}^{-1}$$$

*稳态阻抗矩阵，用于计算初始稳态工作点*

**公式5**: $$$V_1 = V_{SS} + V_{TR}$$$

*总电压响应分解为稳态分量和暂态扰动分量*

**公式6**: $$$V_{SS} = Z_{SS}I_2$$$

*稳态电压响应计算，其中I2为外部注入电流*

**公式7**: $$$V_{TR} = Z_{red}I_1$$$

*暂态电压响应计算，由开关操作引起的电流注入产生*

**公式8**: $$$i_{close}(t) = -v_{k,l}(t)u(t-t_{close})/R_{sw}$$$

*开关闭合操作的诺顿等效电流源模型*

**公式9**: $$$i_{open}(t) = -i_{k,l}(t)u(t-t_{open})$$$

*开关断开操作的辅助电流源模型*

**公式10**: $$$V_1^{(p)} = Z_{red}^{(p)}I_1^{(p)} + V_1^{(p-1)}$$$

*第p次开关操作后的电压响应递推公式*

**公式11**: $$$v_1^{(p)} = z^{(p)} * i_{SW}^{(p)} + v_1^{(p-1)}$$$

*时域卷积形式的暂态响应计算，*表示卷积运算*

**公式12**: $$$N_t = \omega_{max} \times T_{max} / 2\pi$$$

*数值拉普拉斯变换所需的采样点数计算公式*

### 算法步骤

1. 网络矩阵构建与降阶：建立完整网络的节点导纳矩阵$Y_{bus}$，通过Kron降阶法将其分块为$Y_{11}$（含开关和观测节点）、$Y_{12}$、$Y_{21}$、$Y_{22}$，计算降阶矩阵$Y_{red} = Y_{11} - Y_{12}Y_{22}^{-1}Y_{21}$，并求逆得到$Z_{red}$

2. 稳态初始化：根据外部注入电流$I_2$和稳态阻抗矩阵$Z_{SS} = -Z_{red}Y_{12}Y_{22}^{-1}$，计算初始稳态电压$V_{SS} = Z_{SS}I_2$，此时开关状态对应的$I_1$为零向量

3. 开关事件建模：对于开关闭合操作，计算辅助电压源$v_{close}(t) = -v_{k,l}(t)u(t-t_{close})$并转换为诺顿等效电流源$i_{close}(t) = v_{close}(t)/R_{sw}$，其中$R_{sw}$取极小值（对应$G_{sw}=10^6$ S）；对于开关断开操作，直接注入抵消电流$i_{open}(t) = -i_{k,l}(t)u(t-t_{open})$

4. 拉普拉斯域响应计算：根据开关操作修改$Y_{red}$（最多修改4个元素）并重新求逆得到新的$Z_{red}^{(p)}$，提取与开关操作相关的列组合成$Z^{(p)}$表征网络对该操作的响应

5. 时域转换与预处理：利用逆数值拉普拉斯变换(INLT)将$Z^{(p)}$转换到时域得到$z^{(p)}$，设定最大频率$\omega_{max}$和仿真时长$T_{max}$，计算所需采样点数$N_t$

6. 并行卷积计算：采用多相QMF滤波器组结构，将长卷积$z^{(p)} * i_{SW}^{(p)}$分解为多个并行子滤波器运算，各子滤波器处理不同频段或时间窗的信号分量

7. 序列事件处理：对于多开关操作序列($p=1,2,...,P$)，通过递推公式$v_1^{(p)} = z^{(p)} * i_{SW}^{(p)} + v_1^{(p-1)}$逐步计算，利用叠加原理累积各事件的影响

8. 结果合成与输出：将各并行通道的计算结果合并，加上稳态分量$v_{SS}$，得到完整的暂态电压响应波形

### 关键参数

- **开关等效电导**: $G_{sw} = 10^6$ S（理想开关近似）

- **额定角频率**: $\omega_0 = 100\pi$ 或 $120\pi$ rad/s（对应50Hz或60Hz系统）

- **阻尼常数**: $c$（拉普拉斯域复变量$s = c + j\omega$的实部）

- **最大仿真频率**: $\omega_{max}$（决定暂态现象的最高频率分量）

- **最大仿真时间**: $T_{max}$（单次仿真的时间窗长度）

- **采样点数**: $N_t = \omega_{max} \times T_{max} / 2\pi$

- **降阶后节点数**: $N_1$（原网络$N$个节点经Kron降阶后保留的节点数，$N_1 \ll N$）

## 仿真结果

### 仿真测试

| 测试场景 | 结果描述 | 对比基线 |

|---------|---------|----------|

| 17节点测试电网开关操作暂态仿真 | 在17节点配电网络中进行开关操作仿真，验证所提方法在计算精度和速度方面的性能。测试包括单开关事件和多开关序列事件，观测节点电压的暂态恢复过程 | 与PSCAD/EMTDC仿真结果高度吻合，偏差小于传统数值积分方法；通过多相QMF并行架构实现计算速度数百倍于实时仿真（hundreds of times faster than real-time） |

| FPGA硬件实现验证 | 在FPGA平台上实现基于多相QMF滤波器组的并行卷积计算架构，验证硬件并行处理的可行性和资源利用率 | 证明了该算法在硬件并行架构上的适用性，但指出目前实现尚未优化至可处理长时程（>1s）或详细电力电子模型的水平 |

## 量化发现

- 计算复杂度降低：通过Kron降阶将原网络节点数$N$缩减至$N_1$（仅含开关和观测节点），矩阵求逆计算量从$O(N^3)$降至$O(N_1^3)$，其中$N_1 \ll N$（典型配电网中$N$可达数千而$N_1$仅数十）
- 仿真加速比：利用多相QMF滤波器组并行计算长卷积，实现百倍于实时的超实时仿真速度（>100x real-time），适用于需要数百次重复仿真的统计性暂态分析
- 开关建模精度：采用$G_{sw} = 10^6$ S模拟理想开关，引入的数值误差可控，与PSCAD/EMTDC对比结果高度一致
- 数值稳定性：避免了传统有理函数逼近（vector fitting）导致的无源性破坏（passivity violation）问题，保证了宽频带内的数值稳定性
- 算法局限性：当前实现不适用于长时程仿真（时间窗>$1$秒）和详细电力电子器件（如IGBT详细模型）的仿真
- 内存需求：长卷积计算需要存储历史数据，内存占用与$N_t = \omega_{max}T_{max}/2\pi$成正比，限制了可仿真的时间窗长度

## 关键公式

### Kron网络降阶公式

$$$Y_{red} = Y_{11} - Y_{12}Y_{22}^{-1}Y_{21}$$$

*将大规模网络导纳矩阵缩减为仅含关注节点（开关和观测点）的低维矩阵，是降低计算维度的核心步骤*

### 时域卷积递推公式

$$$v_1^{(p)} = z^{(p)} * i_{SW}^{(p)} + v_1^{(p-1)}$$$

*计算第p次开关操作后的暂态响应，通过卷积运算将网络 impulse response 与开关电流源波形结合，支持多事件序列的逐步计算*

### 稳态阻抗计算

$$$Z_{SS} = -Z_{red}Y_{12}Y_{22}^{-1}$$$

*用于计算初始稳态工作点，将外部电源$I_2$映射到关注节点的稳态电压，支持谐波稳态初始化（多频率点计算）*

## 验证详情

- **验证方式**: 对比验证（与商业仿真软件PSCAD/EMTDC及传统拉普拉斯变换方法对比）
- **测试系统**: 17节点配电网络测试系统，包含多段线路、负载和开关设备，用于验证开关操作引起的电磁暂态现象
- **仿真工具**: PSCAD/EMTDC（作为参考基准）、MATLAB（算法原型实现）、FPGA开发板（并行架构硬件验证）
- **验证结果**: 所提方法在17节点系统上的计算结果与PSCAD/EMTDC及传统数值拉普拉斯变换具有高度一致性，验证了算法的准确性；通过多相QMF并行结构实现了超实时仿真（数百倍实时速度），证明了该方法在统计性暂态分析（如绝缘协调研究）中的应用潜力；FPGA实现验证了并行计算架构的硬件可行性，但指出当前方法限于短时暂态（<1s）和宏观开关建模，不适用于详细电力电子仿真

## 适用边界

### 适用条件

- 适用于理解本文 `Parallel computation of power system EMTs through Polyphase-QMF filter banks`（2021） 在当前页面抽取范围内讨论的 EMT/电力系统暂态问题。
- 适用于以 数值拉普拉斯变换、kron网络降阶、长卷积计算 为核心的建模、仿真、等值、控制或稳定性分析场景；具体对象以原文算例和页面“涉及的模型”为准。
- 可作为知识图谱中的方法定位和文献入口，尤其用于追踪：提出拉普拉斯域长卷积仿真方法，避免有理逼近导致的无源性破坏问题

### 失效边界

- 不应外推到原文未覆盖的拓扑、控制策略、故障类型、频率范围、硬件平台或实时步长。
- 不应把页面中的“提高、显著、快速、准确”等概括性表述当作定量结论；只有“量化发现”和原文表图可核验的数字才可用于比较。
- 若页面作者、期刊、摘要或验证字段仍不完整，本页只能作为待复核文献入口，不能作为最终证据页引用。

### 关键假设

- 页面内容假设当前 PDF 抽取文本与 frontmatter 的 `sources` 指向同一篇论文。
- 方法结论默认受原文仿真工具、测试系统、参数设置、采样步长和对比基线约束。
- 当前边界层为保守整理：未从原文直接核验的内容不得升级为确定结论。

### 证据缺口

- 具体适用范围仍以原文算例、参数表和验证场景为准，当前页面不应外推到未验证系统。
- 源文件路径：`["EMT_Doc/30/j.epsr.2021.107317.pdf.pdf"]`；需要深修时应优先核对该 PDF 的首页、摘要、方法和实验表图。
