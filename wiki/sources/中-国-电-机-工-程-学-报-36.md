---
title: "中  国  电  机  工  程  学  报"
type: source
authors: ['陈鹏伟 等']
year: 2022
journal: "中国电机工程学报"
tags: ['emt']
created: "2026-04-13"
sources: ["EMT_Doc/36/陈鹏伟 等 - 2021 - 电力电子化电力系统随机电磁暂态仿真算法.pdf"]
---

# 中  国  电  机  工  程  学  报

**作者**: 陈鹏伟 等
**年份**: 2022
**来源**: `36/陈鹏伟 等 - 2021 - 电力电子化电力系统随机电磁暂态仿真算法.pdf`

## 摘要

为了准确高效地反映电力电子化电力系统在参数随机迁移扰动下的运行特性，该文首先分析了基于随机微分方程的电力系统随机动态模型和数值计算方法的基本特点和限制性，并从基本电气元件出发，建立了适用于电磁暂态仿真的参数随机迁移元件动态伴随电路模型。然后，以电阻–电感串联支路为例，解析推导了动态伴随电路模型的数值稳定性特征，进而针对电力电子化电力系统的仿真需求，设计了随机电磁暂态仿真的算法主流程。最后，采用 RLC 简单电路和三相两电平 VSC 电路，通过对比 EMTP(electromagnetic transients program)确定性仿真和蒙特卡罗多轨迹仿真结果，验证动态伴随电路模型和所提随机电磁暂态仿真算法的正确性与有效性，且对 EMTP 算法框架直接兼容。


<!-- deep-review:start -->
## 研究解读

### 1. 需求、对象、挑战与贡献

工程需求来自电力电子化电力系统的EMT分析：柔直、FACTS、微电网和新能源接入使微秒级开关暂态成为关键，同时元件参数会因设备状态、等效区域内部结构或运行条件变化而随机迁移。研究对象不是一般外部随机功率扰动，而是电阻、电感、电容等基本电气元件参数随机迁移对EMTP类电磁暂态仿真的影响。难点在于传统EMTP基于确定性ODE、伴随电路和节点导纳法，默认参数为常数；若只按给定时间序列修改阻抗，不能表达参数作为随机过程时引入的乘性激励，也难以保持电感、电容储能关系和随机能量注入的一致性。本文贡献是把参数随机迁移建模为SDE，并从基本元件推导可嵌入EMTP框架的随机动态伴随电路；再以电阻-电感串联支路分析数值稳定性，形成面向电力电子化系统的随机EMT主流程，而不是另起一套状态空间仿真器。

### 2. 模型、算法与实现技术

本文的核心模型是“参数随机迁移元件动态伴随电路”。参数如R、L、C不再是固定常数，而由SDE描述：漂移项α表示参数的确定性迁移趋势，扩散项βdW表示随机波动。数值上采用含Wiener增量的SDE离散格式，将参数更新与支路电压、电流差分方程耦合。例如电感支路中，电流更新不仅含由端电压差决定的确定性积分项，还含与上一时刻电流和L的随机增量相关的项，因此参数波动表现为乘性随机激励。伴随电路层面，随机迁移后的元件仍被转换为等效导纳加历史电流源/随机等效源，从而可继续组装节点导纳矩阵并求解节点电压。算法流程可理解为：给定拓扑、步长、初始参数和SDE函数；每步生成Wiener增量并更新参数；依据随机参数计算各元件等效导纳和历史源；求解EMTP节点方程；再回代支路电流、电容电压等状态量。关键接口量仍是EMTP熟悉的节点电压、支路电流、等效导纳和注入电流，因此方法强调与ATP/EMTP、PSCAD/EMTDC、RTDS这类EMTP框架的结构兼容性。

### 3. 验证、优势与不足

作者用两个层次验证：一是简单RLC电路，用于检查随机迁移R/L/C伴随电路是否能复现基本元件暂态；二是三相两电平VSC电路，用于考察在电力电子开关动作存在时，随机参数迁移模型是否仍可嵌入EMTP计算流程。对比基线包括EMTP确定性仿真和蒙特卡罗多轨迹仿真：前者用于观察随机模型在无随机或统计均值意义下是否与确定性结果一致，后者用于检验随机轨迹/统计特征是否合理。论文还以电阻-电感串联支路解析推导动态伴随电路的数值稳定性特征，说明所提离散伴随模型不仅是经验实现。优势主要体现在：把参数随机迁移作为SDE内生过程处理，而不是外部给定参数曲线；保持EMTP节点导纳求解框架；能同时表达电力电子开关暂态和元件参数随机变化的耦合。从验证范围看，原文未报告可核验的数值结果，至少在当前证据中没有给出误差百分比、耗时倍率或样本规模等定量指标；测试系统也限于RLC和两电平VSC，未覆盖大规模电网、复杂控制、故障穿越、实时仿真硬件闭环或不同随机过程辨识误差下的鲁棒性。

### 4. 价值、认知与可复用场景

这项工作的主要认知价值在于指出：电力电子化系统中的随机性不只来自外部功率或负荷波动，元件参数自身的随机迁移会通过电感、电容储能方程形成乘性激励，不能简单等同于给定噪声源或逐步改阻抗。它可用于后续构建随机EMT元件库、研究新能源/换流器系统参数不确定性传播、以及在EMTP类平台中加入SDE参数模块。适合被复用到以节点导纳法、伴随电路和随机参数建模为核心的页面或工程场景。不适合直接外推为任意拓扑、任意控制器、任意随机分布和实时仿真步长下均稳定高效；若缺少参数随机过程的物理辨识，模型输入本身仍可能成为主要误差来源。

### 证据边界

- 原文明确给出题名、作者、DOI、摘要、关键词，并说明方法面向参数随机迁移扰动下的电力电子化电力系统随机EMT仿真。
- 原文摘要明确验证对象为简单RLC电路和三相两电平VSC电路，基线为EMTP确定性仿真与蒙特卡罗多轨迹仿真；未在给定证据中提供可核验的误差、耗时或样本数量。
- 动态伴随电路、SDE建模、以电阻-电感串联支路分析数值稳定性属于原文明确表述；具体离散公式和系数若用于引用，应回到论文正文逐式核对。
- 当前证据只能支持“与EMTP算法框架直接兼容”的结构性结论，不能证明已在PSCAD/EMTDC、RTDS等商业/实时平台完成工程级实现。
- 原文讨论的随机迁移主要落在基本R、L、C元件及算例系统；对复杂保护、控制器、饱和磁性元件、宽频线路模型和大规模网络的适用性仍需额外验证。
- 页面元数据中年份与证据中的文章编号存在不一致：证据显示文章编号为2021年第11期，若作为正式文献页应以PDF首页和期刊出版信息复核。
<!-- deep-review:end -->
## 核心贡献

- 问题定位：为了准确高效地反映电力电子化电力系统在参数随机迁移扰动下的运行特性，该文首先分析了基于随机微分方程的电力系统随机动态模型和数值计算方法的基本特点和限制性，并从基本电气元件出发，建立了适用于电磁暂态仿真的参数随机迁移元件动态伴随电路模型。
- 方法机制：本文提出了一种基于随机微分方程(SDE)的电力电子化电力系统随机电磁暂态仿真算法。该方法在保留传统EMTP算法框架的基础上，通过建立考虑参数随机迁移的动态伴随电路模型，实现了对系统内部参数随机迁移（乘性激励）和外部随机扰动的统一仿真。核心创新在于将元件参数（R、L、C）的随机迁移过程建模为SDE，采用隐式Milstein格式（梯形或后向）进行离散化，构建相应的动态伴随电路模型，使其与EMTP的节点导纳法求解框架直接兼容。
- 验证证据：对比验证：与EMTP确定性仿真结果对比验证均值准确性；与蒙特卡罗多轨迹仿真结果对比验证随机动态轨迹的正确性；1) 简单RLC串联电路，用于验证基本元件模型；2) 三相两电平VSC（电压源换流器）电路，用于验证电力电子化系统的适用性；基于EMTP算法框架实现，兼容PSCAD/EMTDC、RTDS等电磁暂态仿真平台；采用SUNDIALS工具集进行算法对比测试
- 量化与结论：Milstein格式（包括隐式梯形和后向格式）具有1阶强收敛精度，显著优于Euler-Maruyama格式的1/2阶收敛性；隐式Milstein格式中的梯形和后向形式在数值稳定性方面表现优于显式Heun格式，特别是在处理乘性噪声时稳定性不受步长限制；所提动态伴随电路模型实现了与EMTP算法框架的直接兼容，无需修改节点导纳法的基本结构；
- 适用边界：适用于理解本文 中 国 电 机 工 程 学 报 （2022） 在当前页面抽取范围内讨论的 EMT/电力系统暂态问题。；适用于以 nodal-analysis、numerical-integration 为核心的建模、仿真、等值、控制或稳定性分析场景；具体对象以原文算例和页面“涉及的模型”为准。

## 使用的方法

- [[nodal-analysis]]
- [[numerical-integration]]

## 涉及的模型

- [[vsc-model]]

## 相关主题

- [[vsc-hvdc]]
- [[topics/real-time-simulation]]

## 主要发现

- 所提动态伴随电路模型与算法能准确高效反映参数随机迁移扰动下的系统运行特性
- 仿真结果与EMTP确定性计算及蒙特卡罗多轨迹结果高度一致，验证了算法的正确性与EMTP框架兼容性

## 方法细节

### 方法概述

本文提出了一种基于随机微分方程(SDE)的电力电子化电力系统随机电磁暂态仿真算法。该方法在保留传统EMTP算法框架的基础上，通过建立考虑参数随机迁移的动态伴随电路模型，实现了对系统内部参数随机迁移（乘性激励）和外部随机扰动的统一仿真。核心创新在于将元件参数（R、L、C）的随机迁移过程建模为SDE，采用隐式Milstein格式（梯形或后向）进行离散化，构建相应的动态伴随电路模型，使其与EMTP的节点导纳法求解框架直接兼容。该方法特别针对电力电子器件频繁开关导致的电磁暂态时间尺度特性，处理了系统刚性与随机性相互作用的问题。

### 数学公式

**公式1**: $$$d\eta(t) = \alpha(\eta, t) dt + \beta(\eta, t) dW(t), \quad \eta(t_0) = \eta_0$$$

*随机微分方程的一般矢量形式，描述系统状态的偏移过程与扩散过程，其中$W(t)$为$n_w$维独立标准Wiener过程*

**公式2**: $$$\eta_n = \eta_{n-1} + [(1-\theta)\alpha(\eta_{n-1}) + \theta\alpha(\tilde{\eta}_n)]\Delta t + D(\eta_{n-1})$$$

*隐式Milstein格式离散方程，当$\theta=0.5$时为梯形Milstein格式，$\theta=1$时为后向Milstein格式，$D(\cdot)$为扩散算子*

**公式3**: $$$D(\eta_{n-1}) = \beta(\eta_{n-1})\Delta W_{n-1} + \frac{1}{2}\beta(\eta_{n-1})\beta'(\eta_{n-1})(\Delta W_{n-1}^2 - \Delta t)$$$

*Milstein格式的扩散算子，包含Wiener过程的一阶和二阶项，确保1阶强收敛精度*

**公式4**: $$$i_{L,n} = \frac{\Delta t}{L_n + \alpha(\tilde{L}_n)\Delta t}(v_{i,n} - v_{j,n}) + K_{A,n}i_{L,n-1} - \frac{K_{A,n}i_{L,n-1}}{L_{n-1}}D(L_{n-1})$$$

*电感元件的后向Milstein格式动态伴随电路模型，$K_{A,n} = L_n/(L_n + \alpha(\tilde{L}_n)\Delta t)$，体现了电感参数随机迁移对电流的乘性激励效应*

**公式5**: $$$G_{L,n} = \Delta t/[2L_n + \alpha(\tilde{L}_n)\Delta t]$$$

*梯形Milstein格式下电感动态伴随电路的等效导纳，实现了与EMTP节点导纳矩阵的直接集成*

**公式6**: $$$i_{R,n} = \frac{1}{R_n}(v_{i,n} - v_{j,n}) = G_{R,n}(v_{i,n} - v_{j,n})$$$

*电阻元件的随机伴随电路模型，其中$R_n$通过Milstein格式离散更新：$R_n = R_{n-1} + \alpha(\tilde{R}_n)\Delta t + D(R_{n-1})$*

**公式7**: $$$dv_C(t) = \frac{i_C(t) - v_C(t)\alpha(C(t))}{C(t)}dt - \frac{v_C(t)\beta(C(t))}{C(t)}dW(t)$$$

*电容参数随机迁移下的电压微分方程，体现了电容值变化与电压的耦合关系及能量变化特性*

### 算法步骤

1. 初始化：设定仿真参数（步长$\Delta t$、总时长）、系统拓扑、元件初始参数$R_0, L_0, C_0$及SDE的偏移函数$\alpha(\cdot)$和扩散函数$\beta(\cdot)$

2. 节点导纳矩阵构建：根据当前时刻元件参数$R_n, L_n, C_n$，计算各元件动态伴随电路的等效导纳$G_{R,n}, G_{L,n}, G_{C,n}$，组装系统节点导纳矩阵$Y_n$

3. 历史电流源计算：基于前一时刻状态变量（电感电流$i_{L,n-1}$、电容电压$v_{C,n-1}$）和参数$L_{n-1}, C_{n-1}$，计算各元件的历史电流源$I_{hist,n-1}$和随机电流源$I_{rand,n-1}$（含$D(\cdot)$项）

4. 网络方程求解：求解线性方程组$Y_n V_n = I_{hist,n-1} + I_{rand,n-1}$，得到当前时刻节点电压$V_n$

5. 支路量更新：根据节点电压计算各支路电流$i_{R,n}, i_{L,n}, i_{C,n}$及电容电压$v_{C,n}$

6. 参数随机迁移更新：采用隐式Milstein格式（梯形或后向）更新元件参数：$L_n = L_{n-1} + [(1-\theta)\alpha(L_{n-1}) + \theta\alpha(\tilde{L}_n)]\Delta t + D(L_{n-1})$，其中预估点$\tilde{L}_n$由Euler-Maruyama格式计算

7. 时间推进：$n \leftarrow n+1$，判断仿真是否结束，否则返回步骤2

### 关键参数

- **$\Delta t$**: 仿真计算步长，需满足数值稳定性条件

- **$\theta$**: 隐式Milstein格式参数，$\theta=0.5$为梯形格式，$\theta=1$为后向格式

- **$\alpha(\cdot)$**: SDE偏移过程函数，描述参数确定性漂移

- **$\beta(\cdot)$**: SDE扩散过程函数，描述参数随机波动强度

- **$\Delta W_{n-1}$**: Wiener过程增量，满足$\Delta W_{n-1} \sim \mathcal{N}(0, \Delta t)$

- **$K_{A,n}, K_{B,n}$**: 电感动态伴随电路的系数，$K_{A,n} = L_n/(L_n + \alpha(\tilde{L}_n)\Delta t)$，$K_{B,n} = 2L_n/(2L_n + \alpha(\tilde{L}_n)\Delta t)$

## 仿真结果

### 仿真测试

| 测试场景 | 结果描述 | 对比基线 |

|---------|---------|----------|

| 简单RLC串联电路 | 在RLC电路中引入电感和电容参数的随机迁移，采用后向Milstein格式进行仿真。结果显示参数随机迁移导致的电流电压轨迹呈现随机波动特性，与确定性EMTP仿真结果相比，轨迹包络线能够覆盖确定性结果。蒙特卡罗多轨迹统计结果显示均值与确定性结果高度一致 | 与EMTP确定性计算结果相比，均值误差极小；与蒙特卡罗多轨迹仿真结果相比，单条轨迹能准确反映随机动态过程，无需大量样本统计 |

| 三相两电平VSC电路 | 在电压源换流器(VSC)系统中考虑直流侧电容和交流侧电感的参数随机迁移。仿真结果显示所提算法能够准确捕捉电力电子开关动作与参数随机迁移的相互作用，输出电压电流波形在保持EMTP精度的同时体现了参数不确定性带来的波动范围 | 与蒙特卡罗方法相比，计算效率显著提升（避免多轨迹重复计算），同时保持了与EMTP框架的直接兼容性，可在PSCAD/EMTDC、RTDS等平台实现 |

## 量化发现

- Milstein格式（包括隐式梯形和后向格式）具有1阶强收敛精度，显著优于Euler-Maruyama格式的1/2阶收敛性
- 隐式Milstein格式中的梯形和后向形式在数值稳定性方面表现优于显式Heun格式，特别是在处理乘性噪声时稳定性不受步长限制
- 所提动态伴随电路模型实现了与EMTP算法框架的直接兼容，无需修改节点导纳法的基本结构
- 对于电感元件，参数随机迁移引入的乘性激励通过项$\frac{K_{A,n}i_{L,n-1}}{L_{n-1}}D(L_{n-1})$体现，其幅值与电流大小成正比
- 仿真算法能够处理微秒级电磁暂态时间尺度与参数随机迁移慢动态过程的强刚性耦合问题

## 关键公式

### 电感元件动态伴随电路通用形式

$$$i_{L,n} = G_{L,n}(v_{i,n} - v_{j,n}) + I_{L,hist,n-1} + I_{L,rand,n-1}$$$

*将电感支路表示为诺顿等效电路，其中$G_{L,n}$为等效导纳，$I_{L,hist,n-1}$为确定性历史电流源，$I_{L,rand,n-1}$为随机历史电流源，实现与EMTP节点法的兼容*

### 隐式Milstein离散格式

$$$\eta_n = \eta_{n-1} + [(1-\theta)\alpha(\eta_{n-1}) + \theta\alpha(\tilde{\eta}_n)]\Delta t + \beta(\eta_{n-1})\Delta W_{n-1} + \frac{1}{2}\beta(\eta_{n-1})\beta'(\eta_{n-1})(\Delta W_{n-1}^2 - \Delta t)$$$

*用于离散化元件参数（R、L、C）的随机微分方程，是算法实现1阶强收敛精度的核心，其中$\theta=0.5$时为梯形格式，兼顾精度与稳定性*

## 验证详情

- **验证方式**: 对比验证：与EMTP确定性仿真结果对比验证均值准确性；与蒙特卡罗多轨迹仿真结果对比验证随机动态轨迹的正确性
- **测试系统**: 1) 简单RLC串联电路，用于验证基本元件模型；2) 三相两电平VSC（电压源换流器）电路，用于验证电力电子化系统的适用性
- **仿真工具**: 基于EMTP算法框架实现，兼容PSCAD/EMTDC、RTDS等电磁暂态仿真平台；采用SUNDIALS工具集进行算法对比测试
- **验证结果**: 所提动态伴随电路模型和随机电磁暂态仿真算法能够准确高效地反映参数随机迁移扰动下的系统运行特性。与EMTP确定性计算相比，均值轨迹高度一致；与蒙特卡罗多轨迹结果相比，单条轨迹统计特性吻合，验证了算法对EMTP框架的直接兼容性和正确性。算法实现了1阶强收敛精度，数值稳定性优于显式格式

## 适用边界

### 适用条件

- 适用于理解本文 `中  国  电  机  工  程  学  报`（2022） 在当前页面抽取范围内讨论的 EMT/电力系统暂态问题。
- 适用于以 nodal-analysis、numerical-integration 为核心的建模、仿真、等值、控制或稳定性分析场景；具体对象以原文算例和页面“涉及的模型”为准。
- 可作为知识图谱中的方法定位和文献入口，尤其用于追踪：提出考虑参数随机迁移的动态伴随电路模型，适用于电磁暂态仿真

### 失效边界

- 不应外推到原文未覆盖的拓扑、控制策略、故障类型、频率范围、硬件平台或实时步长。
- 不应把页面中的“提高、显著、快速、准确”等概括性表述当作定量结论；只有“量化发现”和原文表图可核验的数字才可用于比较。
- 若页面作者、期刊、摘要或验证字段仍不完整，本页只能作为待复核文献入口，不能作为最终证据页引用。

### 关键假设

- 页面内容假设当前 PDF 抽取文本与 frontmatter 的 `sources` 指向同一篇论文。
- 方法结论默认受原文仿真工具、测试系统、参数设置、采样步长和对比基线约束。
- 当前边界层为保守整理：未从原文直接核验的内容不得升级为确定结论。

### 证据缺口

- 作者元数据仍需回到 PDF 首页或 metadata.json 复核。
- 源文件路径：`["EMT_Doc/36/陈鹏伟 等 - 2021 - 电力电子化电力系统随机电磁暂态仿真算法.pdf"]`；需要深修时应优先核对该 PDF 的首页、摘要、方法和实验表图。
