---
title: "中  国  电  机  工  程  学  报"
type: source
authors: ['姚蜀军 等']
year: 2022
journal: "中国电机工程学报"
tags: ['emt']
created: "2026-04-13"
sources: ["EMT_Doc/34/姚蜀军 等 - 2022 - 半隐式延迟解耦电磁暂态仿真方法(二)：单端口子模块MMC通用解耦与快速仿真.pdf"]
---

# 中  国  电  机  工  程  学  报

**作者**: 姚蜀军 等
**年份**: 2022
**来源**: `34/姚蜀军 等 - 2022 - 半隐式延迟解耦电磁暂态仿真方法(二)：单端口子模块MMC通用解耦与快速仿真.pdf`

## 摘要

由于模块化多电平换流器子模块数众多及子模块拓扑的复杂性和时变性，导致仿真精度与效率难以兼顾。该文将半隐式延迟解耦原理用于模块化多电平换流器(modular multilevel converter，MMC)高效电磁暂态仿真建模研究，所得模型具有常规子模块拓扑导纳矩阵恒定、解耦电路简单、计算效率高、方法通用性强等特点。该文首先从 MMC 单端口子模块状态方程出发，利用矩阵分裂和延迟技术，构建 MMC 单端口子模块通用半隐式延迟解耦模型，并应用于半桥、全桥和双半桥子模块。然后，基于对模型参数的简化，进一步提出适用于任意单端口子模块拓扑的 MMC 通用仿真框架，并分析模型的特点，给出计算流程，最后通过算例验证该文方法的准确性和有效性。


<!-- deep-review:start -->
## 研究解读

### 1. 需求、对象、挑战与贡献

工程需求来自高电平数MMC的电磁暂态仿真：柔直和直流电网中每个桥臂串联大量子模块，若采用详细开关模型，需要小步长捕捉IGBT/二极管动作，并在开关状态变化时反复重构拓扑、求解高阶线性方程组；若采用平均值、开关函数、动态相量等模型，又会牺牲子模块电容充放电、均压控制和开关损耗等内部细节。本文研究对象是“单端口子模块MMC”，包括半桥、全桥、双半桥等可抽象为一个外部端口、内部含若干电容和开关支路的SM。难点在于子模块数量多、拓扑随开关状态变化、内部电容状态又不能被简单合并。本文贡献不是只做等值降阶，而是把半隐式延迟解耦原理用于SM状态方程：通过矩阵分裂把电容电压与桥臂电流分组，并引入半步时延，使子模块对外呈现导纳矩阵恒定的解耦电路，从而减少拓扑重构，并给出面向任意单端口SM拓扑的参数求取与仿真框架。

### 2. 模型、算法与实现技术

本文建立的核心模型是MMC单端口子模块通用半隐式延迟解耦模型。状态量分为两类：子模块内部m个电容电压向量uC，以及流经子模块端口/桥臂的电流iarm；接口量主要是子模块端口电压usm和由内部状态形成的等效受控源。作者先把桥臂电感按子模块数均分为Lsm=Larm/N，写出包含C、Lsm、Geq、Req、Ku、Ki的通用状态方程，其中Geq、Req描述内部等效导纳/电阻，Ku、Ki描述电容电压与桥臂电流之间的耦合。随后利用改进节点电压法构造增广节点方程，把端口电感替换为电流源、内部电容替换为电压源，通过关联矩阵求取不同拓扑下的等效参数。算法机制的关键是半隐式延迟：电容电压在整数时刻更新，桥臂电流在半整数时刻更新，二者相差Δt/2；桥臂电流递推使用上一整数时刻电容电压形成的等效电压源Ueq=KuuC，电容电压递推则使用半步桥臂电流形成的等效电流源Jeq=Kiiarm。这样，桥臂网络求解与各子模块内部电容更新被拆开，同一桥臂多个SM可并行计算，且常规拓扑在开关切换时不必频繁改变系统导纳矩阵。

### 3. 验证、优势与不足

原文说明通过算例验证所提方法的准确性和有效性，并明确把通用模型应用于半桥子模块、全桥子模块和双半桥子模块三类MMC拓扑；对比基线表述为详细模型或传统详细电磁暂态仿真思路，验证目标包括波形精度、导纳矩阵恒定、避免拓扑重构和支持并行计算。但当前提供的抽取文本未给出可核验的仿真软件名称、测试系统规模、步长、桥臂子模块数、运行平台、误差百分比、计算耗时或加速比，因此不能把“快速”“高效”解读为已量化的性能结论。优势主要体现在机制层面：相对平均值和动态相量模型，它保留子模块电容状态；相对详细开关模型，它通过等效受控源和半步时延降低开关拓扑变化对系统矩阵的影响；相对简单一步延时解耦，它采用半隐式格式，意在缓解前向欧拉式延时带来的稳定性与精度问题。从验证范围看，本文结论更稳妥地限于单端口SM、文中列举的HBSM/FBSM/D-HBSM及类似可按该状态方程抽象的拓扑；对于多端口子模块、复杂控制保护、故障穿越、实时仿真硬件约束、极端步长稳定性等，当前证据不足。

### 4. 价值、认知与可复用场景

这项工作的价值在于把MMC快速EMT仿真的瓶颈从“如何整体平均化桥臂”转向“如何在保留子模块电容状态的同时解耦端口网络与内部状态”。它适合用于需要观察SM电容电压、均压策略、不同单端口拓扑影响但又不能承受全详细模型代价的仿真框架，也可被后续关于MMC并行计算、通用子模块建模、节点导纳矩阵恒定化、实时仿真预研的页面复用。使用时应把它视为一种状态空间—节点分析结合的解耦建模方法，而不是已证明在所有MMC场景下都最快或最精确的模型；尤其不应外推到原文未覆盖的多端口换流器、非典型半导体模型、具体硬件实时平台或未经验证的故障工况。

### 证据边界

- 来自原文的确定信息：论文题名、作者、单位、关键词、研究对象为单端口子模块MMC，方法为半隐式延迟解耦，应用拓扑包括HBSM、FBSM和D-HBSM。
- 来自原文的确定机制：状态方程包含电容电压uC和桥臂电流iarm，桥臂电感按Lsm=Larm/N均分，利用矩阵分裂和半步时延实现两类状态量解耦。
- 来自原文摘要和引言的确定比较：平均值、开关函数、动态相量等模型计算快但忽略或弱化内部特性；详细模型可保留开关细节但计算量大。
- 当前抽取文本未报告可核验的数值结果：没有误差百分比、加速比、耗时、矩阵规模、仿真步长、子模块数量或硬件平台等量化指标。
- 仿真工具和完整测试系统参数在当前证据中未明确，不能断言使用PSCAD、MATLAB或RTDS，也不能断言具体工程算例规模。
- 对任意单端口子模块拓扑的通用性属于作者方法框架主张；其边界仍受单端口抽象、开关电阻模型、参数求取方式和已验证拓扑范围约束。
<!-- deep-review:end -->
## 核心贡献

- 问题定位：由于模块化多电平换流器子模块数众多及子模块拓扑的复杂性和时变性，导致仿真精度与效率难以兼顾。该文将半隐式延迟解耦原理用于模块化多电平换流器(modular multilevel converter，MMC)高效电磁暂态仿真建模研究，所得模型具有常规子模块拓扑导纳矩阵恒定、解耦电路简单、计算效率高、方法通用性强等特点。
- 方法机制：该研究提出基于半隐式延迟解耦原理(SILDP)的MMC单端口子模块通用电磁暂态仿真方法。方法核心在于：首先建立包含m个电容的单端口子模块通用状态方程，将桥臂电感均分至各子模块端口(Lsm=Larm/N)；然后利用矩阵分裂技术将状态变量分为电容电压组与桥臂电流组，通过构造半步时延的半隐式差分格式实现状态变量组间解耦；
- 验证证据：基于单端口子模块的MMC系统，包括半桥(HBSM)、全桥(FBSM)和双半桥(D-HBSM)三种拓扑结构的电磁暂态仿真模型；文本未明确指定具体仿真软件名称（如PSCAD/EMTDC、MATLAB等），仅提及电磁暂态仿真环境；所提半隐式延迟解耦模型在保持与详细模型相当仿真精度的同时，实现了导纳矩阵恒定、避免拓扑重构，并支持并行计算。具体误差范围、加速比等定量指标文本未提供。
- 量化与结论：IGBT//VD开关组导通电阻Ron=0.01Ω，关断电阻Roff=1MΩ；桥臂电感均分参数：Lsm=Larm/N，其中N为单桥臂子模块数；当开关处于关断状态时，等效导纳Geq≈0（近似为零矩阵），实现子模块内部各电容间解耦；采用半步时延格式(Δt/2)实现状态变量组间解耦，桥臂电流与子模块电容电压计算时刻相差半个时步
- 适用边界：适用于理解本文 中 国 电 机 工 程 学 报 （2022） 在当前页面抽取范围内讨论的 EMT/电力系统暂态问题。；适用于以 state-space、nodal-analysis、parallel 为核心的建模、仿真、等值、控制或稳定性分析场景；具体对象以原文算例和页面“涉及的模型”为准。

## 使用的方法

- [[state-space-method]]
- [[nodal-analysis]]
- [[parallel-computing]]
- [[fixed-admittance]]

## 涉及的模型

- [[mmc-model]]

## 相关主题

- [[mmc-model]]
- [[vsc-hvdc]]
- [[vsc-hvdc]]

## 主要发现

- 所提模型在子模块开关状态切换时保持导纳矩阵恒定，有效避免了传统详细模型中频繁的拓扑重构与矩阵求逆
- 该方法在维持与详细模型相当仿真精度的同时显著提升了计算效率，并天然适用于半桥、全桥及双半桥等多种单端口子模块拓扑

## 方法细节

### 方法概述

该研究提出基于半隐式延迟解耦原理(SILDP)的MMC单端口子模块通用电磁暂态仿真方法。方法核心在于：首先建立包含m个电容的单端口子模块通用状态方程，将桥臂电感均分至各子模块端口(Lsm=Larm/N)；然后利用矩阵分裂技术将状态变量分为电容电压组与桥臂电流组，通过构造半步时延的半隐式差分格式实现状态变量组间解耦；最后建立等效解耦电路，在保持导纳矩阵恒定的同时，实现桥臂电流与子模块电容电压的交替求解(相差半个时步)，并支持同一桥臂内各子模块的并行计算。该方法适用于半桥(HBSM)、全桥(FBSM)及双半桥(D-HBSM)等多种拓扑，通过二值电阻模型(IGBT导通Ron=0.01Ω，关断Roff=1MΩ)处理开关状态。

### 数学公式

**公式1**: $$$$\begin{bmatrix} C & \\ & L_{sm} \end{bmatrix} \begin{bmatrix} \frac{du_C}{dt} \\ \frac{di_{arm}}{dt} \end{bmatrix} = \begin{bmatrix} -G_{eq} & K_i \\ -K_u & -R_{eq} \end{bmatrix} \begin{bmatrix} u_C \\ i_{arm} \end{bmatrix} + \begin{bmatrix} 0 \\ u_{sm} \end{bmatrix}$$$$

*单端口子模块通用状态方程，其中C为电容对角阵，Lsm为均分桥臂电感，Geq为等效导纳，Req为等效电阻，Ku和Ki为耦合系数矩阵，uC为电容电压矢量，iarm为桥臂电流，usm为子模块端口电压*

**公式2**: $$$$\begin{bmatrix} Y & A_C \\ A_C^T & 0 \end{bmatrix} \begin{bmatrix} U \\ I_C \end{bmatrix} = \begin{bmatrix} I_L \\ U_C \end{bmatrix}$$$$

*增广节点电压方程，用于通过改进节点电压法求解等效电路参数，Y为节点导纳矩阵，AC为增广系数矩阵*

**公式3**: $$$$\begin{cases} G_{eq} = (M_G^T Y^{-1} M_G)^{-1} \\ R_{eq} = M_R^T Y'^{-1} M_R \\ K_{uC} = M_K^T (Y'^{-1} \begin{bmatrix} 0 \\ U_C \end{bmatrix}) \end{cases}$$$$

*等效参数计算公式，通过关联矩阵MG、MR、MK计算等效导纳Geq、等效电阻Req及受控源系数，适用于任意单端口子模块拓扑*

**公式4**: $$$$\begin{bmatrix} C(u_C^{n+1}-u_C^n) \\ L_{sm}(i_{arm}^{n+1/2}-i_{arm}^{n-1/2}) \end{bmatrix} = \begin{bmatrix} 0 & \\ & -R_{eq} \end{bmatrix} \begin{bmatrix} u_C^{n+1}+u_C^n \\ i_{arm}^{n+1/2}+i_{arm}^{n-1/2} \end{bmatrix} \frac{\Delta t}{2} + \begin{bmatrix} & K_i \\ -K_u & \end{bmatrix} \begin{bmatrix} u_C^n \\ i_{arm}^{n+1/2} \end{bmatrix} \Delta t + \begin{bmatrix} 0 \\ u_{sm}^n \end{bmatrix} \Delta t$$$$

*半步时延的半隐式差分方程，实现电容电压与桥臂电流的解耦，采用半隐式格式保证数值稳定性*

**公式5**: $$$$u_{Cj}^{n+1} = u_{Cj}^n + \frac{J_{eq,j}^{n+1/2} \Delta t}{C_j}, \quad j=1,\cdots,m$$$$

*电容电压递推公式，m为子模块内部电容数量，Jeq为等效电流源，显示电容电压可在求得桥臂电流后独立更新*

**公式6**: $$$$i_{arm}^{n+1/2} = \frac{(\frac{L_{sm}}{\Delta t} - \frac{R_{eq}}{2})i_{arm}^{n-1/2} - U_{eq}^n + u_{sm}^n}{\frac{L_{sm}}{\Delta t} + \frac{R_{eq}}{2}}$$$$

*桥臂电流递推公式，Ueq=KuuC为等效电压源，显示桥臂电流求解仅依赖于上一时步的电容电压和桥臂电流*

### 算法步骤

1. 构建单端口子模块通用状态方程：将桥臂电感Larm均分为N份(Lsm=Larm/N)分配至各子模块端口，对IGBT//VD开关组采用二值电阻模型(导通Ron=0.01Ω，关断Roff=1MΩ)建立等效电路

2. 建立增广节点电压方程：利用改进节点电压法，将端口电感用电流源替代，内部电容用电压源替代，构建替代电路的增广节点电压方程Y'U'=I'

3. 计算等效解耦参数：通过关联矩阵MG、MR、MK计算等效导纳Geq、等效电阻Req及受控源系数Ku、Ki，当开关关断电阻近似无穷大时Geq≈0，实现内部电容间解耦

4. 矩阵分裂与半步时延构造：将状态方程按电容电压和桥臂电流分为两个状态变量组，采用矩阵分裂技术构造半步时延的半隐式差分格式，实现状态变量组间解耦

5. 建立解耦等效电路：构建包含等效电压源Ueq=KuuC和等效电流源Jeq=Kiiarm的解耦电路模型，确保开关状态切换时导纳矩阵保持恒定

6. 简化模型参数：根据开关关断特性及开关状态组合，简化解耦模型参数的求取过程，构建适用于任意单端口子模块拓扑(HBSM、FBSM、D-HBSM)的通用仿真框架

7. 交替求解与并行计算：在n时刻基于uC^n计算桥臂电流iarm^{n+1/2}，在n+1/2时刻基于iarm^{n+1/2}并行计算各子模块电容电压uC^{n+1}，实现半步时延的交替求解和子模块级并行计算

### 关键参数

- **Lsm**: 均分至单个子模块端口的桥臂电感，Lsm=Larm/N

- **N**: 每个桥臂串联的子模块数量

- **m**: 单个子模块内部包含的电容数量

- **Ron**: IGBT//VD开关组导通电阻，0.01Ω

- **Roff**: IGBT//VD开关组关断电阻，1MΩ

- **Geq**: 等效导纳矩阵，当开关关断时近似为0

- **Req**: 等效电阻

- **Ku**: 与电容电压相关的系数矩阵

- **Ki**: 与桥臂电流相关的系数矩阵

- **Δt**: 仿真步长

- **MG_MR_MK**: 节点-端口关联矩阵，用于等效参数计算

## 仿真结果

### 仿真测试

| 测试场景 | 结果描述 | 对比基线 |

|---------|---------|----------|

| 半桥子模块(HBSM)电磁暂态仿真验证 | 文本未提供具体误差百分比数值，但验证表明所提方法在开关状态切换时保持导纳矩阵恒定，避免了传统详细模型中的拓扑重构与矩阵求逆 | 与详细模型相比保持相当的仿真精度，但具体加速比和误差范围文本未明确给出 |

| 全桥子模块(FBSM)电磁暂态仿真验证 | 通过构建通用半隐式延迟解耦模型，验证了方法对全桥拓扑的适用性，实现了子模块内部状态变量解耦 | 文本未提供具体数值对比结果 |

| 双半桥子模块(D-HBSM)电磁暂态仿真验证 | 验证了方法对包含多个电容的复杂子模块拓扑的适用性，通过Geq≈0条件实现内部电容间解耦 | 文本未提供具体数值对比结果 |

## 量化发现

- IGBT//VD开关组导通电阻Ron=0.01Ω，关断电阻Roff=1MΩ
- 桥臂电感均分参数：Lsm=Larm/N，其中N为单桥臂子模块数
- 当开关处于关断状态时，等效导纳Geq≈0（近似为零矩阵），实现子模块内部各电容间解耦
- 采用半步时延格式(Δt/2)实现状态变量组间解耦，桥臂电流与子模块电容电压计算时刻相差半个时步
- 子模块内部电容数量m可为任意正整数，方法适用于包含m个电容的单端口子模块拓扑
- 文本未提供具体的仿真误差百分比(如<0.5%)或加速比(如快X倍)等定量结果

## 关键公式

### 单端口子模块半步时延半隐式差分方程

$$$$\begin{bmatrix} C(u_C^{n+1}-u_C^n) \\ L_{sm}(i_{arm}^{n+1/2}-i_{arm}^{n-1/2}) \end{bmatrix} = \begin{bmatrix} 0 & \\ & -R_{eq} \end{bmatrix} \begin{bmatrix} u_C^{n+1}+u_C^n \\ i_{arm}^{n+1/2}+i_{arm}^{n-1/2} \end{bmatrix} \frac{\Delta t}{2} + \begin{bmatrix} & K_i \\ -K_u & \end{bmatrix} \begin{bmatrix} u_C^n \\ i_{arm}^{n+1/2} \end{bmatrix} \Delta t + \begin{bmatrix} 0 \\ u_{sm}^n \end{bmatrix} \Delta t$$$$

*应用于MMC单端口子模块通用解耦，实现电容电压组与桥臂电流组的半隐式解耦，是SILDP方法的核心数学基础*

### 电容电压递推公式

$$$$u_{Cj}^{n+1} = u_{Cj}^n + \frac{J_{eq,j}^{n+1/2} \Delta t}{C_j}$$$$

*在求得桥臂电流后，用于并行更新各子模块内部电容电压，支持j=1,...,m个电容的独立计算*

### 桥臂电流递推公式

$$$$i_{arm}^{n+1/2} = \frac{(\frac{L_{sm}}{\Delta t} - \frac{R_{eq}}{2})i_{arm}^{n-1/2} - U_{eq}^n + u_{sm}^n}{\frac{L_{sm}}{\Delta t} + \frac{R_{eq}}{2}}$$$$

*用于计算桥臂电流半步时值，基于上一时步电容电压和当前端口电压，保持导纳矩阵恒定*

## 验证详情

- **验证方式**: 仿真验证与对比分析（与详细模型对比）
- **测试系统**: 基于单端口子模块的MMC系统，包括半桥(HBSM)、全桥(FBSM)和双半桥(D-HBSM)三种拓扑结构的电磁暂态仿真模型
- **仿真工具**: 文本未明确指定具体仿真软件名称（如PSCAD/EMTDC、MATLAB等），仅提及电磁暂态仿真环境
- **验证结果**: 所提半隐式延迟解耦模型在保持与详细模型相当仿真精度的同时，实现了导纳矩阵恒定、避免拓扑重构，并支持并行计算。具体误差范围、加速比等定量指标文本未提供。

## 适用边界

### 适用条件

- 适用于理解本文 `中  国  电  机  工  程  学  报`（2022） 在当前页面抽取范围内讨论的 EMT/电力系统暂态问题。
- 适用于以 state-space、nodal-analysis、parallel 为核心的建模、仿真、等值、控制或稳定性分析场景；具体对象以原文算例和页面“涉及的模型”为准。
- 可作为知识图谱中的方法定位和文献入口，尤其用于追踪：提出了一种基于半隐式延迟解耦原理的MMC单端口子模块通用建模方法

### 失效边界

- 不应外推到原文未覆盖的拓扑、控制策略、故障类型、频率范围、硬件平台或实时步长。
- 不应把页面中的“提高、显著、快速、准确”等概括性表述当作定量结论；只有“量化发现”和原文表图可核验的数字才可用于比较。
- 若页面作者、期刊、摘要或验证字段仍不完整，本页只能作为待复核文献入口，不能作为最终证据页引用。

### 关键假设

- 页面内容假设当前 PDF 抽取文本与 frontmatter 的 `sources` 指向同一篇论文。
- 方法结论默认受原文仿真工具、测试系统、参数设置、采样步长和对比基线约束。
- 当前边界层为保守整理：未从原文直接核验的内容不得升级为确定结论。

### 证据缺口

- 作者元数据仍需回到 PDF 首页或 metadata.json 复核。
- 源文件路径：`["EMT_Doc/34/姚蜀军 等 - 2022 - 半隐式延迟解耦电磁暂态仿真方法(二)：单端口子模块MMC通用解耦与快速仿真.pdf"]`；需要深修时应优先核对该 PDF 的首页、摘要、方法和实验表图。
