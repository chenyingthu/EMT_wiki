---
title: "Fast electromagnetic transient simulation of modular multilevel converter based on semi-implicit delay model"
type: source
authors: ['Fan 等']
year: 2023
journal: "电力系统自动化"
tags: ['emt']
created: "2026-04-13"
sources: ["EMT_Doc/18/Fan 等 - 2017 - Fast electromagnetic transient simulation for flexible DC Power Grid.pdf"]
---

# Fast electromagnetic transient simulation of modular multilevel converter based on semi-implicit delay model

**作者**: Fan 等
**年份**: 2023
**来源**: `18/Fan 等 - 2017 - Fast electromagnetic transient simulation for flexible DC Power Grid.pdf`

## 摘要

摘要： 柔性直流电网含多个模块化多电平换流器与直流断路器 ，需要快速准确的换流器与直流断 路器仿真模型。为提高模块化多电平换流器的仿真速度 ，利用电容能量均分的思想简化子模块均 压策略，并准确保留桥臂闭锁与不闭锁时的外特性 ，提出了一种能量均分模型。针对级联全桥型直 流断 路 器，给 出 一 种 断 路 器 支 路 子 模 块 串 等 值 电 路 简 化 方 法 ，提 出 了 一 种 快 速 仿 真 模 型。 在 PSCAD / EMTDC 中搭建模型并进行了电磁暂态仿真 ，验证了所提出模型的准确性与快速性 。 关键词： 柔性直流电网; 模块化多电平换流器; 直流断路器; 快速仿真


<!-- deep-review:start -->
## 研究解读

### 1. 需求、对象、挑战与贡献

柔性直流电网通常由多个MMC换流站、架空直流线路和大量直流断路器组成，控制保护方案需要通过EMT仿真反复校核故障、闭锁、开断等快速暂态过程。研究对象不是单个器件的物理细节，而是MMC桥臂及级联全桥型直流断路器在系统级仿真中的外部电气行为。难点在于：MMC详细模型要逐个表示大量子模块，详细等价模型虽减少开关细节，但仍需执行子模块电容电压排序和均压控制；直流断路器开断过程又包含多支路、多全桥子模块串和避雷器耗能，规模化接入直流电网后计算量迅速膨胀。本文的贡献是把“桥臂内部均压已充分发挥作用”转化为能量均分模型，用投入/未投入等效电容代替逐子模块状态，并保留桥臂闭锁与非闭锁外特性；同时把级联全桥断路器子模块串聚合为端口等效电路，使大规模直流电网EMT仿真可在不逐个跟踪内部子模块的条件下完成。

### 2. 模型、算法与实现技术

MMC部分提出能量均分模型EEM。其输入包括控制系统给出的桥臂投入子模块数Non、桥臂电流、上一时刻平均电容电压uc及历史等效电压源；输出是桥臂端口等效电压源Ubr、等效电阻Rbr以及更新后的平均电容电压。模型假设同一桥臂内所有子模块电容电压相等，把投入子模块合成为电容Con=C/Non、端电压ucon约为Non·uc的等效支路，把未投入子模块合成为Coff=C/Noff、端电压ucoff约为Noff·uc的等效支路。梯形积分公式把电容转化为历史受控电压源与等效电阻，便于接入PSCAD/EMTDC网络方程；能量守恒式把投入和未投入两部分能量重新平均分配到N个子模块，从而用一次能量更新替代详细等价模型中的电压排序和选择。断路器部分针对级联全桥型直流断路器，将单个全桥子模块在闭锁/导通状态下表示为含受控电压源和可变电阻的等效电路，再对N个串联子模块采用u'=Nu、R'=NR进行端口聚合。这样网络求解只看到等效端口，而不需要显式求解每个全桥子模块的内部开关状态。

### 3. 验证、优势与不足

作者在PSCAD/EMTDC中建立对比模型，基线包括MMC详细模型、详细等价模型，以及直流断路器详细模型。验证场景包括双端±250 kV/500 MW直流系统中的MMC稳态与极间短路故障、500 kV级联全桥直流断路器测试电路，以及含4个MMC、5条线路、28台直流断路器的±500 kV四端柔性直流电网。评价方式主要是关键电气量波形对比、残差可信度指标和仿真耗时。页面给出的量化结果显示，推荐步长为10~25 μs；1001电平MMC中EEM相对详细等价模型提速38倍以上，且耗时与子模块电平数基本解耦；断路器快速模型耗时由16125 ms降至578 ms，断路器电压和主支路电流残差可信度分别为0.9908和0.9886。优势集中在系统级外特性仿真：能够保留桥臂闭锁/非闭锁和断路器开断过程的端口行为，同时显著减少排序、开关和子模块串求解负担。从验证范围看，结论主要适用于半桥MMC、级联全桥型直流断路器和文中控制/故障设置；未证明其可用于研究子模块电容离散不均衡、均压算法本身、器件损耗热过程或其他断路器拓扑。

### 4. 价值、认知与可复用场景

这项工作的核心认知是：在大规模直流电网EMT仿真中，若研究目标是换流站和断路器对外暂态响应，而不是桥臂内部每个子模块的电压分散过程，则可用能量守恒和端口等效替代大量内部排序与串联子模块求解。它适合被后续直流电网故障保护、系统级控制策略筛选、多端柔直网规划仿真、含大量直流断路器的快速EMT算例页面复用，也可作为详细等价模型与平均值模型之间的工程折中方案。不适合外推为通用MMC内部行为模型：若问题关注子模块均压策略优劣、个别电容过压、开关器件应力、损耗、热设计或硬件实时仿真的极小步长稳定性，仍需回到更详细模型或重新验证。

### 证据边界

- 作者、题名、DOI和摘要来自给定原文抽取：范新凯、王艳婷、张保会，《柔性直流电网的快速电磁暂态仿真》，DOI为10.7500/AEPS20160623003；用户元数据中的英文题名和2023年份与抽取文本存在不一致，不能据此改写为另一篇论文。
- MMC能量均分模型、投入/未投入等效电容、梯形积分电容等效、能量守恒更新和闭锁/非闭锁外特性保留均来自原文方法段落；对子模块电容电压完全均衡的解释属于对模型假设的归纳。
- PSCAD/EMTDC、双端±250 kV/500 MW系统、500 kV断路器测试、±500 kV四端柔直网和28台断路器等验证信息来自当前页面整理；若用于正式引用，应回查原文表图确认参数和算例设置。
- 38倍以上加速、16125 ms到578 ms、残差可信度0.9908和0.9886、步长10~25 μs等数值来自当前页面已有抽取内容；在给出的原文片段中未完整展示对应表图，因此应标为需复核的量化证据。
- 验证主要覆盖端口电压、电流等外特性，未见对内部子模块电压离散分布、均压控制动态、器件级损耗和热应力的独立验证；因此不能把该模型用于这些内部行为的精确分析。
- 从验证范围看，模型针对半桥MMC和级联全桥型直流断路器；其他MMC拓扑、混合式断路器、不同保护时序、实时仿真硬件和更宽频率范围需要重新验证。
<!-- deep-review:end -->
## 核心贡献

- 问题定位：摘要： 柔性直流电网含多个模块化多电平换流器与直流断路器 ，需要快速准确的换流器与直流断 路器仿真模型。为提高模块化多电平换流器的仿真速度 ，利用电容能量均分的思想简化子模块均 压策略，并准确保留桥臂闭锁与不闭锁时的外特性 ，提出了一种能量均分模型。
- 方法机制：提出一种面向柔性直流电网的快速电磁暂态仿真方法，核心包含MMC能量均分模型（EEM）与级联全桥直流断路器快速等效模型。EEM基于理想均压假设与能量守恒原理，将桥臂内投入与未投入的子模块分别聚合为单一等效电容，通过能量均分原则直接迭代更新桥臂平均电容电压，彻底规避传统详细等价模型中耗时的子模块电压排序运算。断路器模型将全桥子模块抽象为含受控电压源与可变电阻的双支路等效电路，利用梯形积分法进行离散化，并将串联子模块串整体等效为单端口网络。
- 验证证据：对比仿真验证（详细模型/详细等价模型 vs 提出模型）；双端±250kV/500MW直流系统（MMC验证）；500kV级联全桥直流断路器测试电路；±500kV四端柔性直流电网（含4个MMC、5条线路、28台断路器）；PSCAD/EMTDC V4.6.0 (运行环境：Windows 10 64位, Intel i7-4770, 16GB RAM)
- 量化与结论：仿真步长推荐区间为10~25 μs；步长>25 μs时两种模型精度均明显下降，步长<10 μs时耗时剧增但精度无显著提升。；电平MMC仿真中，能量均分模型较详细等价模型计算速度提升38倍以上，且计算耗时与子模块电平数实现解耦。；直流断路器快速模型仿真耗时从16125 ms降至578 ms，加速比达28倍，关键电气量残差可信度指标均大于0.988。；
- 适用边界：适用于理解本文 Fast electromagnetic transient simulation of modular multilevel converter based on semi-implicit delay model （2023） 在当前页面抽取范围内讨论的 EMT/电力系统暂态问题。；

## 使用的方法

- [[能量均分法|能量均分法]]
- [[nodal-analysis|等效电路法]]
- [[受控电压源建模|受控电压源建模]]
- [[numerical-integration|梯形积分法]]

## 涉及的模型

- [[mmc-model|MMC]]
- [[mmc-model|半桥子模块]]
- [[级联全桥直流断路器|级联全桥直流断路器]]
- [[直流架空线路|直流架空线路]]

## 相关主题

- [[柔性直流电网|柔性直流电网]]
- [[快速电磁暂态仿真|快速电磁暂态仿真]]
- [[换流器简化建模|换流器简化建模]]
- [[直流断路器建模|直流断路器建模]]
- [[model-order-reduction|模型降阶]]

## 主要发现

- 能量均分模型精度与详细等价模型一致，且仿真耗时几乎不随电平数增加。
- 断路器快速模型较详细模型提速约十倍，关键电气量波形残差可信度指标高。
- 仿真步长推荐10至25微秒，可在保证精度的同时实现最优计算效率。

## 方法细节

### 方法概述

提出一种面向柔性直流电网的快速电磁暂态仿真方法，核心包含MMC能量均分模型（EEM）与级联全桥直流断路器快速等效模型。EEM基于理想均压假设与能量守恒原理，将桥臂内投入与未投入的子模块分别聚合为单一等效电容，通过能量均分原则直接迭代更新桥臂平均电容电压，彻底规避传统详细等价模型中耗时的子模块电压排序运算。断路器模型将全桥子模块抽象为含受控电压源与可变电阻的双支路等效电路，利用梯形积分法进行离散化，并将串联子模块串整体等效为单端口网络。两者均严格保留换流器与断路器在闭锁/非闭锁状态下的外部电气特性，在PSCAD/EMTDC平台中实现，适用于大规模、高电平数直流电网的稳态运行与故障暂态全过程仿真。

### 数学公式

**公式1**: $$$u_c(t) = R_c i_c(t) + u_{ceq}(t-\Delta T)$，$R_c = \frac{\Delta T}{2C}$$$

*基于梯形积分法的电容离散化等效公式，用于将子模块电容转化为受控电压源与等效电阻串联形式*

**公式2**: $$$\frac{1}{2}NCu_c^2(t) = \frac{1}{2}C_{on}u_{con}^2(t) + \frac{1}{2}C_{off}u_{coff}^2(t)$$$

*桥臂电容能量守恒方程，将投入与未投入子模块的总能量等效分配至全桥臂*

**公式3**: $$$u_c(t) = \frac{u_{con}(t) + u_{coff}(t)}{N}$$$

*经泰勒展开一阶近似简化后的电容电压迭代更新公式，避免开方运算以提升计算效率*

**公式4**: $$$U_{br}(t) = u_{ceqon}(t-\Delta T)$，$R_{br} = NR_{on} + R_{con}$$$

*MMC桥臂外特性等效公式，将桥臂整体表征为受控电压源与等效电阻串联*

**公式5**: $$$u(t) = R_c(i_a(t-\Delta T) + i_b(t-\Delta T)) + u_c(t-\Delta T)$$$

*全桥子模块闭锁状态下的离散化电压方程，用于构建断路器快速等效电路*

**公式6**: $$$u'(t) = Nu(t)$，$R' = NR$$$

*N个全桥子模块串联后的端口等效缩放公式，实现子模块串的快速聚合*

### 算法步骤

1. 初始化仿真参数：设定步长$\Delta T$，读取上一时刻桥臂平均电容电压$u_c(t-\Delta T)$及历史等效电压源状态。

2. 调制指令解析：根据控制系统输出的参考电压，计算当前桥臂需投入的子模块数量$N_{on}$，并得出未投入数量$N_{off} = N - N_{on}$。

3. 构建等效电容网络：计算投入等效电容$C_{on}=C/N_{on}$及其历史电压源$u_{ceqon}$，以及未投入等效电容$C_{off}=C/N_{off}$及其电压$u_{coff}$。

4. 网络方程求解：将桥臂替换为等效受控源$U_{br}$与电阻$R_{br}$，结合外部直流电网拓扑求解当前时刻桥臂电流$i_c(t)$及等效电容端电压$u_{con}(t)$。

5. 能量均分与状态更新：利用简化公式$u_c(t) = (u_{con}(t) + u_{coff}(t))/N$计算新时刻平均电容电压；若$u_c(t)<0$则强制置零以符合二极管单向导通特性。

6. 断路器等效处理：根据主支路/转移支路触发或闭锁指令，更新全桥子模块等效电阻$R$与电压源$u$；对串联子模块串应用$u'=Nu, R'=NR$进行端口聚合，接入直流网络求解。

7. 历史变量刷新：更新所有等效电压源历史值$u_{ceq}(t-\Delta T)$、$u_c(t-\Delta T)$等，推进至下一仿真步长循环迭代。

### 关键参数

- **仿真步长_ΔT**: 推荐10~25 μs

- **电容等效电阻_Rc**: ΔT/(2C)

- **IGBT导通电阻_Ron**: 器件固有参数，通常取毫欧级

- **桥臂子模块数_N**: 11~1001（验证范围）

- **子模块电容_C**: 根据额定电压与能量设计确定

## 仿真结果

### 仿真测试

| 测试场景 | 结果描述 | 对比基线 |

|---------|---------|----------|

| MMC极间短路故障仿真 | 在11至161电平双端±250kV/500MW系统中进行极间金属性短路测试，能量均分模型与详细模型的正极直流电压、电流波形高度重合，残差可信度指标φ均接近1。 | 在1001电平工况下，能量均分模型较详细等价模型提速38倍以上，且仿真耗时几乎不随电平数增加而上升。 |

| 级联全桥直流断路器开断过程仿真 | 500kV断路器（主支路4个子模块，转移支路128个子模块）在0.5s故障后0.2ms启动分断，2.5ms机械开关开断，5ms完全开断。快速模型准确复现了电流转移与避雷器耗能过程。 | 断路器电压U_brk与主支路电流i_main的残差可信度指标分别为0.9908和0.9886；仿真运行时间由详细模型的16125 ms大幅缩减至578 ms，提速约28倍。 |

| 四端柔性直流电网系统级仿真 | 搭建±500kV四端电网（含4个MMC、5条架空线、28台直流断路器），对比传统详细等价模型+详细断路器（模型1）与本文快速模型组合（模型2）。 | 模型2在保持与模型1同等电磁暂态精度的前提下，彻底解决高电平数与多断路器导致的计算瓶颈，满足大规模直流电网高效仿真需求。 |

## 量化发现

- 仿真步长推荐区间为10~25 μs；步长>25 μs时两种模型精度均明显下降，步长<10 μs时耗时剧增但精度无显著提升。
- 1001电平MMC仿真中，能量均分模型较详细等价模型计算速度提升38倍以上，且计算耗时与子模块电平数实现解耦。
- 直流断路器快速模型仿真耗时从16125 ms降至578 ms，加速比达28倍，关键电气量残差可信度指标均大于0.988。
- 能量均分模型与详细等价模型在稳态及故障工况下的波形误差水平无显著差异，残差可信度指标φ均趋近于1，验证了外特性等效的准确性。

## 关键公式

### MMC能量均分简化迭代公式

$$$u_c(t) = \frac{u_{con}(t) + u_{coff}(t)}{N}$$$

*用于每个仿真步长结束时，根据投入与未投入等效电容的电压快速更新桥臂平均电容电压，替代复杂的排序与开方运算*

### 桥臂电容能量守恒方程

$$$\frac{1}{2}NCu_c^2(t) = \frac{1}{2}C_{on}u_{con}^2(t) + \frac{1}{2}C_{off}u_{coff}^2(t)$$$

*能量均分模型的理论基础，确保聚合等效前后桥臂总储能严格一致*

### 级联全桥子模块串端口等效公式

$$$u'(t) = Nu(t)$，$R' = NR$$$

*用于直流断路器转移支路等大量串联子模块的快速聚合，将N个全桥子模块等效为单一受控源与电阻串联支路*

### MMC桥臂外特性等效公式

$$$U_{br}(t) = u_{ceqon}(t-\Delta T)$，$R_{br} = NR_{on} + R_{con}$$$

*将桥臂整体替换为受控电压源与等效电阻，用于接入外部直流网络进行节点导纳矩阵求解*

## 验证详情

- **验证方式**: 对比仿真验证（详细模型/详细等价模型 vs 提出模型）
- **测试系统**: 双端±250kV/500MW直流系统（MMC验证）；500kV级联全桥直流断路器测试电路；±500kV四端柔性直流电网（含4个MMC、5条线路、28台断路器）
- **仿真工具**: PSCAD/EMTDC V4.6.0 (运行环境：Windows 10 64位, Intel i7-4770, 16GB RAM)
- **验证结果**: 所提能量均分模型与断路器快速模型在多种稳态及故障工况下，与详细模型/详细等价模型的电气量波形高度吻合，残差可信度指标均>0.98。在1001电平MMC及含28台断路器的直流电网中实现数十倍计算加速，验证了模型在保留关键外特性的同时具备极高的计算效率与工程适用性。

## 适用边界

### 适用条件

- 适用于理解本文 `Fast electromagnetic transient simulation of modular multilevel converter based on semi-implicit delay model`（2023） 在当前页面抽取范围内讨论的 EMT/电力系统暂态问题。
- 适用于以 能量均分法、等效电路法、受控电压源建模 为核心的建模、仿真、等值、控制或稳定性分析场景；具体对象以原文算例和页面“涉及的模型”为准。
- 可作为知识图谱中的方法定位和文献入口，尤其用于追踪：提出MMC能量均分模型，简化子模块均压排序算法，保留桥臂闭锁外特性。

### 失效边界

- 不应外推到原文未覆盖的拓扑、控制策略、故障类型、频率范围、硬件平台或实时步长。
- 不应把页面中的“提高、显著、快速、准确”等概括性表述当作定量结论；只有“量化发现”和原文表图可核验的数字才可用于比较。
- 若页面作者、期刊、摘要或验证字段仍不完整，本页只能作为待复核文献入口，不能作为最终证据页引用。

### 关键假设

- 页面内容假设当前 PDF 抽取文本与 frontmatter 的 `sources` 指向同一篇论文。
- 方法结论默认受原文仿真工具、测试系统、参数设置、采样步长和对比基线约束。
- 当前边界层为保守整理：未从原文直接核验的内容不得升级为确定结论。

### 证据缺口

- 作者元数据仍需回到 PDF 首页或 metadata.json 复核。
- 摘要抽取偏短，可能没有覆盖完整问题定义、方法和验证结论。
- 源文件路径：`["EMT_Doc/18/Fan 等 - 2017 - Fast electromagnetic transient simulation for flexible DC Power Grid.pdf"]`；需要深修时应优先核对该 PDF 的首页、摘要、方法和实验表图。
