---
title: "An enhanced K-means two-step clustering method for dynamic equivalent modeling of DFIG wind farms based on LVRT characteristics"
type: source
authors: ['Junhua Xu']
year: 2025
journal: "Electric Power Systems Research"
tags: ['emt']
created: "2026-04-13"
sources: ["EMT_Doc/07&08/Xu et al. - 2026 - An enhanced K-means two-step clustering method for dynamic equivalent modeling of DFIG wind farms ba.pdf"]
---

# An enhanced K-means two-step clustering method for dynamic equivalent modeling of DFIG wind farms based on LVRT characteristics

**作者**: Junhua Xu
**年份**: 2025
**来源**: `07&08/Xu et al. - 2026 - An enhanced K-means two-step clustering method for dynamic equivalent modeling of DFIG wind farms ba.pdf`

## 摘要

本文提出一种基于低电压穿越（LVRT）特性的增强型K-means两步聚类风电场动态等值建模方法。首先，根据风机实时有功出力与LVRT动态响应特征，将全场风机划分为具有聚类特性（启动区、恒速区、恒功率区）与无聚类特性（MPPT区）两类。针对MPPT区风机，采用增强型K-means算法进行二次聚类。该算法融合K-Means++概率初始化策略优化初始中心选择，引入KD树索引加速高维数据最近邻搜索，并利用Davies-Bouldin指数（DBI）自适应确定最优聚类数，克服传统算法依赖人工设定参数与易陷局部最优的缺陷。最后，基于聚类结果按容量、阻抗、控制参数等比例聚合计算等值机参数，构建多机等值模型，实现故障期间动态响应的高精度复现与仿真效率的大幅提升。


<!-- deep-review:start -->
## 研究解读

### 1. 需求、对象、挑战与贡献

实际需求是：在含大规模DFIG风电场的电磁暂态/动态稳定仿真中，详细建模每台机组会带来很高计算负担，但简单单机等值又难以复现故障期间各机组因风速、出力和控制状态不同而产生的差异化LVRT响应。研究对象是并网DFIG风电场的多机动态等值，尤其关注低电压穿越期间的有功、无功、电压等动态响应。难点在于：DFIG的LVRT响应不是只由拓扑决定，还与故障前有功出力、运行区间和控制限幅等有关；传统聚类方法往往需要人为指定初始中心和聚类数，且可能忽略故障期间的响应机理，导致物理相似机组被错误合并或拆分。本文贡献是把“按LVRT响应和有功出力进行机理预分类”与“对不具备明显聚类特征的MPPT区机组再用增强K-means细分”结合起来；增强部分包含概率初始化、KD树加速搜索和DBI自动选取聚类数，目标是在保留故障动态差异的同时减少等值机数量。

### 2. 模型、算法与实现技术

本文提出的是面向DFIG风电场的两步聚类动态等值流程。输入量主要是各风机故障前或运行过程中的有功出力及与LVRT响应相关的特征量；当前页面给出的聚类特征包括有功出力P、风速Vw、风能利用系数Cp。第一步不是直接做黑箱聚类，而是根据DFIG运行区间和LVRT响应差异，把机组划分为具有明显聚类特征的区域和不具备明显聚类特征的MPPT区域；前者可按机理直接归类，后者再进入增强K-means。第二步中，K-Means++式概率初始化用距离已有中心越远、被选为新中心概率越大的策略，降低随机初值导致局部最优的风险；KD树按维度方差选择分裂轴、递归构建索引，用于加速样本到聚类中心的最近邻搜索；WCSS作为K-means迭代目标，驱动样本分配和中心更新；DBI用类内离散度与类间距离的比值评价聚类质量，取DBI最小的K作为二次聚类数。聚类完成后，属于同一类的多台DFIG被聚合为一台等值机，容量、阻抗、集电线路及控制参数按等值规则折算，从而在PCC侧保留主要动态响应接口。

### 3. 验证、优势与不足

作者采用仿真对比验证方法。根据页面和摘要信息，测试对象为中国南方某实际风电场拓扑，包含20台2.25 MW DFIG机组、集电线路、35/220 kV升压变压器及PCC节点；仿真工具为PSCAD/EMTDC v4.6.2；故障场景是在PCC施加0.2 p.u.三相电压跌落，持续625 ms。基线包括详细模型，以及传统K-means、模糊C均值和单机等值等对比方法。评价指标包括故障期间有功、无功、电压动态曲线一致性、RMSE误差、仿真耗时、聚类搜索时间和DBI选出的聚类数。原文摘要明确报告：KD树使数据搜索时间较传统K-means约减少75%，等值模型仿真时间约减少90%，动态响应与详细模型保持高精度，对应页面给出精度超过98%和RMSE小于2%的说法。优势在于把运行机理与数据聚类结合，避免完全依赖人工指定K和初始中心，并使等值模型比单机等值更能保留机组差异。从验证范围看，结论主要支撑20台DFIG风电场、三相电压跌落和所给控制/参数条件；未看到对不对称故障、不同风场规模、不同控制策略、不同电网强度、实时仿真步长或硬件平台的系统验证。

### 4. 价值、认知与可复用场景

这项工作的核心认知价值在于：风电场动态等值不应只按地理位置或稳态功率粗略合并，而应把LVRT期间的响应机理纳入聚类准则；对具有明确运行区间特征的机组可用机理规则先分类，对MPPT区这类响应连续、边界不清的机组再用数据聚类细分。它可用于后续构建DFIG风电场多机等值模型、进行故障穿越稳定性分析、比较不同聚类等值策略，或作为知识图谱中“LVRT特性驱动等值建模”的入口。它不适合直接外推到全功率变流器风机、构网型控制、混合新能源场站、非LVRT扰动或未经验证的大规模实时EMT仿真；这些场景需要重新定义特征量、控制接口和验证指标。

### 证据边界

- 原文摘要明确给出方法组成：LVRT特性两步聚类、概率初始化、KD树、DBI自动确定聚类数，以及搜索时间约减少75%、仿真时间约减少90%。
- 测试系统、PSCAD/EMTDC版本、20台2.25 MW机组、0.2 p.u.三相电压跌落和625 ms持续时间来自当前页面抽取内容；若用于正式引用，应回到原文算例章节和图表核验。
- 精度超过98%、RMSE小于2%、DBI确定K=2等数值在当前页面中出现，但所给原文片段只覆盖摘要和引言，尚未展示对应表格或曲线，需以论文实验部分为准。
- 等值参数按容量、阻抗、电容等比例折算属于页面给出的实现说明；当前证据未展开全部参数折算公式和控制参数聚合细节。
- 与传统K-means、模糊C均值、单机等值的比较来自页面摘要式整理；缺少完整误差表、计算平台配置和统计重复次数，难以判断算法加速对更大样本规模的可推广性。
- 验证边界主要覆盖DFIG风电场的三相电压跌落LVRT场景；原文片段未显示对不对称故障、频率扰动、弱电网、不同风况分布或其他机型的验证。
<!-- deep-review:end -->
## 核心贡献

- 问题定位：本文提出一种基于低电压穿越（LVRT）特性的增强型K-means两步聚类风电场动态等值建模方法。首先，根据风机实时有功出力与LVRT动态响应特征，将全场风机划分为具有聚类特性（启动区、恒速区、恒功率区）与无聚类特性（MPPT区）两类。针对MPPT区风机，采用增强型K-means算法进行二次聚类。
- 方法机制：本文提出一种基于低电压穿越（LVRT）特性的增强型K-means两步聚类风电场动态等值建模方法。首先，根据风机实时有功出力与LVRT动态响应特征，将全场风机划分为具有聚类特性（启动区、恒速区、恒功率区）与无聚类特性（MPPT区）两类。针对MPPT区风机，采用增强型K-means算法进行二次聚类。
- 验证证据：中国南方某实际风电场拓扑，包含20台2.25 MW DFIG机组、集电线路、35/220 kV升压变压器及PCC节点；PSCAD/EMTDC (v4.6.2)；在0.2 p.u.三相电压跌落故障下，所提两步聚类等值模型动态响应与详细模型高度一致，精度>98%，计算时间减少90%。
- 量化与结论：等值模型在故障期间动态响应精度超过98%（RMSE误差<2%）；仿真计算时间大幅缩减约90%，显著提升高比例风电系统稳定性分析效率；KD树加速使聚类单次迭代耗时降至传统K-means的25.1%（数据搜索时间减少约75%）；DBI指标自动确定最优二次聚类数K=2，实现类内紧凑与类间分离的最佳平衡
- 适用边界：适用于理解本文 An enhanced K-means two-step clustering method for dynamic equivalent modeling of DFIG wind farms based on LVRT characteristics （2025） 在当前页面抽取范围内讨论的 EMT/电力系统暂态问题。

## 使用的方法

- 两步聚类法
- 增强K-means算法
- K-Means++初始化
- KD树加速搜索
- Davies-Bouldin指数
- [[动态等值建模|动态等值建模]]
- [[vector-control|矢量控制]]

## 涉及的模型

- [[dfig-model|DFIG]]
- [[wind-farm-modeling|风电场]]
- 转子侧变流器
- [[grid-side-converter|网侧变流器]]
- 电磁暂态详细模型

## 相关主题

- [[动态等值建模|动态等值建模]]
- [[lvrt|低电压穿越]]
- 风电场聚合
- [[emt-simulation|电磁暂态仿真]]
- 电力系统稳定性分析

## 主要发现

- 等值模型在故障期间与详细模型动态响应高度一致，仿真精度超过98%
- 相比传统详细模型，等效模型仿真计算时间大幅缩减约90%，显著提升效率
- 算法自动确定最佳聚类数与初始中心，有效消除人工设定参数的主观误差

## 方法细节

### 方法概述

本文提出一种基于低电压穿越（LVRT）特性的增强型K-means两步聚类风电场动态等值建模方法。首先，根据风机实时有功出力与LVRT动态响应特征，将全场风机划分为具有聚类特性（启动区、恒速区、恒功率区）与无聚类特性（MPPT区）两类。针对MPPT区风机，采用增强型K-means算法进行二次聚类。该算法融合K-Means++概率初始化策略优化初始中心选择，引入KD树索引加速高维数据最近邻搜索，并利用Davies-Bouldin指数（DBI）自适应确定最优聚类数，克服传统算法依赖人工设定参数与易陷局部最优的缺陷。最后，基于聚类结果按容量、阻抗、控制参数等比例聚合计算等值机参数，构建多机等值模型，实现故障期间动态响应的高精度复现与仿真效率的大幅提升。

### 数学公式

**公式1**: $$$$WCSS = \sum_{i=1}^{K} \sum_{x \in C_i} \| x - \mu_i \|^2$$$$

*聚类内平方和（WCSS）目标函数，用于衡量数据点到所属聚类中心的距离总和，指导K-means迭代优化。*

**公式2**: $$$$Var(X_d) = \frac{1}{a-1} \sum_{i=1}^{a} (v_{di} - \mu_d)^2$$$$

*KD树构建时的维度方差计算公式，用于选择方差最大的维度作为当前分裂轴。*

**公式3**: $$$$P(x) = \frac{D^2(x)}{\sum_{y \in T \setminus t} D^2(y)}$$$$

*K-Means++初始化中下一个聚类中心的概率选择公式，距离现有中心越远的点被选中的概率越大。*

**公式4**: $$$$DBI = \frac{1}{K} \sum_{i=1}^{K} \max_{j \neq i} \frac{S_i + S_j}{\| \mu_i - \mu_j \|}$$$$

*Davies-Bouldin指数，用于评估聚类质量，值越小表示类内越紧凑、类间分离度越高，用于自适应确定最优K值。*

**公式5**: $$$$E = \sqrt{\frac{1}{m} \sum_{i=1}^{m} \left( \frac{A_i - A_{eq,i}}{A_i} \right)^2}$$$$

*均方根误差（RMSE）评估公式，用于量化等值模型与详细模型在电气量动态响应上的偏差。*

### 算法步骤

1. 步骤1：构建KD树。计算数据集各维度方差，选取方差最大维度作为分裂轴，按中位数划分左右子树，递归构建直至子树节点数≤预设阈值（5）。

2. 步骤2：KD树加速分配。以各聚类中心为查询点在KD树中执行最近邻搜索，通过递归比较与回溯批量分配数据点，将分配阶段时间复杂度从O(a·K·D)降至O(a·log(a)·D)。

3. 步骤3：K-Means++概率初始化。随机选取首个中心，利用KD树计算剩余点到已选中心的最小欧氏距离D(x)，按概率P(x)=D²(x)/∑D²(y)随机采样下一个中心，重复至选满K个中心。

4. 步骤4：迭代聚类优化。基于WCSS目标函数分配数据点至最近中心，更新聚类中心坐标，循环迭代直至目标函数收敛或达到最大迭代次数。

5. 步骤5：DBI优选聚类数。遍历候选K值（K∈{2,3,…,a}），对每个K执行步骤3-4并计算DBI值，选取DBI最小值对应的K作为最优二次聚类数，完成最终风机分区。

### 关键参数

- **KD树节点阈值**: 5

- **聚类特征维度D**: 3

- **聚类特征变量**: 有功出力P、风速Vw、风能利用系数Cp

- **无功支撑系数K1**: 1.5~3 (依电网规范)

- **RSC电流限值ir_lim**: 1.6 p.u.

- **尾流模型推力系数CT**: 0.8

- **尾流衰减系数∂**: 0.075

- **风机叶片半径r0**: 38 m

- **故障电压跌落深度**: 0.2 p.u.

- **故障持续时间**: 625 ms

## 仿真结果

### 仿真测试

| 测试场景 | 结果描述 | 对比基线 |

|---------|---------|----------|

| 20台DFIG风电场三相电压跌落故障仿真 | 在PCC点施加0.2 p.u.三相电压跌落（持续625 ms），等值模型（Method A）的有功、无功及电压动态响应曲线与详细模型高度吻合，RMSE误差极低。 | 仿真计算时间较详细模型缩减约90%，单次聚类迭代耗时仅为传统K-means的25.1%（搜索时间减少约75%）。 |

| 不同聚类算法对比验证 | 对比传统K-means（Method B）、模糊C均值（Method C）与单机等值（Method D），所提方法在MPPT区风机分组上物理可解释性最强，EM1仅包含低风速机组，EM3聚合大部分MPPT机组。 | 聚类结果紧凑度与一致性显著优于Method B（存在功能相似机组碎片化分散）与Method C（遗漏相似机组），为高保真等值奠定基础。 |

## 量化发现

- 等值模型在故障期间动态响应精度超过98%（RMSE误差<2%）
- 仿真计算时间大幅缩减约90%，显著提升高比例风电系统稳定性分析效率
- KD树加速使聚类单次迭代耗时降至传统K-means的25.1%（数据搜索时间减少约75%）
- DBI指标自动确定最优二次聚类数K=2，实现类内紧凑与类间分离的最佳平衡
- 等值机参数聚合公式严格遵循容量、阻抗、电容的N倍/N分之一比例缩放，保证电磁暂态特性一致性

## 关键公式

### 聚类内平方和（WCSS）

$$$$WCSS = \sum_{i=1}^{K} \sum_{x \in C_i} \| x - \mu_i \|^2$$$$

*用于K-means算法迭代过程中评估聚类紧密度并更新聚类中心*

### Davies-Bouldin指数（DBI）

$$$$DBI = \frac{1}{K} \sum_{i=1}^{K} \max_{j \neq i} \frac{S_i + S_j}{\| \mu_i - \mu_j \|}$$$$

*用于自适应确定最优聚类数K，替代人工经验设定*

### 等值机参数聚合公式

$$$$X_{m,eq} = \frac{X_m}{N}, R_{s,eq} = \frac{R_s}{N}, C_{eq} = NC, Z_{T,eq} = \frac{Z_T}{N}$$$$

*将N台同聚类风机聚合为单台等值机时，电抗/电阻/变压器阻抗除以N，直流电容乘以N*

### 均方根误差（RMSE）

$$$$E = \sqrt{\frac{1}{m} \sum_{i=1}^{m} \left( \frac{A_i - A_{eq,i}}{A_i} \right)^2}$$$$

*用于量化评估等值模型与详细模型在故障期间电气量动态响应的偏差*

## 验证详情

- **验证方式**: 电磁暂态仿真对比分析
- **测试系统**: 中国南方某实际风电场拓扑，包含20台2.25 MW DFIG机组、集电线路、35/220 kV升压变压器及PCC节点
- **仿真工具**: PSCAD/EMTDC (v4.6.2)
- **验证结果**: 在0.2 p.u.三相电压跌落故障下，所提两步聚类等值模型动态响应与详细模型高度一致，精度>98%，计算时间减少90%。聚类结果物理可解释性强，显著优于传统K-means与模糊C均值法，验证了算法在LVRT工况下的有效性与工程实用性。

## 适用边界

### 适用条件

- 适用于理解本文 `An enhanced K-means two-step clustering method for dynamic equivalent modeling of DFIG wind farms based on LVRT characteristics`（2025） 在当前页面抽取范围内讨论的 EMT/电力系统暂态问题。
- 适用于以 两步聚类法、增强k-means算法、k-means-初始化 为核心的建模、仿真、等值、控制或稳定性分析场景；具体对象以原文算例和页面“涉及的模型”为准。
- 可作为知识图谱中的方法定位和文献入口，尤其用于追踪：提出基于有功出力与LVRT响应的两步聚类框架，实现风机精准预分类

### 失效边界

- 不应外推到原文未覆盖的拓扑、控制策略、故障类型、频率范围、硬件平台或实时步长。
- 不应把页面中的“提高、显著、快速、准确”等概括性表述当作定量结论；只有“量化发现”和原文表图可核验的数字才可用于比较。
- 若页面作者、期刊、摘要或验证字段仍不完整，本页只能作为待复核文献入口，不能作为最终证据页引用。

### 关键假设

- 页面内容假设当前 PDF 抽取文本与 frontmatter 的 `sources` 指向同一篇论文。
- 方法结论默认受原文仿真工具、测试系统、参数设置、采样步长和对比基线约束。
- 当前边界层为保守整理：未从原文直接核验的内容不得升级为确定结论。

### 证据缺口

- 具体适用范围仍以原文算例、参数表和验证场景为准，当前页面不应外推到未验证系统。
- 源文件路径：`["EMT_Doc/07&08/Xu et al. - 2026 - An enhanced K-means two-step clustering method for dynamic equivalent modeling of DFIG wind farms ba.pdf"]`；需要深修时应优先核对该 PDF 的首页、摘要、方法和实验表图。
