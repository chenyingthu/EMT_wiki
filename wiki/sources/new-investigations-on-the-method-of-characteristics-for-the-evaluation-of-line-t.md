---
title: "New investigations on the method of characteristics for the evaluation of line transients"
type: source
authors: ['T. Kauffmann']
year: 2018
journal: "Electric Power Systems Research"
tags: ['emt']
created: "2026-04-13"
sources: ["EMT_Doc/27&28/New investigations on the method of characteristics for the evaluation of line transients.pdf"]
---

# New investigations on the method of characteristics for the evaluation of line transients

**作者**: T. Kauffmann
**年份**: 2018
**来源**: `27&28/New investigations on the method of characteristics for the evaluation of line transients.pdf`

## 摘要

本文提出了一种基于特征线法(Method of Characteristics, MoC)的频变输电线路与电缆暂态分析方法。核心创新在于通过矢量拟合(Vector Fitting, VF)技术对串联阻抗矩阵进行有理函数逼近，将频域线路方程转换至时域，并利用模态变换将多导体系统解耦为独立的模态分量。研究重点在于探讨消除空间离散化的可行性：通过变量替换和线性化近似建立线路两端直接关系，避免传统MoC所需的空间网格划分。然而，理论分析表明这种消除会引入显著的数值误差，特别是在大积分步长（由模态延迟决定）条件下会导致精度下降和数值不稳定。


<!-- deep-review:start -->
## 研究解读

### 1. 需求、对象、挑战与贡献

EMT仿真中，输电线路和电缆的频变参数会使暂态响应依赖宽频带传播特性。主流做法是行波类频变模型，如ULM，在频域拟合传播函数H和特征导纳Yc，再用递归卷积进入时域。MoC则直接把时域偏微分方程沿特征线化为常微分方程，但通常必须同时离散时间和空间；对均匀线路而言，这使它比行波模型低效。本文研究对象是均匀多导体线路及电缆的频变暂态MoC建模，核心问题是：能否利用模态波速和端点关系取消空间离散化，从而让MoC具备比行波模型更少卷积、更易拟合的优势。难点在于频变串联阻抗引入卷积项，且端点直接关系需要对特征线上的微分方程做变量替换和线性化。论文的贡献不是简单提出更快模型，而是系统澄清这种“无空间离散MoC”的可行性与失败原因：空间离散可在形式上消除，但端点ODE线性化误差和由模态延迟决定的大积分步长会限制精度并可能导致数值不稳定，因此实际仍需线路分段。

### 2. 模型、算法与实现技术

论文采用的建模入口是频域线路方程：电压、电流沿线变化由串联阻抗矩阵Z(s)和并联导纳矩阵Y(s)决定。实现上先对串联阻抗元素进行有理函数拟合，而不是像ULM那样拟合传播函数H和特征导纳Yc；这样做的动机是Z(s)元素通常更平滑。拟合形式把Z(s)分解为直流电阻、常数高频电感项D以及若干极点—留数项。经拉普拉斯反变换后，频变部分变成由指数核表示的卷积，可用递归方式更新。随后通过模态变换把多导体相域变量转换为模态电压、电流，使D和C的组合给出各模态传播速度；在x-t平面上，沿正、反向特征线，原偏微分方程变为关于模态端变量的常微分方程。无空间离散版本的关键步骤是利用模态传播延迟，把一端某时刻变量与另一端延迟时刻变量联系起来，并用线性化近似把沿特征线的ODE积分成端口代数关系。输入是线路/电缆频变参数、几何或等效RLC数据及端口网络条件；接口量是线路两端相域或模态域电压、电流；输出是EMT时间步中的端口历史电流/电压关系及暂态波形。

### 3. 验证、优势与不足

从给定原文看，作者通过理论推导和数值实验讨论MoC取消空间离散化的后果，并把其动机与行波类模型，特别是ULM，进行方法层面对照。验证对象包括输电线路暂态，并扩展到电缆暂态；重点指标不是某个单一误差百分比，而是数值精度、稳定性、卷积计算量、拟合对象复杂度以及是否需要线路分段。论文指出该路线的潜在优势有两点：一是每个端口只需处理一个卷积，而行波模型通常涉及传播函数和特征导纳相关的卷积；二是MoC拟合的是串联阻抗元素，通常比H和Yc更平滑，拟合问题在形式上更容易。验证得到的主要负面结论是：为了把线路两端直接关联，必须对特征线ODE做线性化，而当空间离散被取消时，积分步长受整段线路模态延迟约束，可能过大；这会放大近似误差并引发不稳定。改进办法是把线路细分，缩短每段延迟和积分步长，但这会削弱原本的计算效率优势。给定摘录未报告可核验的具体误差数值、运行时间、测试线路参数或图表数据，因此不能据此声称该方法在某算例中定量优于或劣于ULM。

### 4. 价值、认知与可复用场景

这项工作的价值在于把一个看似能超越行波模型的MoC变体从“形式可行”推进到“数值机制可解释”。它说明：EMT线路模型的效率不能只看卷积次数或拟合对象是否平滑，还必须看端口关系建立过程中引入的积分步长和线性化误差。该结论可供后续研究复用，例如评估无网格MoC、频变线路递归卷积、线路分段策略、模态变换质量和实时EMT线路模型时，用作误差来源分析框架。它适合支持“为什么均匀线路仍常用行波模型/ULM”的背景说明，也适合指导MoC在非线性、非均匀或需沿线变量的场景中谨慎使用。不适合外推为所有MoC都不稳定，也不能外推到原文未验证的控制器交互、故障类型、实时硬件平台或特定电缆工程参数。

### 证据边界

- 来自原文的明确证据：论文研究MoC取消均匀线路空间离散化的可能性，并扩展讨论电缆暂态；结论是形式上可行但数值精度有限且可能不稳定。
- 来自原文的明确证据：主要误差源被归因于为建立线路两端关系而对微分方程进行线性化；若要提高精度，需要将线路细分。
- 来自原文的明确证据：作者将MoC路线与行波类模型/ULM的建模思想对照，指出MoC潜在优势是卷积数量较少、拟合串联阻抗元素而非H和Yc。
- 据方法机制推断但摘录未给出完整细节：递归卷积、模态端口关系和线路分段会影响计算量与稳定性；具体实现公式和离散格式需查全文方法部分确认。
- 缺少关键信息：给定摘录未提供具体测试系统参数、仿真工具、误差曲线、运行时间或稳定性判据，因此不能形成可核验的定量性能结论。
- 适用边界：摘录聚焦均匀线路/电缆的频变暂态；不应直接外推到非均匀线路、强非线性元件耦合、复杂网络拓扑或实时仿真硬件表现。
<!-- deep-review:end -->
## 核心贡献

- 问题定位：本文提出了一种基于特征线法(Method of Characteristics, MoC)的频变输电线路与电缆暂态分析方法。核心创新在于通过矢量拟合(Vector Fitting, VF)技术对串联阻抗矩阵进行有理函数逼近，将频域线路方程转换至时域，并利用模态变换将多导体系统解耦为独立的模态分量。
- 方法机制：本文提出了一种基于特征线法(Method of Characteristics, MoC)的频变输电线路与电缆暂态分析方法。核心创新在于通过矢量拟合(Vector Fitting, VF)技术对串联阻抗矩阵进行有理函数逼近，将频域线路方程转换至时域，并利用模态变换将多导体系统解耦为独立的模态分量。研究重点在于探讨消除空间离散化的可行性：通过变量替换和线性化近似建立线路两端直接关系，避免传统MoC所需的空间网格划分。
- 验证证据：理论分析与数值实验验证相结合，对比分析消除空间离散化与保留空间离散化的MoC实现，以及与基于行波的通用线路模型(ULM)的精度和效率；均匀多导体输电线路和电缆系统，重点考察频变参数（集肤效应）影响下的暂态响应；EMT-type仿真程序框架，基于MATLAB或类似科学计算环境实现矢量拟合(VF)算法和MoC数值求解器
- 量化与结论：频率拟合范围覆盖至少9个数量级（从mHz到MHz），使用100-200个频率采样点可保证有理函数逼近的精度；模态速度必须严格小于光速（3×10^8 m/s），若拟合结果导致速度接近或超过光速，需固定矩阵D的高频值；消除空间离散化要求的积分步长等于线路模态延迟τ（线路长度/模态速度），对于长线路或高波速模态，该步长过大（可达毫秒级），导致数值精度显著下降；
- 适用边界：适用于理解本文 New investigations on the method of characteristics for the evaluation of line transients （2018） 在当前页面抽取范围内讨论的 EMT/电力系统暂态问题。；

## 使用的方法

- [[特征线法-moc|特征线法(MoC)]]
- [[矢量拟合-vf|矢量拟合(VF)]]
- [[methods/rational-function-approximation|有理函数逼近]]
- [[模态变换|模态变换]]
- [[时域卷积计算|时域卷积计算]]
- [[空间离散化消除|空间离散化消除]]

## 涉及的模型

- [[models/transmission-line-model|输电线路]]
- [[电缆|电缆]]
- [[频变线路模型|频变线路模型]]
- [[串联阻抗矩阵|串联阻抗矩阵]]
- [[并联导纳矩阵|并联导纳矩阵]]

## 相关主题

- [[topics/emt-simulation|电磁暂态仿真]]
- [[频变参数建模|频变参数建模]]
- [[行波模型对比|行波模型对比]]
- [[数值稳定性分析|数值稳定性分析]]
- [[线路暂态分析|线路暂态分析]]

## 主要发现

- 消除空间离散化会引入线性化近似误差，导致模型数值精度受限且易失稳
- 模态延迟要求的大积分步长是引发数值问题的主因，需线路分段以改善精度
- 线路分段虽能提升精度与稳定性，但完全抵消了该方法相比行波模型的效率优势

## 方法细节

### 方法概述

本文提出了一种基于特征线法(Method of Characteristics, MoC)的频变输电线路与电缆暂态分析方法。核心创新在于通过矢量拟合(Vector Fitting, VF)技术对串联阻抗矩阵进行有理函数逼近，将频域线路方程转换至时域，并利用模态变换将多导体系统解耦为独立的模态分量。研究重点在于探讨消除空间离散化的可行性：通过变量替换和线性化近似建立线路两端直接关系，避免传统MoC所需的空间网格划分。然而，理论分析表明这种消除会引入显著的数值误差，特别是在大积分步长（由模态延迟决定）条件下会导致精度下降和数值不稳定。

### 数学公式

**公式1**: $$$-\frac{dV(x,s)}{dx} = Z(s)I(x,s), \quad -\frac{dI(x,s)}{dx} = Y(s)V(x,s)$$$

*频域分布参数线路方程，s为拉普拉斯算子，Z(s)为串联阻抗矩阵，Y(s)为并联导纳矩阵*

**公式2**: $$$Z(s) \approx R_{DC} + s\left(D + \sum_{i=1}^{N} \frac{K_i}{s-p_i}\right)$$$

*串联阻抗矩阵的部分分式展开有理逼近，RDC为直流电阻，D为常数电感矩阵，Ki为留数矩阵，pi为极点*

**公式3**: $$$Y(s) = sC$$$

*并联导纳矩阵近似，仅考虑常数电容矩阵C（忽略电导和频变效应）*

**公式4**: $$$\frac{\partial v(x,t)}{\partial x} + C\frac{\partial v(x,t)}{\partial t} = 0$$$

*时域电压方程，由频域转换得到的偏微分方程*

**公式5**: $$$\frac{\partial v(x,t)}{\partial x} + D\frac{\partial i(x,t)}{\partial t} + R_h i(x,t) + \xi(x,t) = 0$$$

*时域电流方程，包含卷积项ξ(x,t)表征频变特性*

**公式6**: $$$T_V^{-1}DCT_V = \Lambda^{-2}, \quad \Lambda = \text{diag}(\lambda_1, \ldots, \lambda_n)$$$

*模态变换，将线路参数对角化，得到模态速度λi*

**公式7**: $$$\frac{dv_m}{dt} + Z_w\frac{di_m}{dt} + \lambda R_m i_m + \lambda \xi_m = 0$$$

*沿正向特征线的模态域常微分方程（反向传播类似，符号相反）*

### 算法步骤

1. 频率依赖参数拟合：首先提取直流电阻矩阵RDC（使用极低频采样），构造矩阵(Z(s)-RDC)/s，应用矢量拟合(VF)方法识别矩阵各元素公共极点pi；建立超定线性方程组求解留数矩阵Ki和常数电感矩阵D，确保拟合覆盖宽频带（几mHz至几MHz）；验证DC乘积产生的模态速度是否小于光速（3×10^8 m/s），否则需固定D值。

2. 时域转换与卷积计算：将拟合后的有理函数代入线路方程，通过拉普拉斯逆变换转换至时域；计算脉冲响应h(t)=ΣKie^(pit)，建立卷积积分表达式；将卷积项与历史电流关联，准备递归计算。

3. 模态解耦与特征线建立：通过变换矩阵TV和TI将相域电压电流转换为模态变量vm和im；计算模态速度矩阵Λ=diag(λ1,...,λn)，确定特征线斜率dx/dt=±λi；建立(x,t)平面上的特征曲线方程。

4. 空间离散化消除与数值求解：尝试以线路两端为边界，沿特征线应用梯形法线性化ODEs，消除空间离散化；分析线性化近似误差对数值精度的影响；评估大积分步长（等于模态延迟τ）下的数值稳定性；得出结论：必须对线路进行分段以控制误差，但这会抵消计算效率优势。

### 关键参数

- **fitting_frequency_range**: 几毫赫兹至几兆赫兹 (mHz to MHz)

- **frequency_samples**: 100-200个样本点

- **poles_number**: N（根据拟合精度需求确定）

- **modal_velocity_constraint**: 必须小于光速 (c < 3×10^8 m/s)

- **integration_step**: 由模态延迟τ决定，等于线路长度除以模态速度

- **line_subdivision**: 为保证精度和稳定性所需的线路分段数

## 仿真结果

### 仿真测试

| 测试场景 | 结果描述 | 对比基线 |

|---------|---------|----------|

| 均匀输电线路暂态分析（消除空间离散化） | 应用提出的MoC方法消除空间离散化后，观察到数值精度受限现象。由于梯形法线性化近似和单个大积分步长（等于最快模态的传输延迟）的使用，模型在仿真高频暂态分量时产生显著误差，且可能出现数值振荡或不稳定行为。 | 与带空间离散化的传统MoC相比，消除离散化后单步长过大导致误差不可控；与行波模型(ULM)相比，虽然减少了卷积计算量（每端仅需一次卷积而非两次），但因必须采用线路分段以保持精度，最终计算效率优势完全丧失 |

| 电缆系统暂态分析 | 将方法扩展应用于电缆暂态分析，验证了频变参数有理拟合在电缆建模中的适用性。发现电缆的频变特性同样要求精细的空间离散化或极小的积分步长以保证数值稳定性。 | 传统行波模型需拟合传播函数H和特征导纳Yc，而本文方法仅需拟合更平滑的串联阻抗元素，理论上有拟合优势，但数值实现中的线性化误差抵消了这一优势 |

## 量化发现

- 频率拟合范围覆盖至少9个数量级（从mHz到MHz），使用100-200个频率采样点可保证有理函数逼近的精度
- 模态速度必须严格小于光速（3×10^8 m/s），若拟合结果导致速度接近或超过光速，需固定矩阵D的高频值
- 消除空间离散化要求的积分步长等于线路模态延迟τ（线路长度/模态速度），对于长线路或高波速模态，该步长过大（可达毫秒级），导致数值精度显著下降
- 串联阻抗矩阵有理拟合的留数矩阵Ki和极点pi需满足：R_h = R_DC + ΣKi 为高频电阻，且卷积核h(t)=ΣK_i e^(p_i t)需保证数值稳定性
- 当线路被细分为M段时，每段延迟为τ/M，积分步长减小M倍，数值误差按O((Δt)^2)减小，但计算量增加M倍，与行波模型效率相当

## 关键公式

### 串联阻抗矢量拟合有理逼近式

$$$Z(s) \approx R_{DC} + sD + s\sum_{i=1}^{N} \frac{K_i}{s-p_i}$$$

*用于将频变串联阻抗矩阵转换为时域递归卷积形式，是MoC建模的基础*

### 特征线方程

$$$\frac{dx}{dt} = \pm \lambda_i, \quad i=1,\ldots,n$$$

*定义了模态波在x-t平面上的传播轨迹，是消除空间离散化的理论基础*

### 模态域特征线常微分方程

$$$\frac{dv_m}{dt} \pm Z_w\frac{di_m}{dt} + \lambda R_m i_m + \lambda \xi_m = 0$$$

*沿特征线建立的ODE，用于数值积分求解线路两端暂态过程*

## 验证详情

- **验证方式**: 理论分析与数值实验验证相结合，对比分析消除空间离散化与保留空间离散化的MoC实现，以及与基于行波的通用线路模型(ULM)的精度和效率
- **测试系统**: 均匀多导体输电线路和电缆系统，重点考察频变参数（集肤效应）影响下的暂态响应
- **仿真工具**: EMT-type仿真程序框架，基于MATLAB或类似科学计算环境实现矢量拟合(VF)算法和MoC数值求解器
- **验证结果**: 验证了消除空间离散化虽在理论上有计算效率优势（减少卷积数量、拟合更平滑参数），但因线性化近似误差和大积分步长导致的数值不稳定问题，实际应用中必须采用线路分段，这使得该方法相比传统行波模型(ULM)无显著优势。确认了模态延迟决定的积分步长是限制精度的关键因素。

## 适用边界

### 适用条件

- 适用于理解本文 `New investigations on the method of characteristics for the evaluation of line transients`（2018） 在当前页面抽取范围内讨论的 EMT/电力系统暂态问题。
- 适用于以 特征线法-moc、矢量拟合-vf、有理函数逼近 为核心的建模、仿真、等值、控制或稳定性分析场景；具体对象以原文算例和页面“涉及的模型”为准。
- 可作为知识图谱中的方法定位和文献入口，尤其用于追踪：提出基于矢量拟合的串联阻抗矩阵有理函数拟合流程，实现宽频带参数高效拟合

### 失效边界

- 不应外推到原文未覆盖的拓扑、控制策略、故障类型、频率范围、硬件平台或实时步长。
- 不应把页面中的“提高、显著、快速、准确”等概括性表述当作定量结论；只有“量化发现”和原文表图可核验的数字才可用于比较。
- 若页面作者、期刊、摘要或验证字段仍不完整，本页只能作为待复核文献入口，不能作为最终证据页引用。

### 关键假设

- 页面内容假设当前 PDF 抽取文本与 frontmatter 的 `sources` 指向同一篇论文。
- 方法结论默认受原文仿真工具、测试系统、参数设置、采样步长和对比基线约束。
- 当前边界层为保守整理：未从原文直接核验的内容不得升级为确定结论。

### 证据缺口

- 摘要抽取偏短，可能没有覆盖完整问题定义、方法和验证结论。
- 源文件路径：`["EMT_Doc/27&28/New investigations on the method of characteristics for the evaluation of line transients.pdf"]`；需要深修时应优先核对该 PDF 的首页、摘要、方法和实验表图。
