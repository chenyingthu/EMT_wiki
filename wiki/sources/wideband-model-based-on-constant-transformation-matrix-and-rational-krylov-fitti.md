---
title: "Wideband model based on constant transformation matrix and rational Krylov fitting"
type: source
authors: ['Emmanuel Francois']
year: 2023
journal: "Electric Power Systems Research"
tags: ['emt']
created: "2026-04-13"
sources: ["EMT_Doc/40/Francois 等 - 2023 - Wideband model based on constant transformation matrix and rational Krylov fitting.pdf"]
---

# Wideband model based on constant transformation matrix and rational Krylov fitting

**作者**: Emmanuel Francois
**年份**: 2023
**来源**: `40/Francois 等 - 2023 - Wideband model based on constant transformation matrix and rational Krylov fitting.pdf`

## 摘要

本文提出基于恒定变换矩阵(Constant T)的宽带模型(CTWB)，通过模态分解将传播函数分解为模态形式，使用有理Krylov拟合(RKF)或向量拟合(VF)在模域进行拟合，再利用恒定的实变换矩阵将结果转换回相域。该方法允许处理多导体传输线，同时研究了恒定对系统无源性的影响，并首次证明恒定的选择会内在地导致特定频段的无源性违反。验证信息：三导体束架空传输线（Three-conductor bundle），包含地线，具体参数包括：地线半径4.75mm、电阻3.75Ohm/km；导体半径15.29mm、电阻0.0701Ohm/km；束半径230.09mm；基于EMT仿真原理的数值实现（文中提及ULM和FDCM的实现框架，使用精确状态空间离散化和两步插值方法）


<!-- deep-review:start -->
## 研究解读

### 1. 需求、对象、挑战与贡献

EMT仿真需要把多导体架空线/电缆的频率相关传播函数H和特性导纳Yc转成可时域递推的有理模型。传统ULM/FDCM通常在相域拟合矩阵函数，精度和无源性较可控，但当导体数很多时，矩阵元素多、拟合阶数和递归卷积数量都会增加。另一类“恒定实变换矩阵T”的思路先把传播函数模态解耦，分别拟合各模态，再用固定T回到相域，计算上有吸引力；难点在于真实线路的模态变换矩阵随频率变化，强行固定会改变相域传播矩阵的物理性质，尤其可能破坏由H和Yc组成的端口无源性。本文的贡献不是简单提出一个新线路模型，而是把constant T选项纳入WB模型框架，系统考察用部分分式展开拟合模态传播函数时的后果；首次指出并展示：constant T的频率点选择本身可能使线路节点导纳矩阵在某些频率区间出现负特征值，即内在导致无源性违反。同时，论文评估了新近的rational Krylov fitting（RKF）相对vector fitting（VF）在该模态拟合任务中的阶数与精度表现，并把传统基于Bode拟合的constant T频变线路模型放到相域误差视角下解释其时域不准确来源。

### 2. 模型、算法与实现技术

本文讨论的CTWB流程以多导体线路的单位长度阻抗Z(s)、导纳Y(s)为输入，计算传播函数H=exp(-ΓL)和特性导纳Yc，其中Γ可由YZ或ZY的矩阵平方根得到。核心接口量仍是EMT线路模型两端的相域电压、电流，例如I0−YcV0=−H(IL+YcVL)这类行波关系；但识别阶段转入模域：对H在各频率样本做特征分解H=T Hm T^{-1}，把复杂矩阵拟合问题拆为若干标量模态传播函数Hi。constant T模型不是使用随频率变化的T(ω)，而是在某一频率样本选取特征向量矩阵，经相位旋转并取实部形成固定实矩阵Tconst；随后每个模态Hi用部分分式展开拟合，形式上包含极点、留数和时延项，如Σ r_k/(s−p_k)·e^{-sτi}。拟合算法比较了VF与RKF：VF是EMT宽带模型中常用的有理拟合工具，RKF则通过Krylov子空间近似生成有理模型。拟合后的对角模态模型Hmodefit再由Hfit=Tconst Hmodefit Tconst^{-1}重构到相域。时域实现沿用WB模型的数值框架：把有理函数转成离散状态空间，采用分段线性输入假设下的精确解，并可用两步插值和DC校正改善数值误差与稳定性。机制上，constant T降低了模态间耦合和递归卷积负担，但其关键风险也正来自把频率相关的模态基冻结为一个固定基。

### 3. 验证、优势与不足

作者采用理论分析加数值算例验证。原文给出的算例包括一条三导体束架空传输线并包含地线，页面抽取到的几何和电阻参数包括地线半径4.75 mm、地线直流电阻3.75 Ω/km、导体半径15.29 mm、导体直流电阻0.0701 Ω/km、束半径230.09 mm。验证对象主要是传播函数H的拟合，因为它比Yc更复杂；同时也讨论可将Yc直接在相域拟合，以观察对暂态波形的影响。基线包括WB/ULM/FDCM中常用的相域宽带建模思想、模态constant T下的VF拟合、RKF拟合，以及传统FD模型中基于Bode拟合的constant T方法。指标包括频域拟合误差、模型阶数或极点数量、由H和Yc生成的线路节点导纳矩阵特征值是否出现负值，以及开关暂态等时域波形的一致性。优势主要体现在两点：第一，模态解耦使constant T WB模型可面向较多导体线路，理论上减少相域矩阵拟合和时域卷积负担；第二，算例表明RKF可在保持准确拟合的同时使用比VF更低阶的模型。但原文摘要和所给抽取内容未报告可核验的具体阶数、误差百分比、运行时间或波形误差数值，因此不能把“更低阶、准确”扩展为通用定量结论。限制也很明确：constant T的无源性依赖选取频率，并非所有选择都会失稳，但某些选择会内在破坏无源性；验证范围主要是给定线路算例和频域/时域测试，未证明对所有电缆、强不对称多导体系统、极端频带或实时仿真步长均成立。

### 4. 价值、认知与可复用场景

这项工作最有价值的认知是：constant T不是一个只影响拟合误差的数值近似，它会改变相域传播函数的物理可实现性，甚至使原本无源的线路端口模型在某些频率区间变为非无源。因此，在EMT线路建模中，选择模态基和选择有理拟合算法同等重要。该页适合作为后续研究constant T宽带线路模型、RKF/VF拟合、ULM/FDCM实现、线路模型无源性检查与修正、多导体线路降阶仿真的入口。工程上，它可提醒用户在采用模态解耦以降低计算负担时，必须检查由H和Yc构成的节点导纳矩阵特征值，而不能只看单个模态拟合曲线。不适合将本文外推为“RKF总是优于VF”或“constant T不可用”；更准确的结论是：在作者算例中RKF表现出较低阶优势，而constant T需要按选频和无源性验证谨慎使用。

### 证据边界

- 来自原文的明确结论：constant T的选取频率可能使由H和Yc构成的线路节点导纳矩阵在某些频率样本出现负特征值，从而违反无源性；并且并非所有constant T选择都会导致该问题。
- 来自原文的明确结论：论文重点拟合传播函数H，因为其复杂度高于特性导纳Yc；Yc可在相域直接拟合并用于考察暂态波形影响。
- 来自原文的明确结论：算例显示RKF相对VF可得到准确且较低阶的模态有理模型；但所给抽取文本未提供可核验的极点数、误差值或运行时间。
- 测试系统参数中三导体束线路、地线和导体半径/电阻等来自当前页面抽取；若需引用完整几何布置、线路长度、频率采样范围和开关工况，应回到原PDF表图核验。
- 关于减少递归卷积和适合多导体线路是由constant T模态解耦机制和原文说明支持的优势，但具体计算加速比、内存节省和实时仿真性能在抽取文本中未报告可核验数值。
- 从验证范围看，本文结论不应直接外推到所有电缆模型、换流器连接网络、非线性元件耦合场景、任意步长离散化或未经无源性检查的工程模型。
<!-- deep-review:end -->
## 核心贡献

- 问题定位：本文提出基于恒定变换矩阵(Constant T)的宽带模型(CTWB)，通过模态分解将传播函数分解为模态形式，使用有理Krylov拟合(RKF)或向量拟合(VF)在模域进行拟合，再利用恒定的实变换矩阵将结果转换回相域。该方法允许处理多导体传输线，同时研究了恒定对系统无源性的影响，并首次证明恒定的选择会内在地导致特定频段的无源性违反。
- 方法机制：本文提出基于恒定变换矩阵(Constant T)的宽带模型(CTWB)，通过模态分解将传播函数分解为模态形式，使用有理Krylov拟合(RKF)或向量拟合(VF)在模域进行拟合，再利用恒定的实变换矩阵将结果转换回相域。该方法允许处理多导体传输线，同时研究了恒定对系统无源性的影响，并首次证明恒定的选择会内在地导致特定频段的无源性违反。
- 验证证据：三导体束架空传输线（Three-conductor bundle），包含地线，具体参数包括：地线半径4.75mm、电阻3.75Ohm/km；导体半径15.29mm、电阻0.0701Ohm/km；束半径230.09mm；基于EMT仿真原理的数值实现（文中提及ULM和FDCM的实现框架，使用精确状态空间离散化和两步插值方法）；RKF在CTWB框架下比VF生成更低阶且精确的模型；
- 量化与结论：恒定变换矩阵的选择频率默认为1000 Hz，此时在特定测试案例中观察到无源性违反；RKF算法生成的有理模型阶数（极点总数）显著低于VF算法，在保持相同精度的前提下实现模型降阶；传统FD模型基于Bode拟合在相域的拟合误差显著大于现代方法（VF/RKF），这直接解释了其时域仿真的不准确性；三导体束案例中的导体直流电阻为0.0701 Ohm/km，地线直流电阻为3.
- 适用边界：适用于理解本文 Wideband model based on constant transformation matrix and rational Krylov fitting （2023） 在当前页面抽取范围内讨论的 EMT/电力系统暂态问题。；适用于以 vector-fitting 为核心的建模、仿真、等值、控制或稳定性分析场景；

## 使用的方法

- [[vector-fitting]]

## 涉及的模型

- [[transmission-line]]
- [[cable]]

## 相关主题

- [[passivity]]
- [[frequency-dependent]]

## 主要发现

- 恒定变换矩阵的引入会导致最终有理模型在特定频段违反无源性
- 有理Krylov拟合在模型精度和阶数控制上均优于传统的向量拟合算法
- 基于Bode拟合的传统恒定T模型在相域拟合精度较差，直接导致其时域仿真结果不准确

## 方法细节

### 方法概述

本文提出基于恒定变换矩阵(Constant T)的宽带模型(CTWB)，通过模态分解将传播函数$H$分解为模态形式，使用有理Krylov拟合(RKF)或向量拟合(VF)在模域进行拟合，再利用恒定的实变换矩阵$T_{const}$将结果转换回相域。该方法允许处理多导体传输线，同时研究了恒定$T$对系统无源性的影响，并首次证明恒定$T$的选择会内在地导致特定频段的无源性违反。

### 数学公式

**公式1**: $$$Y_c = \Gamma Z^{-1}$, where $\Gamma = \sqrt{YZ}$$$

*特性导纳矩阵定义，其中$Y$和$Z$分别为单位长度并联导纳和串联阻抗矩阵*

**公式2**: $$$H = e^{-\Gamma L}$$$

*传播矩阵定义，$L$为线路长度*

**公式3**: $$$H = T H_m T^{-1}$$$

*传播矩阵的模态分解，$T$为特征向量矩阵，$H_m$为对角模态传播矩阵*

**公式4**: $$$H_m = \text{diag}[e^{\lambda_1}, e^{\lambda_2}, \cdots, e^{\lambda_N}]$$$

*模态传播函数对角矩阵，$\lambda_i$为$-\sqrt{YZ}L$的特征值*

**公式5**: $$$\hat{H}_i = \sum_{k=1}^{M_i} \frac{r_k}{s - p_{i,k}} e^{-s\tau_i} \cong H_i$$$

*第$i$个模态的有理函数拟合形式，$M_i$为极点数，$\tau_i$为时延*

**公式6**: $$$H_{fit} = T_{const} H_{modefit} T_{const}^{-1}$$$

*使用恒定变换矩阵将拟合后的模态函数转换回相域*

**公式7**: $$$I_0 - Y_c V_0 = -H(I_L + Y_c V_L)$$$

*线路首端电流电压关系（相域）*

**公式8**: $$$e = \|H - T_{const} H_{modefit} T_{const}^{-1}\|_2$$$

*恒定$T$拟合误差计算，用于选择最优的常数变换矩阵*

### 算法步骤

1. 计算频域样本：在所需频带内计算多导体线路的单位长度参数矩阵$Y$和$Z$，进而计算特性导纳$Y_c$和传播矩阵$H$的频域样本

2. 模态分解：对每个频率样本计算传播矩阵$H$的特征值分解，得到模态传播矩阵$H_m = \text{diag}[e^{\lambda_1},...,e^{\lambda_N}]$和变换矩阵$T$

3. 选择恒定变换矩阵：在$N_\omega$个频率样本中选择特定频率点（如1000 Hz）处的变换矩阵$T_i$，取其实部并旋转以最小化虚部，得到$T_{const}$

4. 提取模态函数：从$H_m$中提取各模态传播函数$H_i = e^{\lambda_i}$，并优化时延$\tau_i$以减少拟合误差

5. 有理函数拟合：使用RKF或VF算法将每个模态函数$H_i$拟合为部分分式展开形式$\sum_{k=1}^{M_i} \frac{r_k}{s-p_{i,k}}e^{-s\tau_i}$

6. 相域重构：利用$T_{const}$构建对角矩阵$H_{modefit} = \text{diag}[\hat{H}_1,...,\hat{H}_N]$，计算相域传播矩阵$H_{fit} = T_{const} H_{modefit} T_{const}^{-1}$

7. 无源性验证：检查由$H_{fit}$和$Y_c$构建的节点导纳矩阵特征值，验证在特定频段是否存在负特征值（无源性违反）

8. 时域离散化：将有理近似转换为离散状态空间形式，采用分段线性输入假设的精确解，应用两步插值最小化积分误差，可选进行DC校正

### 关键参数

- **T_const_selection_frequency**: 1000 Hz（默认），用户可手动设置或选择使误差$e$最小的频率点

- **ground_wire_radius**: 4.75 mm

- **ground_wire_DC_resistance**: 3.75 Ohm/km

- **bundle_radius**: 230.09 mm

- **bundle_angle**: 0°

- **conductor_radius**: 15.29 mm

- **conductor_DC_resistance**: 0.0701 Ohm/km

- **number_of_conductors**: 3（三导体束）

- **fitting_order_VF**: 各模态极点数$M_i$的总和（具体数值依案例而定）

- **fitting_order_RKF**: 各模态极点数$M_i$的总和（显著低于VF）

## 仿真结果

### 仿真测试

| 测试场景 | 结果描述 | 对比基线 |

|---------|---------|----------|

| Case-1三导体束传输线 | 对比RKF与VF在CTWB中的拟合性能。RKF生成的模型极点数显著少于VF，同时保持相当的精度。在时域开关瞬态仿真中，CTWB能够精确生成电压波形 | RKF比VF生成更低阶的模型（less order），具体阶数比未明确给出但描述为'remarkably less poles'；传统FD模型（基于Bode拟合）在相域表现出较差的拟合精度 |

| 无源性验证测试 | 验证恒定$T$对系统无源性的影响。当选择特定频率点（如1000Hz）作为$T_{const}$时，重构的相域传播函数在某些频段导致节点导纳矩阵出现负特征值 | 使用恒定$T$内在地违反系统无源性，而非常规WB模型（ULM/FDCM）在相域直接拟合可避免此问题 |

## 量化发现

- 恒定变换矩阵$T_{const}$的选择频率默认为1000 Hz，此时在特定测试案例中观察到无源性违反
- RKF算法生成的有理模型阶数（极点总数）显著低于VF算法，在保持相同精度的前提下实现模型降阶
- 传统FD模型基于Bode拟合在相域的拟合误差显著大于现代方法（VF/RKF），这直接解释了其时域仿真的不准确性
- 三导体束案例中的导体直流电阻为0.0701 Ohm/km，地线直流电阻为3.75 Ohm/km
- 模态分解中，各模态$H_i$的拟合采用部分分式展开，极点数$M_i$取决于所选算法（RKF通常需要更少的极点）
- 无源性违反的程度和频段取决于$T_{const}$所选取的具体频率点，并非所有频率选择都会导致无源性丧失

## 关键公式

### 模态分解方程

$$$H = T H_m T^{-1}$$$

*将相域传播矩阵分解为模态形式，是恒定T方法的基础*

### 恒定T相域重构方程

$$$H_{fit} = T_{const} H_{modefit} T_{const}^{-1}$$$

*使用恒定实变换矩阵将拟合后的模态函数转换回相域，此步骤可能引入无源性违反*

### 模态有理拟合方程

$$$\hat{H}_i = \sum_{k=1}^{M_i} \frac{r_k}{s - p_{i,k}} e^{-s\tau_i}$$$

*使用RKF或VF对每个传播模态进行有理函数拟合的标准形式*

## 验证详情

- **验证方式**: 数值仿真对比与理论分析
- **测试系统**: 三导体束架空传输线（Three-conductor bundle），包含地线，具体参数包括：地线半径4.75mm、电阻3.75Ohm/km；导体半径15.29mm、电阻0.0701Ohm/km；束半径230.09mm
- **仿真工具**: 基于EMT仿真原理的数值实现（文中提及ULM和FDCM的实现框架，使用精确状态空间离散化和两步插值方法）
- **验证结果**: RKF在CTWB框架下比VF生成更低阶且精确的模型；恒定T方法因频率点选择不同会导致无源性违反，而传统基于Bode拟合的FD模型在相域精度显著低于现代部分分式展开方法（VF/RKF），这解释了FD模型在时域的不准确性

## 适用边界

### 适用条件

- 适用于理解本文 `Wideband model based on constant transformation matrix and rational Krylov fitting`（2023） 在当前页面抽取范围内讨论的 EMT/电力系统暂态问题。
- 适用于以 vector-fitting 为核心的建模、仿真、等值、控制或稳定性分析场景；具体对象以原文算例和页面“涉及的模型”为准。
- 可作为知识图谱中的方法定位和文献入口，尤其用于追踪：首次证明使用恒定变换矩阵会因频率点选择不同而内在破坏输电线路系统的无源性

### 失效边界

- 不应外推到原文未覆盖的拓扑、控制策略、故障类型、频率范围、硬件平台或实时步长。
- 不应把页面中的“提高、显著、快速、准确”等概括性表述当作定量结论；只有“量化发现”和原文表图可核验的数字才可用于比较。
- 若页面作者、期刊、摘要或验证字段仍不完整，本页只能作为待复核文献入口，不能作为最终证据页引用。

### 关键假设

- 页面内容假设当前 PDF 抽取文本与 frontmatter 的 `sources` 指向同一篇论文。
- 方法结论默认受原文仿真工具、测试系统、参数设置、采样步长和对比基线约束。
- 当前边界层为保守整理：未从原文直接核验的内容不得升级为确定结论。

### 证据缺口

- 具体适用范围仍以原文算例、参数表和验证场景为准，当前页面不应外推到未验证系统。
- 源文件路径：`["EMT_Doc/40/Francois 等 - 2023 - Wideband model based on constant transformation matrix and rational Krylov fitting.pdf"]`；需要深修时应优先核对该 PDF 的首页、摘要、方法和实验表图。
