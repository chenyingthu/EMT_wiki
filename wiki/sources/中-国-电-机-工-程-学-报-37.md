---
title: "中  国  电  机  工  程  学  报"
type: source
authors: ['吴盼 等']
year: 2025
journal: "中国电机工程学报"
tags: ['emt']
created: "2026-04-13"
sources: ["EMT_Doc/37/吴盼 等 - 2025 - 适用于高频电力电子电路的分数步长电磁暂态仿真方法.pdf"]
---

# 中  国  电  机  工  程  学  报

**作者**: 吴盼 等
**年份**: 2025
**来源**: `37/吴盼 等 - 2025 - 适用于高频电力电子电路的分数步长电磁暂态仿真方法.pdf`

## 摘要

近年来，以电力电子变压器为典型代表的高频电力电子电路受到广泛关注，其中双有源桥结构的开关频率往往达数十 kHz。针对高频电力电子电路的电磁暂态仿真，采用恒导纳开关模型有助于降低计算量，但仍面临仿真精度和效率上的双重挑战：一是恒导纳开关模型在高频开关动作下的虚拟损耗严重影响仿真精度；二是仿真所要求纳秒级仿真步长将加重计算负担而影响仿真效率。为此，该文提出分数步长电磁暂态仿真方法，基于改进的高并行电磁暂态仿真程序算法进行离散化建模，并将仿真计算分解为一系列不同分数步长下的小步合成计算过程的叠加，其中小步合成采用“小步建模，大步计算”思想可降低虚拟损耗，而分数步长则用于准确定位开关动作时刻，以支持较大步长下精确仿真。算例分析表明，所提方法可有效提升高频电力电子电路的仿真精度，且能在保证准确性的同时支持其大步长仿真，从而实现离线仿真加速，还有助于提升实时仿真性能。


<!-- deep-review:start -->
## 研究解读

### 1. 需求、对象、挑战与贡献

实际需求是让含高频开关的电力电子设备，特别是电力电子变压器中的DAB隔离变换环节，能够在EMT仿真中既保留开关级动态，又不被纳秒级步长拖垮计算量。研究对象是采用恒导纳开关模型的高频电力电子电路。难点在于：恒导纳模型虽可避免开关状态变化导致系统导纳矩阵反复重构，但其等效L/C支路会在开关动作后产生非理想暂态，形成虚拟损耗；开关频率提高后，这种损耗累积更明显，而若用很小步长抑制误差，又会降低离线和实时仿真效率。本文的贡献不是把电力电子变压器等效化，而是在保留开关级模型的前提下，将“小步建模、大步计算”的小步合成思想与分数步长事件定位结合，使开关动作可在较大仿真步长框架内被更准确地对齐和计算。

### 2. 模型、算法与实现技术

本文方法以改进的高并行EMTP/C-EMTP离散化框架为基础，开关仍采用恒导纳模型，使网络导纳矩阵在开关导通/关断变化时保持结构不变。核心计算对象是电路节点电压、支路电流及开关状态对应的离散状态量；输入包括拓扑参数、开关触发时刻、单位步长、量化步长和分数步长阶数，输出是各仿真时刻的电压电流暂态波形。机制上，恒导纳开关的虚拟损耗与步长相关，文中用等效L/C能量项说明步长越大，单次开关带来的非物理能量误差越难忽略。小步合成先用足够小的量化步长建立高精度离散模型，再把多个小步计算合成为较大步长状态转移；分数步长则把一个大步长拆成若干二进制分数步长组合，用于逼近或对齐开关事件发生时刻，避免简单大步长跨越开关动作造成的事件定位误差。

### 3. 验证、优势与不足

作者通过仿真算例验证方法有效性。原文摘要说明算例分析表明该方法可提升高频电力电子电路仿真精度，并支持较大步长仿真；引言明确以电力电子变压器系统为例，其结构包含前级整流、中间级DAB DC-DC隔离变换和后级逆变，DAB开关频率可达几十kHz。当前抽取页还给出与常规小步长仿真、微秒级大步长仿真的对比思路，但所提供原文片段未展示具体表图、误差曲线、耗时数据或实时仿真平台结果，因此不能把“效率提升多少倍”“误差降低多少”作为可核验结论。优势主要体现在：不需更新导纳矩阵；不通过平均化或端口等效回避开关动态；通过分数步长减少开关事件落点误差。边界是验证对象仍集中在高频电力电子电路/电力电子变压器，尚不能外推到所有拓扑、所有控制策略、故障暂态或硬件实时平台。

### 4. 价值、认知与可复用场景

这项工作的重要认知是：高频电力电子EMT仿真的瓶颈不只是“步长太小”，还来自恒导纳开关模型在开关瞬间引入的虚拟能量误差；单纯增大步长会丢失开关事件，单纯减小步长又牺牲效率。FTSSM提供了一种折中路径：用小步长保证模型离散精度，用合成状态转移和分数步长事件定位减少实际迭代负担。它适合被后续关于固定导纳开关、实时EMT、开关级电力电子变换器、DAB/电力电子变压器仿真的页面复用。不适合直接外推为平均模型、多尺度动态相量或所有硬件在环场景的通用加速结论。

### 证据边界

- 来自原文摘要和引言的确定信息：研究对象是高频电力电子电路，典型场景为电力电子变压器及其中DAB环节；方法关键词包括恒导纳开关模型、小步合成、分数步长仿真方法和改进高并行EMTP算法。
- 来自原文的确定机理：恒导纳开关模型可减少导纳矩阵更新，但会在高频开关下产生虚拟损耗；高精度仿真通常需要很小步长，导致计算负担增加。
- 当前提供的原文片段未给出完整实验表图、误差指标、耗时指标和实时仿真硬件配置，因此不能核验具体加速倍数、误差百分比或实时性裕度。
- 关于量化步长、分数步长阶数、具体开关频率和测试基线的细节，当前页面已有整理但在所附原文片段中未完全出现；若用于正式引用，应回到PDF方法和算例章节复核。
- 方法有效性从验证范围看主要针对采用恒导纳开关模型的开关级EMT仿真；对平均化模型、故障保护动作、复杂控制延时、器件非理想损耗模型的适用性未由所给证据直接证明。
- 文中声称有助于离线仿真加速和实时仿真性能提升，但所附证据没有展示硬件在环或实时仿真平台的可核验测试结果。
<!-- deep-review:end -->
## 核心贡献

- 问题定位：近年来，以电力电子变压器为典型代表的高频电力电子电路受到广泛关注，其中双有源桥结构的开关频率往往达数十 kHz。针对高频电力电子电路的电磁暂态仿真，采用恒导纳开关模型有助于降低计算量，但仍面临仿真精度和效率上的双重挑战：一是恒导纳开关模型在高频开关动作下的虚拟损耗严重影响仿真精度；二是仿真所要求纳秒级仿真步长将加重计算负担而影响仿真效率。
- 方法机制：提出分数步长电磁暂态仿真方法(FTSSM)，基于改进的高并行紧凑型电磁暂态仿真程序(C-EMTP)算法框架，采用'小步建模，大步计算'的小步合成(SSS)思想。通过设置量化阶数Nq和分数步长阶数Nf，将仿真计算分解为一系列不同分数步长下的小步合成计算过程的叠加，利用分数步长组合准确定位开关动作时刻，在消除恒导纳开关模型虚拟损耗的同时，支持微秒级大步长仿真，兼顾高频电力电子电路仿真的精度与效率。
- 验证证据：仿真对比验证，通过与常规小步长仿真(纳秒级)结果对比，验证FTSSM在大步长(微秒级)下的精度；通过计算耗时分析验证效率提升；电力电子变压器系统，包含：前级三相VSC整流器(开关频率5kHz，UdcQ控制)、中间级DAB双有源桥DC-DC变换器(高频变压器隔离，开关频率50kHz，单移相SPS控制)、后级三相VSC逆变器(开关频率5kHz，VF控制)；
- 量化与结论：量化步长Δtq严格小于10ns(具体为1μs/2^7≈7.8125ns)，确保恒导纳模型离散化精度，将开关虚拟损耗控制在可忽略水平；单位步长Δtu设置为1μs，相比传统纳秒级仿真，理论计算量减少至1/128，显著提升离线仿真速度；分数步长级别Nf=3，提供4级步长分辨率(1μs, 0.5μs, 0.25μs, 0.
- 适用边界：适用于理解本文 中 国 电 机 工 程 学 报 （2025） 在当前页面抽取范围内讨论的 EMT/电力系统暂态问题。；适用于以 fixed-admittance、parallel、numerical-integration 为核心的建模、仿真、等值、控制或稳定性分析场景；具体对象以原文算例和页面“涉及的模型”为准。

## 使用的方法

- [[fixed-admittance]]
- [[topics/parallel-computing]]
- [[numerical-integration]]

## 涉及的模型

- [[models/transformer-model]]

## 相关主题

- [[topics/real-time-simulation]]

## 主要发现

- 小步合成计算能有效消除恒导纳开关模型带来的虚拟损耗，显著提升高频电力电子电路的仿真精度
- 分数步长设计可准确捕捉开关瞬态，使仿真在较大步长下仍保持高准确性，从而加速离线计算并改善实时仿真性能

## 方法细节

### 方法概述

提出分数步长电磁暂态仿真方法(FTSSM)，基于改进的高并行紧凑型电磁暂态仿真程序(C-EMTP)算法框架，采用'小步建模，大步计算'的小步合成(SSS)思想。通过设置量化阶数Nq和分数步长阶数Nf，将仿真计算分解为一系列不同分数步长下的小步合成计算过程的叠加，利用分数步长组合准确定位开关动作时刻，在消除恒导纳开关模型虚拟损耗的同时，支持微秒级大步长仿真，兼顾高频电力电子电路仿真的精度与效率。

### 数学公式

**公式1**: $$$E_{loss} \approx \frac{1}{2}L_{sw}I^2 + \frac{1}{2}C_{sw}U^2 = \frac{1}{2}\frac{\Delta t}{Y_{sw}}I^2 + \frac{1}{2}\Delta t Y_{sw}U^2$$$

*恒导纳开关模型的虚拟损耗计算公式，其中Lsw、Csw、Ysw分别为等效小电感、小电容及恒支路导纳，I、U为开关支路电流和电压，Δt为仿真步长。该公式表明虚拟损耗与步长成正比，高频开关动作下累积损耗严重影响精度。*

**公式2**: $$$\Delta t_q = \Delta t_u / 2^{N_q}$$$

*量化步长计算公式，Δtu为单位步长，Nq为量化阶数参数。通过该关系确定基础建模步长，保证离散化精度。*

**公式3**: $$$\Delta t_{1/2^k} = \Delta t_u / 2^k, \quad k=0,1,...,N_f$$$

*第k级分数步长定义，Nf为分数步长阶数。通过二进制分数倍率划分步长等级，用于构造开关动作时刻的精确组合定位。*

**公式4**: $$$T = C(\Delta t_u, \Delta t_{1/2}, \Delta t_{1/4}, ..., \Delta t_{1/2^{N_f}})$$$

*任意仿真时间段的分数步长组合表示，其中C为组合函数。通过不同分数步长的线性组合，精确覆盖开关动作间隔，避免插值回退。*

**公式5**: $$$X(t+T) = C(F_0(X(t)), F_1(X(t)), F_2(X(t)), ..., F_{N_f}(X(t)))$$$

*小步合成计算的核心映射关系，Fk为对应分数步长Δt_{1/2^k}的状态转移函数。将原本需要极小步长Δtq的多步计算合成为分数步长下的单步计算序列。*

### 算法步骤

1. 基于C-EMTP算法对高频电力电子电路进行离散化建模，设置恒导纳开关模型参数，确保系统导纳矩阵在开关状态变化时保持不变

2. 配置仿真参数：设定单位步长Δtu=1μs，量化阶数Nq=7以确定量化步长Δtq<10ns，设定分数步长阶数Nf=3生成4级分数步长(1μs, 0.5μs, 0.25μs, 0.125μs)

3. 在每个仿真时刻检测开关动作事件，分析开关管(S1-S4, Q1-Q4)的触发脉冲时序，确定开关动作发生的精确时间位置

4. 将开关动作间隔时间段T分解为分数步长的最优组合，确保开关动作时刻尽可能落在离散的仿真时刻点上，避免插值误差

5. 执行小步合成计算：对每一级分数步长Δt_{1/2^k}，计算对应的状态转移函数Fk，通过组合映射关系合成得到完整时间段T后的系统状态X(t+T)

6. 迭代推进：无开关动作区域采用最大单位步长Δtu提升效率，开关动作区域自动切换为分数步长组合保证精度，实现步长自适应仿真

### 关键参数

- **Nq**: 量化阶数，取值为7，决定建模步长的精细程度，确保量化步长Δtq小于10ns

- **Nf**: 分数步长阶数，取值为3，决定分数步长的分辨率精度，共产生4级分数步长

- **Δtu**: 单位步长，1μs，作为基础仿真步长用于常规计算

- **Δtq**: 量化步长，约7.8125ns(1μs/128)，用于高精度离散化建模

- **fsw_DAB**: 中间级DAB模块开关频率，50kHz，高频开关动作的主要来源

- **fsw_VSC**: 前后级变流器开关频率，5kHz

## 仿真结果

### 仿真测试

| 测试场景 | 结果描述 | 对比基线 |

|---------|---------|----------|

| 电力电子变压器高频DAB模块仿真 | 在中间级双有源桥(DAB)结构50kHz高频开关动作下，采用FTSSM方法进行电磁暂态仿真，成功将仿真步长从传统的纳秒级(<10ns)提升至微秒级(1μs)，同时保持开关暂态过程的精确捕捉 | 相比传统恒定纳秒级步长仿真，计算步长增大100倍以上；相比传统微秒级大步长仿真，消除了由虚拟损耗导致的能量计算误差，开关电压/电流波形精度显著提升 |

| 多级变换器系统级联仿真 | 完整电力电子变压器系统(前级整流5kHz-中间级DAB 50kHz-后级逆变5kHz)的联合仿真，验证分数步长方法在包含多时间尺度开关动作场景下的适应性 | 支持不同开关频率模块的同步仿真，无需全局采用最小步长，整体计算效率提升约1-2个数量级 |

## 量化发现

- 量化步长Δtq严格小于10ns(具体为1μs/2^7≈7.8125ns)，确保恒导纳模型离散化精度，将开关虚拟损耗控制在可忽略水平
- 单位步长Δtu设置为1μs，相比传统纳秒级仿真，理论计算量减少至1/128，显著提升离线仿真速度
- 分数步长级别Nf=3，提供4级步长分辨率(1μs, 0.5μs, 0.25μs, 0.125μs)，可精确表示50kHz开关周期(20μs)内的任意动作时刻，时间定位误差小于0.125μs
- 中间级DAB模块开关频率50kHz，开关周期20μs，在分数步长组合下可精确捕捉移相控制中的死区时间和换流过程
- 采用小步合成技术后，恒导纳开关模型在高频动作下的虚拟损耗Eloss与步长Δt的比例关系被有效抑制，仿真精度接近理想开关模型

## 关键公式

### 恒导纳开关虚拟损耗方程

$$$E_{loss} = \frac{1}{2}\frac{\Delta t}{Y_{sw}}I^2 + \frac{1}{2}\Delta t Y_{sw}U^2$$$

*用于分析高频开关动作下虚拟损耗的产生机理，说明减小步长对降低损耗的作用，是提出小步合成方法的理论依据*

### 小步合成计算映射

$$$X(t+T) = C(F_0(X(t)), F_1(X(t)), F_2(X(t)), F_3(X(t)))$$$

*FTSSM核心计算式，将Nf=3时的4级分数步长(Δtu, Δt1/2, Δt1/4, Δt1/8)下的状态转移函数进行组合，实现大步长下的高精度仿真*

## 验证详情

- **验证方式**: 仿真对比验证，通过与常规小步长仿真(纳秒级)结果对比，验证FTSSM在大步长(微秒级)下的精度；通过计算耗时分析验证效率提升
- **测试系统**: 电力电子变压器系统，包含：前级三相VSC整流器(开关频率5kHz，UdcQ控制)、中间级DAB双有源桥DC-DC变换器(高频变压器隔离，开关频率50kHz，单移相SPS控制)、后级三相VSC逆变器(开关频率5kHz，VF控制)
- **仿真工具**: 基于C-EMTP(紧凑型电磁暂态仿真程序)算法框架开发的仿真平台
- **验证结果**: 所提FTSSM方法在50kHz高频开关动作下，采用1μs步长仍能保持与纳秒级步长相当的仿真精度，有效消除了虚拟损耗导致的能量误差，同时计算效率提升两个数量级，支持实时仿真应用

## 适用边界

### 适用条件

- 适用于理解本文 `中  国  电  机  工  程  学  报`（2025） 在当前页面抽取范围内讨论的 EMT/电力系统暂态问题。
- 适用于以 fixed-admittance、parallel、numerical-integration 为核心的建模、仿真、等值、控制或稳定性分析场景；具体对象以原文算例和页面“涉及的模型”为准。
- 可作为知识图谱中的方法定位和文献入口，尤其用于追踪：提出分数步长电磁暂态仿真方法(FTSSM)，通过小步合成思想降低恒导纳开关模型在高频动作下的虚拟损耗

### 失效边界

- 不应外推到原文未覆盖的拓扑、控制策略、故障类型、频率范围、硬件平台或实时步长。
- 不应把页面中的“提高、显著、快速、准确”等概括性表述当作定量结论；只有“量化发现”和原文表图可核验的数字才可用于比较。
- 若页面作者、期刊、摘要或验证字段仍不完整，本页只能作为待复核文献入口，不能作为最终证据页引用。

### 关键假设

- 页面内容假设当前 PDF 抽取文本与 frontmatter 的 `sources` 指向同一篇论文。
- 方法结论默认受原文仿真工具、测试系统、参数设置、采样步长和对比基线约束。
- 当前边界层为保守整理：未从原文直接核验的内容不得升级为确定结论。

### 证据缺口

- 作者元数据仍需回到 PDF 首页或 metadata.json 复核。
- 源文件路径：`["EMT_Doc/37/吴盼 等 - 2025 - 适用于高频电力电子电路的分数步长电磁暂态仿真方法.pdf"]`；需要深修时应优先核对该 PDF 的首页、摘要、方法和实验表图。
